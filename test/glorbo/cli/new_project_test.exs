defmodule Glorbo.CLI.NewProjectTest do
  @moduledoc "Stubs — filled in by Plan 02."
  use GlorboTest.CLICase, async: false

  @moduletag :pending

  describe "new project" do
    test "valid <co>/<slug> scaffolds projects/<slug>/README.md" do
      flunk("TODO(plan-02): implement new-project happy path")
    end

    test "re-run is idempotent" do
      flunk("TODO(plan-02): implement new-project idempotency")
    end
  end
end
