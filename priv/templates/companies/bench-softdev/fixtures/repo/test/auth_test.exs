defmodule BenchFixture.AuthTest do
  use ExUnit.Case, async: true

  # The engineer adding a regression test for bugs-1 (session_timeout
  # constant fix) should insert it here. This file is otherwise empty
  # — bench templates start with minimal coverage so improvements are
  # attributable to the benchmark run.
end
