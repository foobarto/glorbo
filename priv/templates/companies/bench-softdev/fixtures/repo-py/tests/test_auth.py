"""Regression tests for ``auth.session_timeout``.

Engineer adding the bugs-py-1 fix should insert a new test
asserting ``session_timeout() == 3600``. File starts empty on
purpose so the added coverage is attributable to the benchmark
run.
"""
