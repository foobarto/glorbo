{
  "name": "glorbo-scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Node deps for the GEP-44 visual-regression harness (ui-baseline.sh + ui-baseline-capture.js + ui-baseline-diff.js). Not part of the Phoenix app's asset pipeline; lives here so contributors don't need a global Playwright install.",
  "scripts": {
    "install-browsers": "playwright install chrome"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  }
}
