---
phase: 01-compilable-skeleton-ci-release-pipeline
plan: 04
type: gap-closure
wave: 4
depends_on: [01-01, 01-02, 01-03]
autonomous: true
gap_closure: true
requirements: []  # No FND-* — this is a definitions-quality fix, not a new capability
source: "Quality review of Phase 1 definitions (CONTEXT.md, RESEARCH.md, VALIDATION.md, 3× PLAN.md) performed after Phase 1 completion. No formal VERIFICATION.md / UAT.md — review was conversational."
files_modified:
  - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md
  - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md
  - .planning/config.json
  - .planning/NOTES.md

must_haves:
  truths:
    - "RESEARCH.md §Pitfall 6 example regex uses the actual repo path `github.com/foobarto/glorbo`, not the pre-correction placeholder `github.com/glorbo/glorbo`"
    - "VALIDATION.md version assertions are version-stable — they accept any `MAJOR.MINOR.PATCH` rather than hard-coding `0.1.0`"
    - "RESEARCH.md §Common Pitfalls contains a new Pitfall 9 documenting Burrito's launcher-extract cache at `~/.local/share/<app>/` and the `rm -rf` workaround required during local iteration"
    - "`.planning/config.json` has `workflow.security_enforcement: true` and `workflow.security_asvs_level: 2` so Phase 3's threat-model gate fires automatically when permissions and budgets land"
    - "`.planning/NOTES.md` exists with one entry documenting the forward question: where dynamic `Glorbo.Agent.Server` instances are hosted — to be resolved in Phase 3 CONTEXT.md"
  artifacts:
    - path: ".planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md"
      provides: "Pitfall 6 regex corrected; new Pitfall 9 (Burrito launcher cache) appended"
      contains: "^https://github.com/foobarto/glorbo/\\\\.github/workflows/"
    - path: ".planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md"
      provides: "Version assertions rewritten as regex matches (3 rows) so future mix.exs version bumps don't invalidate the contract"
      contains: "^\\\\d+\\\\.\\\\d+\\\\.\\\\d+"
    - path: ".planning/config.json"
      provides: "security_enforcement: true + security_asvs_level: 2"
      contains: "\"security_enforcement\": true"
    - path: ".planning/NOTES.md"
      provides: "Forward-gaps log — starts with the dynamic-agent-supervisor shape question"
      contains: "Glorbo.Agent.Server"
  key_links:
    - from: ".planning/config.json"
      to: "/gsd-plan-phase (future phases)"
      via: "Plan-phase workflow step 5.55 reads workflow.security_enforcement; when true, each PLAN.md must include a <threat_model> block"
      pattern: "security_enforcement"
    - from: ".planning/NOTES.md"
      to: ".planning/phases/03-agents-routing-permissions-budgets/03-CONTEXT.md (future)"
      via: "Phase 3 discuss-phase will read NOTES.md entries for forward context"
      pattern: "Phase 3"
---

<objective>
Apply five documentation/config fixes identified in the post-Phase-1 quality review. No code changes. No new functionality. Pure definitions hygiene + one config toggle to arm the security gate for Phase 3+.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md
@.planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md
@.planning/phases/01-compilable-skeleton-ci-release-pipeline/01-03-SUMMARY.md
@.planning/config.json

<rationale>
Defects came from a human (Claude) review of the phase's own planning artifacts after execution completed successfully. Phase 1 code is green and shipped — these gaps are in the *definition* layer:

1. **Stale repo URL in RESEARCH.md §Pitfall 6** — written before user confirmation landed on `foobarto/glorbo`. The working VERIFY.md has the correct URL; research still shows the placeholder. Anyone re-reading pitfalls will copy the wrong regex.

2. **Hard-coded `version == "0.1.0"`** — three rows in VALIDATION.md's Per-Task Verification Map assert the exact string. The test code (executed) reads the version dynamically from `Mix.Project.config()`; the validation CONTRACT still hard-codes the string. On `v0.2.0` bump the contract falsely reports red.

3. **Burrito launcher cache pitfall missing** — RESEARCH.md §Pitfall 7 covered `mix release --overwrite` but not the runtime extract cache at `~/.local/share/<app>/<app>_erts-*/`. Plan 01-03 SUMMARY documents this as a real debugging cost. Promote to a formal pitfall.

4. **Security enforcement off** — plan-phase workflow step 5.55 only fires when `workflow.security_enforcement: true`. Phase 1 is infra (low threat), but Phase 3 (application permissions + kernel ACLs + budgets + approval gates) absolutely needs threat-model blocks in each plan. Set now so we don't forget at plan-time.

5. **Forward gap: dynamic agent supervisor** — `Glorbo.Company.Supervisor` is a plain `Supervisor` with 5 named children per Plan 01 interfaces. But Phase 3 dynamically spawns `Glorbo.Agent.Server` instances — which supervisor hosts them? Likely a nested `DynamicSupervisor` under `Glorbo.Company.Supervisor`, but this is undocumented. Log the open question now so Phase 3 discuss-phase surfaces it instead of the planner re-discovering it mid-plan.
</rationale>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply five gap-closure edits in one atomic commit</name>
  <files>
    .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md
    .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md
    .planning/config.json
    .planning/NOTES.md
  </files>
  <read_first>
    - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md around line 1323 (§Pitfall 6) — see exact current regex string
    - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md around line 1335 (§Pitfall 8, last pitfall — Pitfall 9 appends after it)
    - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md lines 53, 57, 59 — the three rows that hard-code `version == "0.1.0"`
    - .planning/config.json — current shape (no security_enforcement key exists yet; add it under `workflow`)
    - .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-03-SUMMARY.md § "Accomplishments" / § "Decisions Made" — sources for Pitfall 9 copy
  </read_first>
  <behavior>
    Five distinct edits, one commit. Each edit is surgical — no refactoring of surrounding content.
  </behavior>
  <action>
**Edit 1 — RESEARCH.md §Pitfall 6 regex correction.**

Locate the `glorbo/glorbo` in the `How to avoid:` paragraph. Replace with `foobarto/glorbo`. There is exactly one occurrence per `grep -c 'glorbo/glorbo' 01-RESEARCH.md`; verify before edit. The corrected line should read:

```
**How to avoid:** Document the canonical verification regex as `^https://github.com/foobarto/glorbo/\.github/workflows/.+@refs/tags/v.+$` in `VERIFY.md`. Never hard-code a specific tag.
```

**Edit 2 — VALIDATION.md version assertions.**

Replace the three hard-coded `.version == "0.1.0"` assertions with version-stable regex. Target rows (per line numbers in current file):

- Line 53 (01-02-T2): `jq -e '.version == "0.1.0"'` → `jq -e '.version | test("^\\d+\\.\\d+\\.\\d+")'`
- Line 57 (01-03-T2): `jq -e '.version == "0.1.0"'` → `jq -e '.version | test("^\\d+\\.\\d+\\.\\d+")'`
- Line 58 (01-03-T2 `podman run` row): `jq .version` expected `"0.1.0"` — change the row's Automated Command description to: `podman run --rm -v $PWD/burrito_out:/b:ro ubuntu:24.04 /b/glorbo_linux_x86_64 doctor --json | jq -e '.version | test("^\\d+\\.\\d+\\.\\d+")'` → exit 0

Preserve all other table cells (Plan, Wave, Requirement, Test Type, File Exists, Status) unchanged.

**Edit 3 — RESEARCH.md §Pitfall 9 (new, appended).**

Append immediately after the existing Pitfall 8 (line ~1339), before `## Code Examples` section header:

```markdown
### Pitfall 9: Burrito Launcher Extract Cache Persists Across Rebuilds
**What goes wrong:** Developer iterates on `lib/glorbo/*.ex`, rebuilds with `MIX_ENV=prod mix release --overwrite`, runs `./burrito_out/glorbo_linux_x86_64 doctor` — and sees the PREVIOUS build's behavior.
**Why it happens:** Burrito extracts the bundled ERTS + BEAM + application archive to `~/.local/share/<app>/<app>_erts-<version>_<app-version>/` on first launch, then reuses the extracted directory on subsequent runs for speed. Rebuilding the binary does NOT invalidate the cached extract — the wrapper's hash-check logic compares application versions, not content hashes.
**How to avoid:** During local iteration, remove the extract dir between rebuilds:
```bash
rm -rf ~/.local/share/glorbo/
```
Or, for paranoid iteration, script the full cycle:
```bash
rm -rf ~/.local/share/glorbo/ burrito_out/ _build/prod/
MIX_ENV=prod mix release --overwrite
./burrito_out/glorbo_linux_x86_64 doctor
```
CI is immune: runners are ephemeral, so the cache never exists.
**Warning signs:** Behavior changes don't appear in binary output; `./glorbo doctor --json` shows a version field that doesn't match `Mix.Project.config()[:version]`; binary appears to run stale code despite fresh `file mtime`.
**Reference:** Plan 01-03 execution hit this during Wave 3 Task 2 smoke testing; see `01-03-SUMMARY.md` § "Decisions Made" → "Burrito build cache NOT cleaned between local iterations."
```

**Edit 4 — .planning/config.json security enforcement.**

In the `workflow` object, add two new keys (place after `"code_review_depth"` and before the closing `}` of the `workflow` block):

```json
"security_enforcement": true,
"security_asvs_level": 2,
"security_block_on": "high"
```

Use `node -e "const fs=require('fs'); const p='.planning/config.json'; const c=JSON.parse(fs.readFileSync(p)); c.workflow.security_enforcement=true; c.workflow.security_asvs_level=2; c.workflow.security_block_on='high'; fs.writeFileSync(p, JSON.stringify(c,null,2)+'\\n');"` to guarantee valid JSON after the edit.

**Edit 5 — Create .planning/NOTES.md.**

Write a new file with a single initial entry:

```markdown
# Project Forward-Notes

Running log of forward questions and deferred architectural decisions surfaced during
planning/execution but not owned by the current phase. Each entry is consumed by the
discuss-phase workflow for the phase that owns the decision.

## N-001 — Dynamic agent supervisor shape (Phase 3)

**Surfaced:** 2026-04-15, during Phase 1 post-completion quality review.
**Owning phase:** Phase 3 (Agents, Routing, Kernel Permissions, Budgets).

**Question:** `Glorbo.Company.Supervisor` is a plain `Supervisor` with 5 named static
children (FileWatcher, Router, Scheduler, BudgetTracker, AuditLog), per Phase 1 Plan
01 interfaces. Phase 3 spawns `Glorbo.Agent.Server` instances *dynamically* per Director
action. Where do those GenServers live in the tree?

**Leading hypothesis:** A nested `DynamicSupervisor` under `Glorbo.Company.Supervisor`,
e.g. `Glorbo.Company.AgentSupervisor`, added as a sixth static child. This preserves
the crash-isolation invariant from CLAUDE.md: agent crash → restarted by AgentSupervisor
(one_for_one), company crash → all agents + siblings restarted together by
CompanySupervisor, other companies unaffected (they're separate branches under the
top-level `Glorbo.CompanySupervisor :: DynamicSupervisor`).

**To resolve in Phase 3 CONTEXT.md:**
- Confirm the nested-DynamicSupervisor approach or document an alternative
- Specify the AgentSupervisor restart strategy (one_for_one vs rest_for_one)
- Document the contract: how does the Router address specific agents
  (via registry, via-tuple, or direct pid)?

**Blocks:** Phase 3 planner cannot write the AgentSupervisor spec without this decision.
Not blocking Phase 2 (Phase 2 doesn't spawn agents).

---
```

**Final check — one commit message with all 5 edits.**

```bash
git add .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md \
        .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md \
        .planning/config.json \
        .planning/NOTES.md

git commit -m "docs(01-04): close 5 gaps from Phase 1 definitions review

- RESEARCH.md §Pitfall 6: fix stale repo URL in example regex
  (glorbo/glorbo → foobarto/glorbo)
- VALIDATION.md: relax 3 version assertions from == \"0.1.0\" to
  MAJOR.MINOR.PATCH regex so future mix.exs version bumps don't
  invalidate the validation contract
- RESEARCH.md §Pitfall 9: document Burrito launcher extract cache
  at ~/.local/share/<app>/ that caused Wave 3 debugging cost
- config.json: enable workflow.security_enforcement + ASVS level 2
  so Phase 3's plan-phase step 5.55 fires threat-model gates on each
  PLAN.md (permissions + budgets land there)
- NOTES.md: new forward-notes log; first entry captures dynamic
  agent supervisor shape question for Phase 3 discuss-phase
"
```
  </action>
  <verify>
    <automated>
      # Edit 1 — regex corrected in RESEARCH.md
      grep -q 'github.com/foobarto/glorbo/\\.github/workflows/' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md &&
      ! grep -q 'github.com/glorbo/glorbo/\\.github/workflows/' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md &&

      # Edit 2 — VALIDATION.md has regex-match, no hard-coded 0.1.0 in version assertions
      grep -q '.version | test' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md &&
      ! grep -q '.version == "0.1.0"' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-VALIDATION.md &&

      # Edit 3 — Pitfall 9 present with correct anchor text
      grep -q 'Pitfall 9: Burrito Launcher Extract Cache' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md &&
      grep -q 'rm -rf ~/.local/share/glorbo/' .planning/phases/01-compilable-skeleton-ci-release-pipeline/01-RESEARCH.md &&

      # Edit 4 — config.json security keys present AND file is valid JSON
      node -e "const c=require('./.planning/config.json'); process.exit(c.workflow.security_enforcement===true && c.workflow.security_asvs_level===2 && c.workflow.security_block_on==='high' ? 0 : 1)" &&

      # Edit 5 — NOTES.md created with N-001 entry
      test -f .planning/NOTES.md &&
      grep -q 'N-001' .planning/NOTES.md &&
      grep -q 'Glorbo.Agent.Server' .planning/NOTES.md &&
      grep -q 'Phase 3' .planning/NOTES.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `01-RESEARCH.md` contains `foobarto/glorbo` in the §Pitfall 6 example regex AND no longer contains the placeholder `github.com/glorbo/glorbo/\.github/`
    - `01-VALIDATION.md` no longer contains `.version == "0.1.0"` anywhere; instead has `.version | test(` regex matcher in 3 rows (01-02-T2, 01-03-T2 local, 01-03-T2 podman)
    - `01-RESEARCH.md` §Common Pitfalls has 9 pitfalls (grep `### Pitfall` returns 9 matches)
    - `01-RESEARCH.md` Pitfall 9 references Plan 01-03-SUMMARY.md and includes the `rm -rf ~/.local/share/glorbo/` recovery command
    - `.planning/config.json` parses as valid JSON AND `workflow.security_enforcement === true` AND `workflow.security_asvs_level === 2` AND `workflow.security_block_on === "high"`
    - `.planning/NOTES.md` exists, contains a header `# Project Forward-Notes`, contains entry `## N-001`, mentions `Glorbo.Agent.Server`, and names Phase 3 as the owning phase
    - Exactly one commit created with subject `docs(01-04): close 5 gaps from Phase 1 definitions review`
    - No other files modified (check with `git show HEAD --stat` — should list exactly 4 files)
    - Full suite still green: `mix test && mix credo --strict && mix format --check-formatted && mix compile --warnings-as-errors` (this plan doesn't touch code — regression check only)
  </acceptance_criteria>
  <done>
    All five gap items from the Phase 1 definitions review are resolved in the artifacts themselves. The validation contract is version-stable. Phase 3's threat-model gate is armed. The forward supervisor-shape question is logged where Phase 3 discuss-phase will find it. Phase 1 code and tests are untouched.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (none — this plan does not address any FND-* requirement; it's a retrospective definitions fix). `/gsd-verify-work` should skip this plan when iterating phase-level must_haves.

Document-level checks:
- Quality-review items 1, 2, 3 (revised numbering: Pitfall 6 regex, version assertions, Pitfall 9) addressed in artifacts
- Config items 4 (security_enforcement) armed for next planning phase
- Forward-gap item 5 logged in NOTES.md

Items from the review marked "retrospective — not fixing":
- Plan 01-01 Task 1 overload — work succeeded, no corrective action
- Plan 01-03 Task 3 autonomous contract deviation — work succeeded, post-facto remediation would be rewriting a completed plan
</verification>

<success_criteria>
- Single atomic commit with all 5 edits
- `git show HEAD --stat` lists exactly 4 files modified/created
- All automated verify commands pass
- Test suite still 52 green / 0 fail (no code changed)
- Credo / format / compile --warnings-as-errors still pass
- No follow-up needed — this closes the Phase 1 review loop
</success_criteria>

<output>
Create `.planning/phases/01-compilable-skeleton-ci-release-pipeline/01-04-SUMMARY.md` after the commit lands, listing:
- The 5 edits with a one-line summary each
- Confirmation that code is untouched (test counts unchanged)
- Which items from the quality review were deliberately NOT fixed and why (retrospective, cost exceeds benefit)
- Closes-the-loop statement: Phase 1 definitions are now production-ready for reference by Phase 2+.
</output>
