---
phase: 03
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - lib/glorbo/company/router.ex
  - lib/glorbo/company/scheduler.ex
  - lib/glorbo/company/budget_tracker.ex
  - lib/glorbo/budget/ledger.ex
  - config/llm_rates.exs
  - config/network_policy.exs
  - test/glorbo/company/router_test.exs
  - test/glorbo/company/scheduler_test.exs
  - test/glorbo/company/budget_tracker_test.exs
  - test/glorbo/budget/ledger_test.exs
autonomous: true
requirements: [AGT-02, AGT-03, AGT-05, SEC-01, SEC-05]
tags: [router, scheduler, budget, application-layer-enforcement, cron, upsert]

locked_decisions:
  - "Cents-only budget arithmetic — inherited from Plan 03-01 D-31 lock; `Glorbo.Budget.Ledger.record!/1` accepts `cost_usd_cents :: non_neg_integer()` only, never floats."
  - "Router rejects `agents:create` categorically (D-22 + AGT-05) — no agent may possess this permission in v0.0.1; the Router's permission check prevents any routed payload whose `to:` target would write `agents/<new>/agent.md`, regardless of sender permissions."
  - "Scheduler recomputes next-run from wall-clock on every firing (D-24 + Pitfall 3) — no cumulative Process.send_after drift."
  - "BudgetTracker is the SOLE writer of the `budgets` table — tests that need direct Ecto writes still go through `BudgetTracker.record/3` to preserve the upsert invariant."
  - "Hard-stop is pre-dispatch only (D-32) — mid-invocation kills are out of scope for v0.0.1; once `Agent.Server` has called `BudgetTracker.check_budget/2 == :ok`, the dispatch runs to completion even if the ongoing call pushes spending over cap. Post-hoc audit flags the overshoot."

must_haves:
  truths:
    - "`Glorbo.Company.Router.route/2` returns `:ok` for a message from a sender with `chat:write:general` writing to channel `general`"
    - "`Glorbo.Company.Router.route/2` returns `{:error, {:permission_denied, _}}` for a sender without matching permission AND writes a rejection file to `history/<id>.rejected.md` + appends `message.reject` audit event"
    - "`Glorbo.Company.Router.route/2` rejects any payload whose `to:` resolves to a recipient whose creation would be `agents/<new>/agent.md` — `{:error, {:permission_denied, \"agents:create:*\"}}` + `agents.create_blocked` / `permission.denied` audit"
    - "`Glorbo.Company.Router.route/2` scans channel writes for `@<agent>` tokens and writes a synthetic mention message to `agents/<target>/inbox/mentions/<ts>-<channel>.md` (D-21)"
    - "`Glorbo.Company.Scheduler.start_link/1` parses each agent's `heartbeat:` cron expression via `Crontab.CronExpression.Parser.parse!/1` and computes next-run via `Crontab.Scheduler.get_next_run_date/2`"
    - "`Glorbo.Company.Scheduler` emits `{:heartbeat, agent_slug}` messages to a dep-injected dispatch_fun at the right wall-clock moment (tested by advancing a fake clock + driving `Process.send_after` manually)"
    - "`Glorbo.Budget.Ledger.record/3` performs an atomic upsert that correctly sums across concurrent writes (unique index `(agent_slug, year_month)` + `on_conflict: [inc: [...]]` — RESEARCH Pitfall 2)"
    - "`Glorbo.Company.BudgetTracker.check_budget/2` returns `:ok` when used_cents < alert_threshold_cents"
    - "`Glorbo.Company.BudgetTracker.check_budget/2` returns `{:alert, used, cap}` at threshold AND writes `alerts/<agent>-budget.md` file"
    - "`Glorbo.Company.BudgetTracker.check_budget/2` returns `{:stop, used, cap}` when used_cents >= cap_cents AND emits `budget.hard_stop` audit event"
    - "`config/llm_rates.exs` exposes a `%{provider => %{model => %{input_usd_per_mtok, output_usd_per_mtok}}}` rate table covering claude-code (opus-4, sonnet-4), gemini-cli (2.5-pro, 2.5-flash), and codex (gpt-5, o3) — used by `Glorbo.Budget.Ledger.compute_cost_cents/4`"
    - "`config/network_policy.exs` exposes a base allowlist `%{claude_code: [...], gemini_cli: [...], codex: [...]}` of allowed hostnames per provider (D-16); file is read-only consumed by Plan 03-05's Proxy"
  artifacts:
    - path: "lib/glorbo/company/router.ex"
      provides: "Per-company router — permission checks + channel append + mention routing + agent-create rejection"
      exports: ["start_link/1", "route/2"]
    - path: "lib/glorbo/company/scheduler.ex"
      provides: "Cron-driven heartbeat scheduler — one-off Process.send_after per agent, recomputed each firing"
      exports: ["start_link/1", "register/3", "unregister/2"]
    - path: "lib/glorbo/company/budget_tracker.ex"
      provides: "Pre-dispatch budget check + alert file + hard-stop"
      exports: ["start_link/1", "check_budget/2", "record/3", "reload_config/1"]
    - path: "lib/glorbo/budget/ledger.ex"
      provides: "Pure-logic layer over `Glorbo.Budget` schema — atomic upsert + cost computation from rate table"
      exports: ["record!/1", "compute_cost_cents/4", "month_bucket/1", "fetch/2"]
    - path: "config/llm_rates.exs"
      provides: "Per-{provider, model} USD/Mtok rate table"
      contains: "claude-code"
    - path: "config/network_policy.exs"
      provides: "api-only base allowlist per provider"
      contains: "api.anthropic.com"
  key_links:
    - from: "lib/glorbo/company/router.ex"
      to: "lib/glorbo/security/acl_mapper.ex"
      via: "ACLMapper.check_action/2 before every route"
      pattern: "ACLMapper\\.check_action"
    - from: "lib/glorbo/company/router.ex"
      to: "lib/glorbo/company/audit_log.ex"
      via: "AuditLog.append/2 for message.route / message.reject / permission.denied"
      pattern: "AuditLog\\.append"
    - from: "lib/glorbo/company/budget_tracker.ex"
      to: "lib/glorbo/budget/ledger.ex"
      via: "Ledger.record!/1 (upsert) + Ledger.fetch/2 (monthly total)"
      pattern: "Glorbo\\.Budget\\.Ledger\\."
    - from: "lib/glorbo/budget/ledger.ex"
      to: "lib/glorbo/budget.ex"
      via: "Repo.insert/2 via Budget.changeset/2 with on_conflict inc"
      pattern: "%Glorbo\\.Budget\\{"

threat_model:
  trust_boundaries:
    - boundary: "agent outbox (filesystem) → Router parser"
      description: "LLM-authored outbox files parsed as YAML frontmatter; malformed / billion-laughs / injected `to:` targets arrive as untrusted input"
    - boundary: "`agent.md` frontmatter `heartbeat:` string → Crontab.CronExpression.Parser"
      description: "User-authored cron string parsed at Scheduler.register; malformed string must produce `{:error, reason}` without crashing Scheduler"
    - boundary: "Python worker usage.json (filesystem, via Watcher) → BudgetTracker.record"
      description: "Dormant v0.0.1 path (worker is disabled in CLI-agent mode) — but Watcher ROUTE is still present; defensive parsing prevents future container-runtime reactivation from crashing"
    - boundary: "Outbox message `to:` field → agent-create guard"
      description: "Attacker-controlled `to:` pointing at `agents/<new>/` must be rejected BEFORE permission dispatch (belt-and-braces with SEC-01)"
  threats:
    - id: "T-03-07"
      category: "Elevation of Privilege"
      component: "lib/glorbo/company/router.ex"
      disposition: "mitigate"
      mitigation: "Router calls `ACLMapper.check_action/2` against sender's declared permissions BEFORE any filesystem write; fail-closed (default :error); explicit agents:create pattern match rejects any `to:` starting with `agents/` AND not matching an existing agent's inbox — even if the sender somehow had `agents:create:*` (which no agent has)"
    - id: "T-03-08"
      category: "Tampering"
      component: "lib/glorbo/budget/ledger.ex"
      disposition: "mitigate"
      mitigation: "Ecto `on_conflict: [inc: [prompt_tokens: ..., completion_tokens: ..., cost_usd_cents: ...]]` uses atomic SQL increment; unique index on `(agent_slug, year_month)` enforces single row; concurrent records cannot lose updates (RESEARCH Pitfall 2 + Plan 03-01 schema)"
    - id: "T-03-09"
      category: "Denial of Service"
      component: "lib/glorbo/company/router.ex"
      disposition: "mitigate"
      mitigation: "Router subscribes to PubSub (emitted in Plan 03-05) with bounded mailbox; pending-outbox map capped at @max_pending 10_000 (mirrors Watcher WR-07 pattern). Messages beyond cap dropped + warning logged"
    - id: "T-03-10"
      category: "Repudiation"
      component: "lib/glorbo/company/router.ex"
      disposition: "mitigate"
      mitigation: "Every route + reject + mention emits an audit event keyed per AUDIT_EVENTS.md (message.route, message.reject, permission.denied). Audit log is append-only (CLAUDE.md invariant). Rejection artifacts land at `history/<id>.rejected.md` on filesystem for post-hoc inspection"
    - id: "T-03-11"
      category: "Denial of Service"
      component: "lib/glorbo/company/scheduler.ex"
      disposition: "mitigate"
      mitigation: "`Crontab.CronExpression.Parser.parse/1` wrapped in try/rescue; malformed expressions return `{:error, reason}` + `provider.unknown`-style audit (new key `scheduler.invalid_cron`); the offending agent is skipped, other agents continue. Pitfall 3 (wall-clock recompute) prevents send_after drift under long-running VMs"
    - id: "T-03-12"
      category: "Spoofing"
      component: "lib/glorbo/company/router.ex"
      disposition: "mitigate"
      mitigation: "Sender identity derived from outbox file path (`agents/<slug>/outbox/*.md`) NOT from `from:` field in the message body. A malicious agent cannot forge its slug — the slug is the directory name Elixir owns (agent has only write permission to its own outbox via bwrap in Plan 03-05, and CANNOT create sibling directories)"
---

<objective>
Fill in Phase-1 stubs for the three per-company GenServers (`Router`, `Scheduler`, `BudgetTracker`) plus introduce the `Budget.Ledger` pure-logic layer that wraps the `Glorbo.Budget` schema shipped in Plan 03-01. This is the "brain" half of Phase 3's brain-vs-body split:

- **Brain (this plan):** application-layer permission check (SEC-01), one-way-flow Router mediation (AGT-03), agent-create rejection (AGT-05), cron-driven heartbeat wake (AGT-02), monthly USD budget tracking with alert + hard-stop (SEC-05).
- **Body (Plan 03-03):** agent server + dispatch + skills + CLI adapters.
- **Wiring (Plan 03-05):** extend Company.Supervisor 2→6 children + extend Watcher to broadcast PubSub events.

Purpose: Keep every piece unit-testable in isolation before the supervision tree + bwrap sandbox land in Plan 03-05. Every module here accepts dep-injected callbacks for audit/filesystem/PubSub so tests never hit real IO beyond the SQLite sandbox.

Output: Three GenServer bodies (no longer stubs) + one pure-logic module + two config files + four unit-test files (~60 tests total).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-01-SUMMARY.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md
@CLAUDE.md
@DESIGN.md
@lib/glorbo/company/router.ex
@lib/glorbo/company/scheduler.ex
@lib/glorbo/company/budget_tracker.ex
@lib/glorbo/company/audit_log.ex
@lib/glorbo/security/acl_mapper.ex
@lib/glorbo/budget.ex
@lib/glorbo/filesystem/frontmatter.ex
@lib/glorbo/filesystem/watcher.ex

<interfaces>
<!-- Stable contracts that Plan 03-03 (Agent.Server) and Plan 03-05 (supervisor wiring) consume. Executor builds these first, then implements against them. -->

Router public API:
```elixir
# lib/glorbo/company/router.ex
@type outbox_msg :: %{
        sender: String.t(),                 # agent slug — derived from outbox path
        sender_permissions: [ACLMapper.permission()],
        to: String.t(),                     # "chat:general" | "agent:ceo" | "broadcast:*"
        body: String.t(),                   # markdown (already frontmatter-stripped)
        raw_path: String.t(),               # source outbox file, for rejection move
        msg_id: String.t()                  # derived from filename stem
      }

@type route_result ::
        :ok
        | {:error, {:permission_denied, missing :: String.t()}}
        | {:error, {:agent_create_blocked, String.t()}}
        | {:error, {:unknown_recipient, String.t()}}
        | {:error, {:invalid_message, term()}}

@spec route(GenServer.server(), outbox_msg()) :: route_result()
@spec start_link(opts :: keyword()) :: GenServer.on_start()
# opts: :name (required), :company (required), :base (default ~/.glorbo),
#       :audit_fun (default &AuditLog.append/2), :fs_fun (default File module
#       functions), :scheduler_fun (default &Scheduler.register/3) — all
#       dep-injectable for unit tests.
```

Scheduler public API:
```elixir
# lib/glorbo/company/scheduler.ex
@type agent_reg :: %{
        agent_slug: String.t(),
        cron: String.t(),                   # "*/30 * * * *"
        dispatch_fun: (trigger :: atom() -> any())
      }

@spec register(GenServer.server(), String.t(), agent_reg()) ::
        :ok | {:error, :invalid_cron | term()}
@spec unregister(GenServer.server(), String.t()) :: :ok
@spec start_link(opts :: keyword()) :: GenServer.on_start()
# opts: :name (required), :company (required), :clock_fun (default &DateTime.utc_now/0),
#       :send_after_fun (default &Process.send_after/3) — fake-clock + timer tests.
```

BudgetTracker public API:
```elixir
# lib/glorbo/company/budget_tracker.ex
@type budget_state :: :ok | {:alert, used_cents :: pos_integer(), cap_cents :: pos_integer()} | {:stop, used_cents :: pos_integer(), cap_cents :: pos_integer()}
@type usage_record :: %{
        agent_slug: String.t(),
        provider: String.t(),
        model: String.t(),
        prompt_tokens: non_neg_integer(),
        completion_tokens: non_neg_integer(),
        task_id: String.t()
      }

@spec check_budget(GenServer.server(), agent_slug :: String.t()) :: budget_state()
@spec record(GenServer.server(), usage_record(), opts :: keyword()) :: :ok
# opts: :month (default current UTC month "YYYY-MM") — fake-month for tests.

@spec reload_config(GenServer.server()) :: :ok
@spec start_link(opts :: keyword()) :: GenServer.on_start()
# opts: :name (required), :company (required), :base, :budgets_fun (dep-inject
#       per-agent cap from agent.md frontmatter; default reads filesystem),
#       :alert_threshold_pct (default 80), :repo (default Glorbo.Repo),
#       :audit_fun (default &AuditLog.append/2), :fs_fun.
```

Budget.Ledger public API:
```elixir
# lib/glorbo/budget/ledger.ex
@spec record!(usage_record()) :: Glorbo.Budget.t()
# Atomic upsert via Ecto on_conflict:[inc: [...]] targeting [:agent_slug, :year_month].
# Raises on changeset validation failure.

@spec compute_cost_cents(
        provider :: String.t(),
        model :: String.t(),
        prompt_tokens :: non_neg_integer(),
        completion_tokens :: non_neg_integer()
      ) :: non_neg_integer()
# Looks up rate from Application.get_env(:glorbo, :llm_rates); returns 0 if model
# not in table (with Logger.warning; no exception — budget tracking is best-effort).

@spec month_bucket(DateTime.t() | Date.t()) :: String.t()
# Returns "YYYY-MM" UTC.

@spec fetch(agent_slug :: String.t(), year_month :: String.t()) :: Glorbo.Budget.t() | nil
# Plain Repo.get_by wrapper — convenience for BudgetTracker.
```

Config contracts:
```elixir
# config/llm_rates.exs — compiled into Application env
import Config

config :glorbo, :llm_rates, %{
  "claude-code" => %{
    "claude-opus-4-6"    => %{input_usd_per_mtok: 15.00, output_usd_per_mtok: 75.00},
    "claude-sonnet-4-5"  => %{input_usd_per_mtok:  3.00, output_usd_per_mtok: 15.00}
  },
  "gemini-cli" => %{
    "gemini-2.5-pro"   => %{input_usd_per_mtok:  1.25, output_usd_per_mtok: 10.00},
    "gemini-2.5-flash" => %{input_usd_per_mtok:  0.30, output_usd_per_mtok:  2.50}
  },
  "codex" => %{
    "gpt-5"   => %{input_usd_per_mtok: 10.00, output_usd_per_mtok: 30.00},
    "o3-mini" => %{input_usd_per_mtok:  3.00, output_usd_per_mtok: 12.00}
  }
}
```

```elixir
# config/network_policy.exs — api-only base allowlist (D-16)
import Config

config :glorbo, :network_policy, %{
  api_only_base_allowlist: %{
    "claude-code" => ~w(api.anthropic.com claude.ai platform.claude.com sentry.io statsig.anthropic.com),
    "gemini-cli" => ~w(generativelanguage.googleapis.com oauth2.googleapis.com accounts.google.com aiplatform.googleapis.com cloudcode-pa.googleapis.com),
    "codex"      => ~w(api.openai.com auth.openai.com chatgpt.com)
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget.Ledger + llm_rates config + atomic upsert coverage</name>
  <files>
    lib/glorbo/budget/ledger.ex,
    config/llm_rates.exs,
    config/config.exs,
    test/glorbo/budget/ledger_test.exs
  </files>
  <read_first>
    lib/glorbo/budget.ex (Plan 03-01 schema — composite unique index `budgets_agent_slug_year_month_index`, cents-only fields),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pitfall 2 full text — Ecto `on_conflict: [inc: [...]]` atomic increment pattern; Code Examples §"Claude Code JSONL usage entry shape" for concrete token-count provenance the Ledger receives),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-29 usage source, D-30 cost calc, D-31 ledger shape, D-32 hard-stop semantics),
    config/config.exs (existing import/config pattern; verify how Phase 2 imported its config files),
    test/support/data_case.ex (Phase 1 — Ecto SQL Sandbox pattern for async tests)
  </read_first>
  <behavior>
    - Test 1: `compute_cost_cents("claude-code", "claude-opus-4-6", 1_000_000, 0)` returns exactly 1500 (input $15/Mtok × 1 Mtok = $15.00 = 1500 cents) — integer math, no float drift.
    - Test 2: `compute_cost_cents("claude-code", "claude-opus-4-6", 0, 1_000_000)` returns 7500 (output $75/Mtok × 1 Mtok = $75.00).
    - Test 3: `compute_cost_cents("claude-code", "claude-opus-4-6", 2_500_000, 500_000)` returns `round(2.5 * 15.00 * 100) + round(0.5 * 75.00 * 100) == 3750 + 3750 == 7500`.
    - Test 4: `compute_cost_cents("gemini-cli", "gemini-2.5-pro", 500_000, 0)` returns 63 (0.5 * 1.25 * 100 = 62.5 → `round/1` banker's-round to even → 62 OR 63 depending on implementation; document the exact expectation: **use `:math.round(x) |> trunc()` with explicit half-up via `trunc(x + 0.5)` for non-negative inputs**; expect 63).
    - Test 5: `compute_cost_cents("unknown-provider", "some-model", 1000, 1000)` returns 0 AND emits a `Logger.warning` (captured via `ExUnit.CaptureLog`). **Does not raise.**
    - Test 6: `compute_cost_cents("claude-code", "unlisted-model", 1000, 1000)` returns 0 AND emits a warning.
    - Test 7: `month_bucket(~U[2026-04-16 12:00:00Z])` returns `"2026-04"`.
    - Test 8: `month_bucket(~U[2026-12-31 23:59:59Z])` returns `"2026-12"`.
    - Test 9: `month_bucket(~D[2026-01-01])` returns `"2026-01"` (Date branch).
    - Test 10: `record!/1` inserts a new row when none exists for `{agent_slug, year_month}` — `Repo.get_by(Budget, agent_slug: "alice", year_month: "2026-04")` returns a row with matching token counts + cost.
    - Test 11: `record!/1` called twice with the same `{agent_slug, year_month}` produces ONE row with summed tokens + summed cost_usd_cents (atomic upsert behavior). Verified by `Repo.aggregate(Budget, :count)` == 1 AND column sums match.
    - Test 12: `record!/1` called concurrently from 10 `Task.async/1` processes (1 kilotok each) produces exactly one row with prompt_tokens == 10_000, completion_tokens == 10_000 — the `on_conflict: [inc: [...]]` race-free test. Uses `Ecto.Adapters.SQL.Sandbox` with `{:shared, self()}` for cross-task access.
    - Test 13: `fetch("alice", "2026-04")` returns the Budget struct; `fetch("alice", "2025-01")` returns `nil`.
    - Test 14: `record!/1` with `prompt_tokens: -1` raises `Ecto.ChangeError` (changeset validation trips `validate_number :greater_than_or_equal_to: 0`).
  </behavior>
  <action>
    1. Create `config/llm_rates.exs` with the exact `%{provider => %{model => %{input_usd_per_mtok, output_usd_per_mtok}}}` table shown in `<interfaces>` above. Verify rates from the following sources per RESEARCH:
       - claude-opus-4-6: Anthropic pricing page 2026-Q2 $15/$75 per Mtok (input/output).
       - claude-sonnet-4-5: $3/$15 per Mtok.
       - gemini-2.5-pro: Google AI pricing $1.25/$10 per Mtok.
       - gemini-2.5-flash: $0.30/$2.50 per Mtok.
       - gpt-5: OpenAI $10/$30 per Mtok (estimated from current gpt-4-turbo pricing; **if researcher prices are outdated, take the 2026-04-16 snapshot and record in a comment at top of the file**).
       - o3-mini: $3/$12 per Mtok.
       These numbers will drift; the file header comment must state "**Verified: 2026-04-16**. Re-verify before each Glorbo release; out-of-date rates produce quiet undercounting (user accepts per D-30)."

    2. Append `import_config "llm_rates.exs"` and `import_config "network_policy.exs"` to `config/config.exs` so the Application env is populated at boot AND in test. (Check the current `config/config.exs` — if it already uses `import_config "#{config_env()}.exs"`, add the two new imports at the top level instead of inside the env branch.)

    3. Create `config/network_policy.exs` with the allowlist shown in `<interfaces>`. Same header comment pattern as llm_rates.

    4. Create `lib/glorbo/budget/ledger.ex` as a pure-logic module (no GenServer, no IO except Repo):
       - `compute_cost_cents/4`: look up rate via `Application.get_env(:glorbo, :llm_rates)`, compute `prompt_tokens / 1_000_000 * input_usd_per_mtok * 100 + completion_tokens / 1_000_000 * output_usd_per_mtok * 100`, apply `trunc(x + 0.5)` rounding for non-negatives, return integer cents. On missing provider OR missing model: `Logger.warning/1` + return 0 (budget undercount is acceptable per D-30; the alternative — raising — would break dispatch).
       - `month_bucket/1`: accepts `DateTime.t()` OR `Date.t()`, uses `Calendar.strftime/2` or manual `"#{year}-#{zero-padded month}"`.
       - `record!/1`: builds `%Budget{}` via `Budget.changeset/2` with cast of `{agent_slug, year_month, prompt_tokens, completion_tokens, cost_usd_cents}`. Writes via `Repo.insert(budget, on_conflict: [inc: [prompt_tokens: delta_prompt, completion_tokens: delta_comp, cost_usd_cents: delta_cost]], conflict_target: [:agent_slug, :year_month])`. **Important: the `inc:` values are the DELTA from this record call, not the total — Ecto runs `SET prompt_tokens = budgets.prompt_tokens + $1` under the hood.** Returns the Budget struct (via `{:ok, b}` branch) or raises on `{:error, changeset}` — unwrap with `Repo.insert!/2` equivalent. Verify by running a unit test that `Repo.one(from b in Budget, where: b.agent_slug == ^slug, select: b.cost_usd_cents)` after two records equals the sum.
       - `fetch/2`: `Repo.get_by(Budget, agent_slug: slug, year_month: ym)`.

    5. Create `test/glorbo/budget/ledger_test.exs` using `Glorbo.DataCase` (Phase 1 sandbox). Cover behaviors 1–14. Use `ExUnit.CaptureLog.capture_log/1` for Test 5 + 6 to assert the warning. Test 12 uses `Ecto.Adapters.SQL.Sandbox.mode(Glorbo.Repo, {:shared, self()})` + 10 concurrent Tasks.

    6. Run `mix format`, `mix credo --strict lib/glorbo/budget/ledger.ex`, `mix compile --warnings-as-errors`, `mix test test/glorbo/budget/ledger_test.exs`.

    Why pure module not GenServer: `record!/1` is stateless — all state lives in the SQLite row, with DB-level atomicity via unique index + on_conflict. A GenServer would serialize writes (bottleneck) without adding any safety. RESEARCH §"Pattern: Budget upsert with atomic increment" — pure fn + Ecto is the BEAM-idiomatic answer.

    Why integer cents not Decimal: Plan 03-01 locked `cost_usd_cents :: non_neg_integer()`. SUM aggregation in Phase 4 dashboards must be float-drift-free. Decimal would add a dep + serialization complexity without solving a real problem at USD-cents granularity.

    Why Logger.warning (not raise) on missing model: a misconfigured `agent.md` pointing at an unlisted model should NOT crash the dispatch pipeline. The warning is loud enough (appears in stdout.log + audit via Phase 2 logger) and the user accepts the undercount per D-30 ("Glorbo owns the mapping").
  </action>
  <verify>
    <automated>mix test test/glorbo/budget/ledger_test.exs --color --trace 2>&1 | tail -40 && mix credo --strict lib/glorbo/budget/ledger.ex && mix format --check-formatted config/llm_rates.exs config/network_policy.exs lib/glorbo/budget/ledger.ex test/glorbo/budget/ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `ls config/llm_rates.exs config/network_policy.exs` lists both files
    - `grep -q 'claude-code' config/llm_rates.exs && grep -q 'gemini-cli' config/llm_rates.exs && grep -q 'codex' config/llm_rates.exs` succeeds (all three providers)
    - `grep -q 'api.anthropic.com' config/network_policy.exs` succeeds
    - `grep -q 'import_config.*llm_rates' config/config.exs` succeeds
    - `grep -q 'import_config.*network_policy' config/config.exs` succeeds
    - `grep -qE 'def compute_cost_cents' lib/glorbo/budget/ledger.ex` succeeds
    - `grep -qE 'def record!' lib/glorbo/budget/ledger.ex` succeeds
    - `grep -qE 'def month_bucket' lib/glorbo/budget/ledger.ex` succeeds
    - `grep -qE 'on_conflict.*inc' lib/glorbo/budget/ledger.ex` succeeds (atomic increment)
    - `grep -qE 'conflict_target.*agent_slug.*year_month' lib/glorbo/budget/ledger.ex` succeeds
    - `! grep -E 'Decimal\\.|Float\\.round' lib/glorbo/budget/ledger.ex` (cents are integers only)
    - `mix test test/glorbo/budget/ledger_test.exs` reports ≥ 14 tests, 0 failures
    - `mix credo --strict lib/glorbo/budget/ledger.ex` exits 0
    - `mix format --check-formatted` exits 0 for all touched files
  </acceptance_criteria>
  <done>Budget.Ledger exposes record!/1, compute_cost_cents/4, month_bucket/1, fetch/2 with all 14 behaviors green including the 10-concurrent-Task atomic upsert race test. llm_rates + network_policy config loads into Application env at boot. Format + credo clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BudgetTracker GenServer (pre-dispatch check + alert + hard-stop)</name>
  <files>
    lib/glorbo/company/budget_tracker.ex,
    test/glorbo/company/budget_tracker_test.exs
  </files>
  <read_first>
    lib/glorbo/company/budget_tracker.ex (current stub — returns :not_implemented),
    lib/glorbo/budget/ledger.ex (just created in Task 1 — verify the fetch/record signatures match),
    lib/glorbo/company/audit_log.ex (Phase 2 shipped — `append/2` signature + append-only invariant),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md (Plan 03-01 — `budget.usage`, `budget.alert`, `budget.hard_stop` payload schemas),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-32 hard-stop pre-dispatch, D-33 alert marker file),
    test/support/data_case.ex + test/support/tmp_glorbo_home.ex (for filesystem tests)
  </read_first>
  <behavior>
    - Test 1: `BudgetTracker.start_link/1` accepts `:name, :company, :base` and starts under the test supervisor; `Process.alive?/1` returns true.
    - Test 2: `check_budget(name, "alice")` where agent has no prior usage returns `:ok` (used == 0, cap from mocked `budgets_fun`).
    - Test 3: `check_budget(name, "alice")` when `budgets_fun.("alice") == 10_000` (cap $100) and current month's `cost_usd_cents == 7_000` returns `:ok` (70% < 80% alert threshold).
    - Test 4: `check_budget(name, "alice")` at 80% (`cost_usd_cents == 8_000`, cap 10_000) returns `{:alert, 8_000, 10_000}` AND writes `<base>/companies/<co>/alerts/alice-budget.md` with frontmatter `used_usd: 80.00, cap_usd: 100.00, threshold_pct: 80, month: "2026-04", created_at: <iso>`. Writes `budget.alert` audit event.
    - Test 5: The alert file is only written ONCE per month — a second `check_budget` call that again triggers alert returns `{:alert, ...}` but does NOT re-write the file (idempotent by file presence check). No duplicate audit event.
    - Test 6: `check_budget(name, "alice")` at 100% (`cost_usd_cents >= cap_cents`) returns `{:stop, used, cap}` AND emits `budget.hard_stop` audit event with payload `%{agent: "alice", year_month: "2026-04", used_cents: 10_000, cap_cents: 10_000, attempted_task: nil}`.
    - Test 7: `record(name, usage_record)` where usage_record is a valid map calls `Ledger.record!/1` with `cost_usd_cents` computed from `Ledger.compute_cost_cents/4`. Subsequent `Repo.get_by(Budget, ...)` returns the row.
    - Test 8: `record/2` emits `budget.usage` audit event with payload matching AUDIT_EVENTS.md schema (`agent, task_id, prompt_tokens, completion_tokens, cost_usd_cents, model`).
    - Test 9: `record/2` with `prompt_tokens: 0, completion_tokens: 0, provider: "claude-code", model: "claude-opus-4-6"` records a row with `cost_usd_cents: 0` — zero-usage tasks still create/update the monthly row (keeps the "agent invoked at least once this month" signal).
    - Test 10: `reload_config(name)` re-reads per-agent caps from filesystem (the `budgets_fun` is invoked fresh) — test by injecting an agent-files fixture dir, mutating, calling reload, and observing the new cap via `check_budget`.
    - Test 11: Crash recovery — `GenServer.stop(pid, :kill)` then re-start with same name; `check_budget` still returns correct state because all state is derived from Repo + filesystem. Verifies D-45 (BudgetTracker is stateless, rebuilds from SQLite).
    - Test 12: Concurrent `record/2` calls from 20 Tasks (different agents OR same agent) complete without error AND final totals sum correctly — delegates to Ledger's atomic upsert from Task 1.
    - Test 13: `check_budget(name, "unconfigured-agent")` where `budgets_fun` returns `nil` (agent has no `budget_usd_cents_month` in agent.md) returns `:ok` and does NOT hard-stop — absence of cap means "unlimited" per D-32 default. Logged once via telemetry.
  </behavior>
  <action>
    1. Rewrite `lib/glorbo/company/budget_tracker.ex` as a full GenServer:
       - `start_link/1` accepts `:name`, `:company`, `:base` (default `~/.glorbo`), `:budgets_fun` (default: read caps from `<base>/companies/<company>/agents/<agent>/agent.md` via `Glorbo.Filesystem.Frontmatter.read/1` + extract `budget_usd_cents_month:` field), `:alert_threshold_pct` (default 80), `:repo` (default `Glorbo.Repo`), `:audit_fun` (default `&Glorbo.Company.AuditLog.append/2`), `:fs_fun` (map of write/exists?/mkdir_p for alert file writes).
       - State: `%{company, base, budgets_fun, alert_threshold_pct, repo, audit_fun, fs_fun, caps_cache: %{}, alerts_fired: MapSet.new()}`.
       - `check_budget/2`: GenServer.call. In handle_call: look up cap from `caps_cache` (populate on miss via `budgets_fun`). Fetch current-month ledger row via `Ledger.fetch/2`. Compute used vs cap. Return `:ok | {:alert, used, cap} | {:stop, used, cap}`. On `:alert` first time for this month: write alert file, emit audit, add `{agent, year_month}` to `alerts_fired`. On `:stop`: always emit audit (even repeat calls — each denied dispatch deserves an audit).
       - `record/3`: GenServer.cast. In handle_cast: compute `cost_usd_cents` via `Ledger.compute_cost_cents/4`; call `Ledger.record!/1`; emit `budget.usage` audit. Caught exceptions (e.g. Ecto changeset error) logged via `Logger.error` but do NOT crash the GenServer — budget recording is best-effort (undercount preferred to crash-during-dispatch-finish).
       - `reload_config/1`: GenServer.call. Clears `caps_cache`. Returns `:ok`.
       - Alert file shape: YAML frontmatter + one-line body. Use `File.write!/2` (not :append) — one file per agent per month.

    2. Write `test/glorbo/company/budget_tracker_test.exs` using `Glorbo.DataCase` + `Glorbo.TmpGlorboHome` (Phase 1 support modules). Cover behaviors 1–13. Use `start_supervised!/1` for GenServer start + automatic teardown.

    3. Run `mix format`, `mix credo --strict`, `mix compile --warnings-as-errors`, `mix test test/glorbo/company/budget_tracker_test.exs`.

    Why pre-dispatch hard-stop (D-32) not mid-call: Claude Code is a subprocess outside Elixir's control once spawned; killing mid-call loses partial work + corrupts the CLI's session state + complicates cleanup. Pre-dispatch is a clean gate — the worst case is ONE overspending dispatch (the one that pushed us over cap), which the post-hoc audit captures.

    Why alerts_fired in GenServer state (not derived from filesystem): the file existence check is the source of truth ("alert already fired") but the MapSet gives a fast in-memory check to avoid hammering the filesystem on every dispatch. On GenServer restart, state rebuilds by scanning the alerts/ dir — implemented lazily in `handle_info({:rescan, _})` NOT at init (init must be fast per OTP best-practice).

    Why `record/3` is cast (not call): record is fire-and-forget. Backpressure is not a concern (N agents × ~1 record/invocation × ~seconds/invocation = low rate). Using call would serialize dispatch completions; using cast allows dispatch pipelines to return immediately + audit to flow asynchronously. Failure cases (Ecto errors) are Logger-ed; telemetry in Phase 4 surfaces the failure rate.

    Why `budgets_fun` dep-injected not baked: tests must mock without filesystem; production default reads agent.md. Same pattern as Phase 2 Doctor checks (Phase 1 D-19 precedent).
  </action>
  <verify>
    <automated>mix test test/glorbo/company/budget_tracker_test.exs --color --trace 2>&1 | tail -50 && mix credo --strict lib/glorbo/company/budget_tracker.ex && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'def check_budget' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE 'def record' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE 'def reload_config' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE ':alert' lib/glorbo/company/budget_tracker.ex && grep -qE ':stop' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE 'budget\\.hard_stop' lib/glorbo/company/budget_tracker.ex` succeeds (audit action key)
    - `grep -qE 'budget\\.alert' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE 'budget\\.usage' lib/glorbo/company/budget_tracker.ex` succeeds
    - `grep -qE 'alerts_fired' lib/glorbo/company/budget_tracker.ex` succeeds (idempotent alert)
    - `grep -qE 'alerts/.*-budget\\.md' lib/glorbo/company/budget_tracker.ex` succeeds (alert file path)
    - `! grep -E 'GenServer\\.call.*record' lib/glorbo/company/budget_tracker.ex` (record is cast, not call)
    - `mix test test/glorbo/company/budget_tracker_test.exs` reports ≥ 13 tests, 0 failures
    - `mix credo --strict lib/glorbo/company/budget_tracker.ex` exits 0
  </acceptance_criteria>
  <done>BudgetTracker GenServer replaces the Phase-1 stub with check_budget/record/reload_config API; pre-dispatch hard-stop + idempotent alert file + audit emission all green; all 13 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scheduler GenServer (cron heartbeat) + Router GenServer (permission-checked routing + mentions + agent-create block)</name>
  <files>
    lib/glorbo/company/scheduler.ex,
    lib/glorbo/company/router.ex,
    test/glorbo/company/scheduler_test.exs,
    test/glorbo/company/router_test.exs
  </files>
  <read_first>
    lib/glorbo/company/scheduler.ex (stub),
    lib/glorbo/company/router.ex (stub — note it uses GenServer + Keyword.fetch!(:name)),
    lib/glorbo/security/acl_mapper.ex (Plan 03-01 — parse_permission/1 + check_action/2 signatures),
    lib/glorbo/company/audit_log.ex (Phase 2 — append/2),
    lib/glorbo/filesystem/frontmatter.ex (Phase 2 — safe YAML parse; Router uses this on outbox files),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md (message.route, message.reject, permission.denied, agent.wake payload schemas),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-19..D-22 router decisions, D-23..D-24 scheduler decisions),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pitfall 3 wall-clock recompute, Pattern 4 Watcher-driven approval gate),
    hex docs: `Crontab.CronExpression.Parser` (parse/1 + parse!/1), `Crontab.Scheduler.get_next_run_date/2` — verify via MCP Context7 if unclear
  </read_first>
  <behavior>
    Scheduler behaviors:
    - S1: `Scheduler.start_link/1` starts; `register/3` for `{slug: "engineer", cron: "*/30 * * * *", dispatch_fun: fn _ -> send(self(), :dispatched) end}` returns `:ok`.
    - S2: Register with invalid cron `"not a cron"` returns `{:error, :invalid_cron}` + emits `scheduler.invalid_cron` audit event; scheduler stays alive.
    - S3: A dep-injected `send_after_fun` captures the `(pid, message, delay_ms)` triple when `register/3` is called; the captured delay matches `DateTime.diff/2` between the mocked clock and `Crontab.Scheduler.get_next_run_date!/2`.
    - S4: When the mocked timer fires (simulated via manual send), Scheduler calls `dispatch_fun.(:heartbeat)` AND re-registers the agent with a freshly-computed next-run (wall-clock recompute per Pitfall 3).
    - S5: `unregister(pid, "engineer")` cancels the pending timer AND removes the agent from state. A subsequent fired message does NOT invoke the dispatch_fun.
    - S6: `register/3` twice for the same agent_slug cancels the first timer and arms the second (no duplicate dispatches on cron hit).
    - S7: Scheduler crash + restart: state is empty after restart (Scheduler is stateless across crashes per D-45; callers re-register on their own restart). Verified by start-stop-start cycle.

    Router behaviors:
    - R1: `route/2` with a well-formed outbox_msg where sender has `chat:write:general` and `to: "chat:general"` writes the body to `<base>/companies/<co>/channels/general.md` via File.open!/2 [:append, :sync], emits `message.route` audit, returns `:ok`.
    - R2: Same payload but sender lacks `chat:write:general`: returns `{:error, {:permission_denied, "chat:write:general"}}`, moves source file to `<base>/companies/<co>/history/<msg_id>.rejected.md`, emits `message.reject` + `permission.denied` audit events, appends a rejection notice to `<base>/companies/<co>/agents/<sender>/inbox/rejections/<ts>-<msg_id>.md`.
    - R3: `to: "agent:ceo"` with sender permission `agents:message:ceo`: writes to `<base>/companies/<co>/agents/ceo/inbox/from-<sender>/<ts>-<msg_id>.md`, emits `message.route`, returns `:ok`.
    - R4: `to: "agent:ceo"` with sender permission `agents:message:engineer` (wrong target): returns `{:error, {:permission_denied, "agents:message:ceo"}}`.
    - R5: `to: "agent:new-hire"` where `new-hire` does NOT exist as a directory: returns `{:error, {:agent_create_blocked, "new-hire"}}` AND emits `permission.denied` audit with `missing_permission: "agents:create:*"` (the pseudo-permission no agent has). Also emits a new `agents.create_blocked` audit event (register in AUDIT_EVENTS.md as part of this task's work).
    - R6: `to: "chat:general"` with body containing `"Hey @engineer, please review this."`: Router writes the channel message (R1-style), AND writes a synthetic mention file `<base>/companies/<co>/agents/engineer/inbox/mentions/<ts>-general.md` with frontmatter `{channel: general, from: <sender>, source_msg: <msg_id>}`. Emits `message.route` once + an `agent.wake` audit with `trigger: "mention"`.
    - R7: Mentions of non-existent agents (`@ghost` where no `agents/ghost/` exists) are silently skipped — no error, no mention file created, audit notes `mention.skipped` in message.route payload under `mentioned_skipped: ["ghost"]`. (The channel message itself still succeeds.)
    - R8: `to: "broadcast:*"` is REJECTED as unsupported in v0.0.1 (no agent permission grants broadcast) — returns `{:error, {:invalid_message, :broadcast_unsupported}}`.
    - R9: Sender slug derivation — the Router receives `outbox_msg.sender`, but the authoritative sender is derived from the source path (`agents/<slug>/outbox/*`) via a helper that the calling code (Plan 03-05 Watcher subscriber) uses. Router's contract: if `sender` in the message doesn't match the inferred slug from `raw_path`, return `{:error, {:invalid_message, :sender_mismatch}}` + audit `permission.denied`. Prevents spoofing (T-03-12).
    - R10: Agent-create categorical block — even if the sender somehow has `agents:create:*` in permissions (tested by constructing a hostile permission list): Router still rejects because the `to:` target is agents creation. The block is BEFORE permission check (defensive belt-and-braces). Audit event emitted.
    - R11: Concurrent routes (20 Tasks writing to the same channel file) all succeed AND produce 20 append lines in `general.md` (File.open with [:append, :sync] serializes at the OS file-handle level — verified by counting lines post-run).
    - R12: Rejection file `history/<msg_id>.rejected.md` contains the full original body AND frontmatter `{rejection_reason: permission_denied, missing: <...>, rejected_at: <iso>}`.
  </behavior>
  <action>
    1. **Scheduler (lib/glorbo/company/scheduler.ex):**
       - `start_link/1`: standard `use GenServer`. opts: `:name, :company, :clock_fun` (default `&DateTime.utc_now/0`), `:send_after_fun` (default `&Process.send_after/3`), `:audit_fun` (default `&Glorbo.Company.AuditLog.append/2`).
       - State: `%{company, agents: %{slug => %{cron, parsed_expr, timer_ref, dispatch_fun}}, clock_fun, send_after_fun, audit_fun}`.
       - `register/3` (GenServer.call): Parse cron via `Crontab.CronExpression.Parser.parse/1` (non-bang — returns `{:ok, expr}|{:error, reason}`). On `{:error, reason}`: emit `scheduler.invalid_cron` audit + return `{:error, :invalid_cron}`. On `{:ok, expr}`: cancel prior timer (if any), compute next-run via `Crontab.Scheduler.get_next_run_date(expr, state.clock_fun.())` (use the dep-injected clock!), compute delay in ms (`DateTime.diff/2` × 1000), arm timer via `state.send_after_fun.(self(), {:heartbeat, slug}, max(0, delay_ms))`, store ref in state.
       - `unregister/2` (GenServer.call): Cancel timer, remove from state.
       - `handle_info({:heartbeat, slug}, state)`: Look up agent's registration (if still present — may have been unregistered after timer armed). If present: `dispatch_fun.(:heartbeat)`, emit `agent.wake` audit with `trigger: "heartbeat"`. Recompute next-run from CURRENT wall-clock (not from the armed time — Pitfall 3). Re-arm timer. Return `{:noreply, state}`.
       - Important: use `Crontab.Scheduler.get_next_run_date` (non-bang) that accepts a DateTime, not `get_next_run_date!`. Verify exact signature via Context7 MCP (`mcp__context7__resolve-library-id "crontab"` then `get-library-docs`) before implementing.

    2. **Router (lib/glorbo/company/router.ex):**
       - `start_link/1`: as above.
       - State: `%{company, base, audit_fun, fs_fun}` (fs_fun is a dep-injection map of `%{write!: fn path, data -> ... end, mkdir_p!: ..., exists?: ..., open_append!: ..., rename!: ...}` — default uses File module functions).
       - `route/2` (GenServer.call): the heavy lift. Pseudocode:
         ```
         with :ok                        <- validate_message(msg),
              :ok                        <- verify_sender_slug(msg),             # R9
              {:parsed_to, to}           <- parse_to(msg.to),                    # "chat:general"|{:agent, slug}|{:broadcast, _}
              :ok                        <- reject_broadcast(to),                # R8
              :ok                        <- reject_agent_create(to, state),      # R5, R10 — BEFORE permission check
              required                   <- required_permission_for(to),         # {"chat","write","general"} etc.
              :ok                        <- ACLMapper.check_action(msg.sender_permissions, required),
              :ok                        <- perform_routing(to, msg, state),     # channel append OR inbox write
              _                          <- maybe_route_mentions(to, msg, state),  # R6, R7
              _                          <- state.audit_fun.(state.company, %{action: "message.route", ...})
           do
             :ok
           else
             {:error, reason} -> handle_rejection(reason, msg, state)
           end
         ```
         - `reject_agent_create/2`: if `to == {:agent, slug}` AND no `<base>/companies/<co>/agents/<slug>/` directory exists, reject with `agents:create:*` missing-permission (AGT-05).
         - `perform_routing/3`: for channel → `File.open!([:append, :sync])` + `IO.binwrite` the body. For agent → create `inbox/from-<sender>/<ts>-<msg_id>.md` with frontmatter + body.
         - `handle_rejection/3`: move source file to `history/`, write rejection notice to sender's inbox, emit two audit events (message.reject + permission.denied), return the error tuple.
       - `maybe_route_mentions/3`: regex scan `~r/@([a-z][a-z0-9_-]{0,63})/` against body; for each mention target that exists as `agents/<m>/`, write `agents/<m>/inbox/mentions/<ts>-<channel>.md` + audit `agent.wake` with `trigger: "mention"`.
       - Use `Glorbo.Filesystem.Frontmatter` for parsing if needed (though most routing works off the decoded outbox_msg map).

    3. **AUDIT_EVENTS.md extension:** add one new event key at the end: `agents.create_blocked` with actor `sender_agent`, fired-by `Router`, payload `%{from: string, attempted_target: string}`. Git-commit this diff as part of Task 3's commit.

    4. **test/glorbo/company/scheduler_test.exs:** Cover behaviors S1–S7. Use `start_supervised!` + dep-injected `clock_fun` (returns a fixed DateTime) + `send_after_fun` (captures the triple in a test `pid`'s mailbox). Test S4 simulates timer fire by `send(scheduler_pid, {:heartbeat, "engineer"})`.

    5. **test/glorbo/company/router_test.exs:** Cover behaviors R1–R12. Use `Glorbo.TmpGlorboHome` to create a fresh base dir with `companies/<co>/agents/<slug>/inbox,outbox,state,workspace` structure for 3 test agents. Inject a capturing `audit_fun` (`fn co, e -> send(test_pid, {:audit, co, e}) end`) so tests can assert on emitted events.

    6. Run `mix format`, `mix credo --strict lib/glorbo/company/scheduler.ex lib/glorbo/company/router.ex`, `mix compile --warnings-as-errors`, `mix test test/glorbo/company/scheduler_test.exs test/glorbo/company/router_test.exs`.

    Why Scheduler uses `Crontab.Scheduler.get_next_run_date/2` not Quantum: Phase 03-01 selected crontab for its pure-function shape. Quantum would bring its own scheduler + job store + supervisor — unnecessary overhead for ~dozens of agents per company. Pitfall 3 (wall-clock recompute) is a 3-line safeguard in our handler.

    Why Router is a GenServer (not pure): routing is inherently sequential per-company (channel-file append order matters). A GenServer serializes writes naturally. Phase 4 dashboard will benefit from a per-company observation point (live route events → PubSub).

    Why agent-create block is categorical (pre-permission-check): even a hypothetical compromised agent who somehow acquired `agents:create:*` must still be blocked — the design invariant (AGT-05: only Director creates agents) is absolute. Belt + braces: no agent HAS this permission (01-01 ACLMapper whitelists the verb but grants no ACL entry; Plan 03-03's parser rejects agent.md files listing `agents:create`), AND Router enforces the block regardless.

    Why sender slug mismatch rejection (R9): spoofing defence. The outbox file path (`agents/<slug>/outbox/*`) is the authoritative source; `sender:` field in the body is convenience. Mismatch = tampering.

    Why `[:append, :sync]` not `:append` alone: `:sync` triggers `fsync` on every write, preventing channel append races where two concurrent writers land interleaved bytes inside a line. Plan 02-04 Watcher already uses this pattern.

    Why Router's `fs_fun` is an injectable map not a single function: different operations (write, rename, mkdir_p, exists?, open_append) need different real/fake behavior in tests; a map with atom keys is the cleanest way to surface the dep-injection points.
  </action>
  <verify>
    <automated>mix test test/glorbo/company/scheduler_test.exs test/glorbo/company/router_test.exs --color --trace 2>&1 | tail -80 && mix credo --strict lib/glorbo/company/scheduler.ex lib/glorbo/company/router.ex && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    Scheduler:
    - `grep -qE 'def register' lib/glorbo/company/scheduler.ex` succeeds
    - `grep -qE 'def unregister' lib/glorbo/company/scheduler.ex` succeeds
    - `grep -qE 'Crontab\\.CronExpression\\.Parser\\.parse' lib/glorbo/company/scheduler.ex` succeeds (non-bang variant)
    - `grep -qE 'Crontab\\.Scheduler\\.get_next_run_date' lib/glorbo/company/scheduler.ex` succeeds
    - `grep -qE 'scheduler\\.invalid_cron' lib/glorbo/company/scheduler.ex` succeeds
    - `grep -qE 'agent\\.wake' lib/glorbo/company/scheduler.ex` succeeds
    - `grep -qE 'clock_fun' lib/glorbo/company/scheduler.ex` succeeds (dep-injection)

    Router:
    - `grep -qE 'def route' lib/glorbo/company/router.ex` succeeds
    - `grep -qE 'ACLMapper\\.check_action' lib/glorbo/company/router.ex` succeeds (SEC-01 hook)
    - `grep -qE 'message\\.route' lib/glorbo/company/router.ex` succeeds
    - `grep -qE 'message\\.reject' lib/glorbo/company/router.ex` succeeds
    - `grep -qE 'permission\\.denied' lib/glorbo/company/router.ex` succeeds
    - `grep -qE 'agents\\.create_blocked' lib/glorbo/company/router.ex` succeeds (AGT-05)
    - `grep -qE ':append.*:sync|\\[.*append.*sync\\]' lib/glorbo/company/router.ex` succeeds (channel append invariant)
    - `grep -qE 'history/.*rejected' lib/glorbo/company/router.ex` succeeds (rejection file)
    - `grep -qE 'inbox/mentions' lib/glorbo/company/router.ex` succeeds (mention wake)
    - `grep -qE '@([a-z]' lib/glorbo/company/router.ex` succeeds (mention regex)
    - `! grep -E 'write_inbox' lib/glorbo/company/router.ex` (no public write_inbox; CLAUDE.md one-way invariant)

    AUDIT_EVENTS.md:
    - `grep -q 'agents\\.create_blocked' .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md` succeeds

    Tests:
    - `mix test test/glorbo/company/scheduler_test.exs` reports ≥ 7 tests, 0 failures
    - `mix test test/glorbo/company/router_test.exs` reports ≥ 12 tests, 0 failures
    - `mix credo --strict lib/glorbo/company/scheduler.ex lib/glorbo/company/router.ex` exits 0
    - `mix format --check-formatted` exits 0 for all touched files
  </acceptance_criteria>
  <done>Scheduler + Router GenServers fully implemented with dep-injected clock/audit/fs. Scheduler drives cron heartbeats with wall-clock recompute. Router performs ACLMapper.check_action/2 + channel-append + agent-inbox-write + mention-wake + agent-create-block + rejection-file + audit emission. AUDIT_EVENTS.md extended with one new key. All 19+ tests pass; format + credo clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM outbox file (filesystem) → Router.route/2 | Untrusted input: malformed `to:`, forged `from:`, billion-laughs YAML, injected `@mentions` pointing at non-existent agents |
| agent.md `heartbeat:` cron string → Crontab.CronExpression.Parser | Untrusted input: invalid cron syntax must not crash the Scheduler for other agents |
| agent.md `budget_usd_cents_month:` field → BudgetTracker.budgets_fun | Untrusted input: negative values, non-integers, missing field |
| Concurrent dispatch finalization → Budget.Ledger.record! | Race-prone: 20 concurrent writers to one (agent, year_month) row must atomically sum |
| Future container-runtime reactivation → Ledger/BudgetTracker | Dormant v0.0.1 paths (worker usage.json) must not crash when/if revived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | lib/glorbo/company/router.ex | mitigate | ACLMapper.check_action/2 call BEFORE any filesystem write; categorical agents:create block BEFORE permission check (R10); sender slug verified against outbox path (R9) |
| T-03-08 | Tampering | lib/glorbo/budget/ledger.ex | mitigate | Atomic `on_conflict: [inc: [...]]` upsert (Pitfall 2); unique index `(agent_slug, year_month)` from Plan 03-01; 20-Task concurrent-write test proves race-free behavior |
| T-03-09 | Denial of Service | lib/glorbo/company/router.ex | mitigate | Router @max_pending cap mirrors Watcher WR-07 pattern; drop-with-warning on overflow; GenServer mailbox serializes per-company writes naturally |
| T-03-10 | Repudiation | lib/glorbo/company/router.ex | mitigate | Every route/reject/mention/agent-create-block emits audit events per AUDIT_EVENTS.md; append-only invariant enforced upstream (Phase 2 AuditLog); rejection files land at history/<id>.rejected.md for post-hoc disk forensics |
| T-03-11 | Denial of Service | lib/glorbo/company/scheduler.ex | mitigate | Crontab.parse/1 (non-bang) wrapped in try/rescue; malformed cron → `{:error, :invalid_cron}` + `scheduler.invalid_cron` audit; offending agent skipped, others continue. Wall-clock recompute (Pitfall 3) prevents Process.send_after drift |
| T-03-12 | Spoofing | lib/glorbo/company/router.ex | mitigate | Sender slug derived from outbox file path (directory Elixir owns) NOT from body's `from:` field. Mismatch between sender-in-body and sender-in-path rejects with `permission.denied` + `invalid_message:sender_mismatch` |
| T-03-13 | Information Disclosure | lib/glorbo/company/budget_tracker.ex | mitigate | `agent.md`'s `budget_usd_cents_month:` parsed as integer only (rejects strings, maps); alert file contains only used/cap/pct/month (no API keys, no task content); hard-stop audit includes attempted_task reference (string path, no body) |
| T-03-14 | Repudiation | lib/glorbo/budget/ledger.ex | accept | `compute_cost_cents/4` returns 0 on missing rate + Logger.warning; under-counting accepted per D-30 ("Glorbo owns the mapping"). Alternative (raise) would crash dispatch-completion path; user explicitly locked accept in Plan 03-01 |
</threat_model>

<verification>
- All three GenServer modules no longer return `:not_implemented` for their primary API
- `mix test test/glorbo/company/ test/glorbo/budget/` — ≥ 50 unit tests, 0 failures
- `mix credo --strict lib/glorbo/company/router.ex lib/glorbo/company/scheduler.ex lib/glorbo/company/budget_tracker.ex lib/glorbo/budget/ledger.ex` — 0 issues
- `mix compile --warnings-as-errors` — clean
- `mix format --check-formatted` — clean
- `config/llm_rates.exs` + `config/network_policy.exs` loaded into Application env (verifiable via `Application.get_env(:glorbo, :llm_rates) |> Map.has_key?("claude-code")` in `iex -S mix`)
- No file overlap with Plan 03-03 or Plan 03-04 (Wave 1 parallel-safety) — this plan ONLY touches `lib/glorbo/{company,budget}/*` + `config/*` + tests
- `.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md` extended with `agents.create_blocked` event
</verification>

<success_criteria>
- AGT-02: Scheduler drives cron heartbeat wake; Router writes synthetic mention-wake messages to target agents' inboxes (two of four wake triggers; inbox trigger is Plan 03-05's Watcher wiring; director-request trigger is Plan 03-03's Agent.Server).
- AGT-03: Router mediates every outbox→destination transfer; no public `write_inbox` API; channels are `[:append, :sync]`; rejection flow is auditable + filesystem-observable.
- AGT-05: Router categorically rejects any `to:` that would write to a non-existent agent's dir; `agents.create_blocked` audit event emitted.
- SEC-01: `ACLMapper.check_action/2` called at the application layer before every routing decision; deny → `permission.denied` + `message.reject` audit.
- SEC-05: `BudgetTracker.check_budget/2` returns `:ok | {:alert, ...} | {:stop, ...}`; alert file at `alerts/<agent>-budget.md` written once per month; hard-stop refuses further dispatch; `Budget.Ledger.record!/1` atomically upserts monthly totals.
- `mix test` green against full Phase 2 + Plan 03-01 + Plan 03-02 suite.
- No regressions in Phase 2 tests (verified by running `mix test` root).
- All five locked_decisions in this plan's frontmatter are reflected in code (cents-only, agents:create block, wall-clock recompute, BudgetTracker sole writer, pre-dispatch-only hard-stop).
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-routing-kernel-permissions-budgets/03-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- requirements-completed: [AGT-02 (partial — cron+mention; Watcher wiring in 03-05), AGT-03 (partial — Router mediation + one-way flow; integration test in 03-05), AGT-05 (full — Router block + audit event), SEC-01 (full — app-layer enforce + integration hooks), SEC-05 (full — BudgetTracker + Ledger + alert + hard-stop; kernel-observed e2e in 03-05)]
- key-decisions: confirm cents-integer math, atomic upsert pattern, pre-dispatch-only hard-stop, sender-slug-from-path, categorical agents-create block
- patterns-established: dep-injected fs_fun/audit_fun/clock_fun/send_after_fun for testability; `on_conflict: [inc: [...]]` atomic Ecto upsert; `[:append, :sync]` for channel append races
- back-edits: AUDIT_EVENTS.md extended with `agents.create_blocked` event
</output>
</content>
</invoke>