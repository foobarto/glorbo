---
phase: 03
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - priv/repo/migrations/20260416120001_create_budgets.exs
  - priv/repo/migrations/20260416120002_create_tasks_approval_state.exs
  - priv/repo/migrations/20260416120003_add_permissions_hash_to_agents.exs
  - lib/glorbo/budget.ex
  - lib/glorbo/tasks_approval_state.ex
  - lib/glorbo/security/acl_mapper.ex
  - lib/glorbo/runtime/uid_allocator.ex
  - containers/glorbo-runtime/worker/routes.py
  - containers/glorbo-runtime/worker/dispatch.py
  - containers/glorbo-runtime/worker/context.py
  - containers/glorbo-runtime/worker/usage.py
  - test/glorbo/security/acl_mapper_test.exs
  - test/glorbo/runtime/uid_allocator_test.exs
  - test/glorbo/budget_schema_test.exs
  - test/glorbo/tasks_approval_state_test.exs
  - test/test_helper.exs
  - .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md
autonomous: true
gap_closure: false
requirements: [SEC-01, SEC-05, AGT-04]
locked_decisions:
  - "OQ1 (D-02 UID base): UID base is read from /etc/subuid for the Director user; per-company offset = subuid_base + 100*company_ordinal. The literal `100000` in CONTEXT.md D-02 is reinterpreted as a relative offset into the host's subordinate-UID range, NOT an absolute UID. UidAllocator returns an error if /etc/subuid lacks an entry for the current user (Doctor will surface this in a follow-up phase)."
  - "OQ2 (--cap-add NET_ADMIN): NARROWLY scoped — added by Network.Policy ONLY for `api-only` containers (Plan 05). Negative tests in invocation_test.exs assert no cap-add for `none` and `open`. This is a deliberate, documented deviation from RT-04's no-cap-add baseline; the trade-off is that without netavark per-container egress (Pitfall 1), in-container nftables is the only sustainable enforcement path."
  - "OQ3 (Proxy vs nftables): Phase 3 ships nftables + pre-resolved CIDR allow-list for Anthropic/OpenAI/Google/HuggingFace endpoints (Plan 05). Userspace tinyproxy on UDS is deferred to v1.1 and tracked in DEFERRED.md."
  - "Cents over fractional USD for budget arithmetic (D-26 confirmed; integer math, no float drift)."
  - "Skills injection at dispatch time as `skills_resolved:` body field (D-34) — additive, not breaking, to Phase 2's /run schema (Phase 2 D-36 stability invariant preserved by adding new field, not renaming existing ones)."
key-links:
  - from: lib/glorbo/security/acl_mapper.ex
    to: lib/glorbo/company/router.ex
    via: "Plan 02 imports for sender-permission check"
  - from: lib/glorbo/budget.ex
    to: lib/glorbo/company/budget_tracker.ex
    via: "Plan 02 inserts/upserts via Ecto"
  - from: lib/glorbo/runtime/uid_allocator.ex
    to: lib/glorbo/security/user_provisioner.ex
    via: "Plan 05 generates /etc/passwd fragment from allocated UIDs"
  - from: containers/glorbo-runtime/worker/routes.py
    to: containers/glorbo-runtime/worker/usage.py
    via: "/run handler writes outbox/usage/<task_id>.json after result"
  - from: containers/glorbo-runtime/worker/usage.py
    to: lib/glorbo/company/budget_tracker.ex
    via: "Watcher path-prefix routes outbox/usage/* to BudgetTracker (Plan 02 wires)"

must_haves:
  truths:
    - "Running `mix deps.get` resolves crontab 1.2 from hex"
    - "Running `mix ecto.migrate` creates `budgets` and `tasks_approval_state` tables and adds `permissions_hash` column to `agents`"
    - "ACLMapper.check_action/3 returns :ok for an agent with `projects:write:website-redesign` writing to `projects/website-redesign/foo.md`"
    - "ACLMapper.check_action/3 returns {:error, {:permission_denied, missing}} for an agent without that permission writing to `projects/other/foo.md`"
    - "ACLMapper.acl_entries/2 returns deterministic [{username, mode, path}] tuples mapping every locked permission verb (chat:read, chat:write, projects:read, projects:write, agents:message, agents:create, tasks:update) to its ACL command shape"
    - "UidAllocator reads /etc/subuid for current user, computes company UID base as `subuid_base + 100*ordinal`, and persists allocations to ~/.glorbo/runtime/.companies-uid.json with a tombstone field (D-04 soft-retire)"
    - "FastAPI worker accepts new `skills_resolved` body field (list of markdown strings); when non-empty, prepends a system-prompt segment containing the skills text"
    - "FastAPI worker writes `outbox/usage/<request_id>.json` AFTER the result file with schema {task_id, timestamp, provider, model, prompt_tokens, completion_tokens, total_tokens, cost_usd, cost_usd_cents}; Ollama returns cost_usd: 0.0 (litellm cost DB miss is coerced to 0.0)"
    - "AUDIT_EVENTS.md exists in the phase dir documenting all Phase-3 audit `action:` keys (message.route, message.reject, permission.denied, skill.missing, provider.unknown, budget.usage, budget.alert, budget.hard_stop, approval.requested, approval.granted, approval.denied, agent.wake, agent.dispatch, agent.complete, acl.reconcile, network.policy_applied)"
  artifacts:
    - path: "mix.exs"
      provides: "crontab dep declared"
      contains: ":crontab"
    - path: "priv/repo/migrations/20260416120001_create_budgets.exs"
      provides: "budgets table — composite unique on (agent_slug, year_month)"
      contains: "create unique_index"
    - path: "priv/repo/migrations/20260416120002_create_tasks_approval_state.exs"
      provides: "tasks_approval_state table"
      contains: "create table"
    - path: "priv/repo/migrations/20260416120003_add_permissions_hash_to_agents.exs"
      provides: "agents.permissions_hash column"
      contains: ":permissions_hash"
    - path: "lib/glorbo/budget.ex"
      provides: "Budget Ecto schema (cents)"
      contains: "schema \"budgets\""
    - path: "lib/glorbo/tasks_approval_state.ex"
      provides: "TasksApprovalState Ecto schema"
      contains: "schema \"tasks_approval_state\""
    - path: "lib/glorbo/security/acl_mapper.ex"
      provides: "Pure permission → ACL-tuple mapper"
      exports: ["check_action/3", "acl_entries/2", "parse_permission/1"]
    - path: "lib/glorbo/runtime/uid_allocator.ex"
      provides: "Per-company UID block allocator with subuid base detection"
      exports: ["allocate/3", "current_allocations/1", "subuid_base/1"]
    - path: "containers/glorbo-runtime/worker/usage.py"
      provides: "Usage-report writer (litellm.completion_cost)"
      exports: ["write_usage_report"]
    - path: "test/glorbo/security/acl_mapper_test.exs"
      provides: "Pure-unit coverage of all permission verbs + denial reasons"
      contains: "test \"check_action"
    - path: "test/glorbo/runtime/uid_allocator_test.exs"
      provides: "subuid base detection + allocation persistence + tombstone"
      contains: "test \"allocate"
    - path: ".planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md"
      provides: "Phase-3 audit event registry (canonical action: keys + payload schemas)"
      contains: "message.route"
  key_links:
    - from: "lib/glorbo/security/acl_mapper.ex"
      to: "Plan 02 lib/glorbo/company/router.ex"
      via: "import ACLMapper.check_action/3 in route handler"
      pattern: "ACLMapper\\.check_action"
    - from: "lib/glorbo/budget.ex"
      to: "Plan 02 lib/glorbo/budget/ledger.ex"
      via: "Repo.insert!(%Budget{...}, on_conflict: ..., conflict_target: [:agent_slug, :year_month])"
      pattern: "%Glorbo\\.Budget\\{"
    - from: "containers/glorbo-runtime/worker/routes.py"
      to: "containers/glorbo-runtime/worker/usage.py"
      via: "_call write_usage_report in /run finally block (after result)"
      pattern: "write_usage_report"

threat_model:
  trust_boundaries:
    - boundary: "Director (host) → Elixir Repo (SQLite)"
      description: "Migrations run host-side; Director-trusted; SQLite file owned by Director"
    - boundary: "agent.md (filesystem) → ACLMapper.parse_permission/1"
      description: "User-provided permission strings parsed; malformed strings must reject without raising"
    - boundary: "FastAPI worker (in container) → outbox/usage/*.json (mounted volume)"
      description: "Untrusted LLM-derived response data is serialized; cost field comes from litellm (trusted lib) but tokens come from upstream provider response"
  threats:
    - id: "T-03-01"
      category: "Tampering"
      component: "lib/glorbo/security/acl_mapper.ex"
      disposition: "mitigate"
      mitigation: "parse_permission/1 uses String.split/3 with limit 3 + pattern-match on a fixed verb whitelist [\"projects\", \"chat\", \"agents\", \"tasks\"]; unknown verbs return {:error, :unknown_resource}; no eval/atom-creation from input"
    - id: "T-03-02"
      category: "Information Disclosure"
      component: "lib/glorbo/runtime/uid_allocator.ex"
      disposition: "mitigate"
      mitigation: "/etc/subuid is world-readable; reading is not a privilege escalation. .companies-uid.json is written under ~/.glorbo/runtime/ which is Director-owned (chmod 0700 by Phase 2). No secrets stored in the file."
    - id: "T-03-03"
      category: "Information Disclosure"
      component: "containers/glorbo-runtime/worker/usage.py"
      disposition: "mitigate"
      mitigation: "Usage report contains tokens + cost only; NO message content, NO api_key. Schema is keyed allow-list (not free-form serialization of response object)."
    - id: "T-03-04"
      category: "Integrity"
      component: "priv/repo/migrations/*"
      disposition: "mitigate"
      mitigation: "All Phase-3 schemas are additive (new tables + one new column). No drops, no renames. Forward-only migration safety preserved."
    - id: "T-03-05"
      category: "Denial of Service"
      component: "containers/glorbo-runtime/worker/usage.py"
      disposition: "accept"
      mitigation: "Disk-fill via runaway usage reports is bounded by per-agent budget hard-stop (SEC-05, Plan 02). No additional rate limit in v1."
---

<objective>
Lay the Wave-0 foundations every other Phase-3 plan depends on. This plan introduces ZERO new runtime processes — it adds Ecto schemas + migrations, a pure ACL-mapper module, the UID-allocator that resolves Open Question #1 against the host's `/etc/subuid`, the `crontab` dep used by Plan 02's Scheduler, the FastAPI worker extensions for usage reporting + skills injection (D-24/D-34) consumed by Plan 02 and Plan 03, and a canonical audit-event registry document so Plans 02-05 can reference stable action keys.

This is the only plan that can run alone in Wave 0 — Plans 02, 03, 04 each depend on at least one artifact created here (ACLMapper for 02 + 04, Budget schema for 02, UID allocator for 05, worker usage.py for 02, crontab dep for 02). Plan 05 in Wave 2 depends on the UID allocator and the audit event registry.

Purpose: De-risk the three open questions the researcher flagged (UID base, NET_ADMIN scope, nftables vs proxy) by locking decisions in this PLAN's `locked_decisions` frontmatter; provide pure (testable-without-IO) building blocks; extend the worker's `/run` schema additively without breaking Phase 2.

Output: 3 migrations + 2 Ecto schemas + 2 Elixir pure modules (ACLMapper + UidAllocator) + 1 Python module (usage.py) + worker route additions + 1 docs file (AUDIT_EVENTS.md) + 4 unit-test files. No GenServer changes. No supervisor changes. No container invocation changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-VALIDATION.md
@.planning/phases/02-filesystem-foundation-container-runtime-local-llm/02-04-SUMMARY.md
@CLAUDE.md
@DESIGN.md
@mix.exs
@lib/glorbo/agent.ex
@lib/glorbo/audit_event.ex
@lib/glorbo/filesystem/reindex_state.ex
@containers/glorbo-runtime/worker/main.py
@containers/glorbo-runtime/worker/routes.py
@containers/glorbo-runtime/worker/dispatch.py
@containers/glorbo-runtime/worker/context.py

<interfaces>
<!-- Stable contracts later plans will consume. Plans 02-05 read these directly. -->

ACLMapper public API (Plan 02 + Plan 05 consume):
```elixir
# lib/glorbo/security/acl_mapper.ex
@type permission :: {resource :: String.t(), action :: String.t(), scope :: String.t()}
@type acl_mode :: :rwx | :rx | :r | :"---"
@type acl_entry :: {username :: String.t(), mode :: acl_mode(), path :: String.t()}
@type action :: {resource :: String.t(), action :: String.t(), target_scope :: String.t() | nil}

@spec parse_permission(String.t()) ::
        {:ok, permission()} | {:error, :unknown_resource | :malformed}
@spec check_action([permission()], action()) ::
        :ok | {:error, {:permission_denied, missing :: String.t()}}
@spec acl_entries(username :: String.t(), [permission()]) :: [acl_entry()]
```

Budget schema (Plan 02 consumes):
```elixir
# lib/glorbo/budget.ex
schema "budgets" do
  field :agent_slug, :string
  field :year_month, :string         # "2026-04" — UTC, calendar-month bucket
  field :prompt_tokens, :integer, default: 0
  field :completion_tokens, :integer, default: 0
  field :cost_usd_cents, :integer, default: 0
  timestamps()
end
# unique_index on [:agent_slug, :year_month]
```

TasksApprovalState schema (Plan 03 consumes):
```elixir
# lib/glorbo/tasks_approval_state.ex
schema "tasks_approval_state" do
  field :task_path, :string             # "companies/acme/projects/redesign/tasks/t-01.md"
  field :agent_slug, :string
  field :status, :string                # "awaiting" | "approved" | "denied"
  field :requested_at, :utc_datetime
  field :resolved_at, :utc_datetime     # nil while awaiting
  field :reason, :string                # nil unless denied
  timestamps()
end
# unique_index on [:task_path]
```

UidAllocator public API (Plan 05 consumes):
```elixir
# lib/glorbo/runtime/uid_allocator.ex
@type allocation :: %{
        company: String.t(),
        ordinal: non_neg_integer(),
        uid_base: pos_integer(),         # subuid_base + 100*ordinal
        agents: %{String.t() => pos_integer()},   # agent_slug → uid
        tombstoned: [String.t()]                  # soft-retired agent slugs
      }
@spec subuid_base(opts :: keyword()) :: {:ok, pos_integer()} | {:error, :no_subuid_entry}
@spec allocate(company :: String.t(), agents :: [String.t()], opts :: keyword()) ::
        {:ok, allocation()} | {:error, term()}
@spec current_allocations(opts :: keyword()) :: %{String.t() => allocation()}
# opts: subuid_path: "/etc/subuid", sidecar_path: "~/.glorbo/runtime/.companies-uid.json",
#       user: System.get_env("USER")
```

Worker /run additions (Plan 02 + Plan 03 consume):
```python
# containers/glorbo-runtime/worker/routes.py — RunRequest gains:
class RunRequest(BaseModel):
    ...existing...
    skills_resolved: List[str] = []   # NEW (D-34) — full markdown bodies
# routes.py /run handler: after result is produced, call:
#   write_usage_report(req.task_path, req.request_id, req.provider, req.model, response, outbox_root)
```

Worker usage report schema (Plan 02 consumes via watcher → BudgetTracker):
```python
# containers/glorbo-runtime/worker/usage.py — write_usage_report writes JSON:
{
  "task_id": str,
  "request_id": str,
  "timestamp": "2026-04-16T12:34:56.789Z",
  "provider": str,
  "model": str,
  "prompt_tokens": int,
  "completion_tokens": int,
  "total_tokens": int,
  "cost_usd": float,            # 0.0 for Ollama (litellm cost DB miss)
  "cost_usd_cents": int         # round(cost_usd * 100); integer math downstream
}
```

Audit event registry (all plans consume):
See `.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations + Ecto schemas + crontab dep</name>
  <files>
    mix.exs,
    mix.lock,
    priv/repo/migrations/20260416120001_create_budgets.exs,
    priv/repo/migrations/20260416120002_create_tasks_approval_state.exs,
    priv/repo/migrations/20260416120003_add_permissions_hash_to_agents.exs,
    lib/glorbo/budget.ex,
    lib/glorbo/tasks_approval_state.ex,
    test/glorbo/budget_schema_test.exs,
    test/glorbo/tasks_approval_state_test.exs
  </files>
  <read_first>
    mix.exs (verify deps shape),
    lib/glorbo/agent.ex (existing schema — `permissions_hash` column added to it),
    lib/glorbo/audit_event.ex (existing schema convention),
    lib/glorbo/filesystem/reindex_state.ex (Ecto schema convention reference),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pitfall 4: SQLite upsert pattern; Code Examples §"Ecto upsert with atomic increment via query")
  </read_first>
  <behavior>
    - Test 1: `mix deps.get` exits 0 after adding `{:crontab, "~> 1.2"}` to deps (verified via Bash).
    - Test 2: `mix ecto.migrate` exits 0 against a fresh test repo and creates all three new schema artifacts.
    - Test 3: `Glorbo.Budget` changeset rejects negative `cost_usd_cents`, requires `agent_slug` and `year_month`.
    - Test 4: Inserting two `Budget` rows with the same `{agent_slug, year_month}` raises `Ecto.ConstraintError` (unique_index enforced).
    - Test 5: `Glorbo.TasksApprovalState` changeset accepts statuses in `["awaiting", "approved", "denied"]` and rejects others.
    - Test 6: Inserting two `TasksApprovalState` rows with the same `task_path` raises constraint error.
    - Test 7: After running migrations, `Glorbo.Repo.aggregate(from(a in "agents", select: count(a.permissions_hash)), :count)` succeeds (column exists, type :string nullable).
  </behavior>
  <action>
    1. Add `{:crontab, "~> 1.2"}` to `deps()` in mix.exs (alphabetical position; place after `:bandit`). Run `mix deps.get` and verify `crontab` appears in mix.lock at version 1.2.x.
    2. Create three migrations using `mix ecto.gen.migration` semantics, but write the .exs files DIRECTLY (do not depend on the generator):

       - **20260416120001_create_budgets.exs**: `create table(:budgets) do add :agent_slug, :string, null: false; add :year_month, :string, null: false; add :prompt_tokens, :integer, default: 0, null: false; add :completion_tokens, :integer, default: 0, null: false; add :cost_usd_cents, :integer, default: 0, null: false; timestamps() end; create unique_index(:budgets, [:agent_slug, :year_month])`
       - **20260416120002_create_tasks_approval_state.exs**: `create table(:tasks_approval_state) do add :task_path, :string, null: false; add :agent_slug, :string, null: false; add :status, :string, null: false; add :requested_at, :utc_datetime, null: false; add :resolved_at, :utc_datetime; add :reason, :string; timestamps() end; create unique_index(:tasks_approval_state, [:task_path])`
       - **20260416120003_add_permissions_hash_to_agents.exs**: `alter table(:agents) do add :permissions_hash, :string end` (nullable — back-compat with existing rows from Phase 2; populated by Plan 05 ACLReconciler, used to skip reconcile when hash unchanged).

    3. Create `lib/glorbo/budget.ex` per the schema in `<interfaces>` above. Include a `changeset/2` that requires `[:agent_slug, :year_month]`, validates `prompt_tokens >= 0`, `completion_tokens >= 0`, `cost_usd_cents >= 0`, and `unique_constraint(:agent_slug, name: :budgets_agent_slug_year_month_index)`. Module follows the Phase-1/2 schema convention (matches `Glorbo.AuditEvent` shape).

    4. Create `lib/glorbo/tasks_approval_state.ex` per the schema in `<interfaces>`. `changeset/2` requires `[:task_path, :agent_slug, :status, :requested_at]`, `validate_inclusion(:status, ["awaiting", "approved", "denied"])`, `unique_constraint(:task_path)`.

    5. Write `test/glorbo/budget_schema_test.exs` and `test/glorbo/tasks_approval_state_test.exs` using `Glorbo.DataCase` (Phase 1 set up the sandbox). Cover behaviors 3–6 above.

    6. Run `mix format`, `mix credo --strict` (must report 0 issues — Phase 1/2 baseline), `mix compile --warnings-as-errors`.

    Why crontab not Quantum: Quantum brings its own supervisor + job store; we have ≤dozens of agents per company and one cron expression each — Crontab is 10× lighter (RESEARCH §Standard Stack — Alternatives Considered).
    Why three migrations not one: each migration is a single, semantically-coherent change; rollbacks are scoped; Phase 2 set this convention (4 separate migrations for companies/agents/audit_events/reindex_state).
    Why nullable permissions_hash: agents created by Phase 2's example-company scaffold predate Phase 3 — a NOT NULL would break those rows. Plan 05's ACLReconciler computes and writes the hash on first reconcile.
  </action>
  <verify>
    <automated>mix deps.get && mix ecto.create --quiet 2>/dev/null; mix ecto.migrate && mix test test/glorbo/budget_schema_test.exs test/glorbo/tasks_approval_state_test.exs --color && mix credo --strict lib/glorbo/budget.ex lib/glorbo/tasks_approval_state.ex && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q ':crontab' mix.exs` succeeds
    - `grep -q '"crontab"' mix.lock` succeeds (version 1.2.x present)
    - `ls priv/repo/migrations/20260416120001_create_budgets.exs priv/repo/migrations/20260416120002_create_tasks_approval_state.exs priv/repo/migrations/20260416120003_add_permissions_hash_to_agents.exs` lists all three files
    - `grep -q 'create unique_index' priv/repo/migrations/20260416120001_create_budgets.exs` succeeds
    - `grep -q 'add :permissions_hash' priv/repo/migrations/20260416120003_add_permissions_hash_to_agents.exs` succeeds
    - `grep -q 'schema "budgets"' lib/glorbo/budget.ex` succeeds
    - `grep -q 'schema "tasks_approval_state"' lib/glorbo/tasks_approval_state.ex` succeeds
    - `mix test test/glorbo/budget_schema_test.exs test/glorbo/tasks_approval_state_test.exs` exits 0 with all 7+ tests green
    - `mix credo --strict` exits 0 (no new issues vs Phase 2 baseline)
  </acceptance_criteria>
  <done>3 migrations applied successfully on a fresh test repo, 2 Ecto schemas with changesets exist, crontab dep resolved at 1.2.x, all schema tests pass, format + credo clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure ACLMapper + UidAllocator + AUDIT_EVENTS.md registry</name>
  <files>
    lib/glorbo/security/acl_mapper.ex,
    lib/glorbo/runtime/uid_allocator.ex,
    test/glorbo/security/acl_mapper_test.exs,
    test/glorbo/runtime/uid_allocator_test.exs,
    .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md
  </files>
  <read_first>
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-01 username scheme, D-02 UID block, D-04 tombstone, D-06 ACL mapping table, D-07 baseline, D-08 channel ACLs, D-41 agents:create restriction),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Code Examples §"setfacl inside container", Architecture Pattern 4 §"Subordinate-UID requirement", Audit event naming table),
    DESIGN.md §7.1 (resource:action:scope), DESIGN.md §7.2 (two-layer enforcement),
    /etc/subuid (verify host has `foobarto:524288:65536` entry — used by tests)
  </read_first>
  <behavior>
    ACLMapper:
    - Test 1: `parse_permission("projects:write:website-redesign")` returns `{:ok, {"projects", "write", "website-redesign"}}`.
    - Test 2: `parse_permission("garbage")` returns `{:error, :malformed}`.
    - Test 3: `parse_permission("unknown:write:foo")` returns `{:error, :unknown_resource}` (verb whitelist enforced).
    - Test 4: `parse_permission("agents:create:*")` returns `{:ok, {"agents", "create", "*"}}` (verb is in whitelist; the policy that no agent is GRANTED this is enforced by Plan 04, not here).
    - Test 5: `check_action([{"projects", "write", "website-redesign"}], {"projects", "write", "website-redesign"})` returns `:ok`.
    - Test 6: `check_action([{"projects", "write", "website-redesign"}], {"projects", "write", "marketing"})` returns `{:error, {:permission_denied, "projects:write:marketing"}}`.
    - Test 7: `check_action([{"projects", "write", "*"}], {"projects", "write", "anything"})` returns `:ok` (wildcard scope).
    - Test 8: `check_action([{"chat", "read", "*"}], {"chat", "write", "general"})` returns `{:error, {:permission_denied, "chat:write:general"}}` (action mismatch).
    - Test 9: `acl_entries("glorbo-acme-engineer", [{"projects", "write", "website-redesign"}, {"chat", "read", "*"}])` returns a list including at minimum `{"glorbo-acme-engineer", :rwx, "agents/engineer/outbox"}`, `{"glorbo-acme-engineer", :rwx, "agents/engineer/workspace"}`, `{"glorbo-acme-engineer", :rwx, "agents/engineer/state"}`, `{"glorbo-acme-engineer", :r, "agents/engineer/inbox"}`, `{"glorbo-acme-engineer", :rwx, "projects/website-redesign"}`, and `{"glorbo-acme-engineer", :r, "channels"}`. (D-07 baseline + D-08 channel-read derivation. Channels never get :rwx — Elixir is sole writer.)
    - Test 10: `acl_entries("glorbo-acme-x", [])` returns the baseline four entries (own outbox/workspace/state :rwx + own inbox :r) AND no others — fail-closed default (D-07).

    UidAllocator:
    - Test 11: `subuid_base(subuid_path: tmpfile_with("foobarto:524288:65536\n"), user: "foobarto")` returns `{:ok, 524288}`.
    - Test 12: `subuid_base(subuid_path: tmpfile_with("other:1000:1000\n"), user: "foobarto")` returns `{:error, :no_subuid_entry}`.
    - Test 13: `allocate("acme", ["ceo", "engineer"], opts)` on an empty sidecar returns `{:ok, %{company: "acme", ordinal: 0, uid_base: 524288, agents: %{"ceo" => 524288, "engineer" => 524289}, tombstoned: []}}`.
    - Test 14: A second `allocate("biz", ["founder"], opts)` returns ordinal 1 with uid_base `524388` (524288 + 100*1).
    - Test 15: After tombstoning "engineer" (re-allocate "acme" with `["ceo"]` only), the sidecar shows `tombstoned: ["engineer"]` and `agents: %{"ceo" => 524288}` — the engineer UID 524289 is NOT recycled (D-04).
    - Test 16: Sidecar JSON file at the `sidecar_path` is created with mode 0600 (Director-private; tested via File.stat!).

    AUDIT_EVENTS.md:
    - Test 17 (file existence + grep): `grep -q '^| `message.route`' AUDIT_EVENTS.md` and similar for all 16 event keys from RESEARCH §"Audit event naming".
  </behavior>
  <action>
    1. Create `lib/glorbo/security/acl_mapper.ex` as a pure module (no GenServer, no IO):

       - `@whitelisted_resources ~w(projects chat agents tasks)`
       - `parse_permission/1`: `String.split(string, ":", parts: 3) |> case ...` — three parts required, resource must be in whitelist, action must be a non-empty downcase string, scope is anything (including `*`).
       - `check_action(permissions, {resource, action, target_scope})`: iterates permissions, returns `:ok` on first matching tuple where resource matches, action matches, AND (permission scope == `*` OR permission scope == target_scope). On no match: `{:error, {:permission_denied, "#{resource}:#{action}:#{target_scope}"}}`.
       - `acl_entries(username, permissions)`: produces the BASELINE entries (own outbox `:rwx`, workspace `:rwx`, state `:rwx`, inbox `:r`) keyed off the agent_slug derivable from the username (`"glorbo-#{co}-#{ag}"` → ag); then for each permission, appends the scope-specific entry per the mapping table:
         - `projects:read:*` → `{user, :rx, "projects"}` (top-level enumerate) + `{user, :rx, "projects/*"}` (read all children); use a single entry `{user, :rx, "projects"}` and let Plan 05's reconciler apply default ACLs for children.
         - `projects:read:<scope>` → `{user, :rx, "projects/<scope>"}`.
         - `projects:write:<scope>` → `{user, :rwx, "projects/<scope>"}`.
         - `projects:write:*` → `{user, :rwx, "projects"}`.
         - `chat:read:*` → `{user, :r, "channels"}`.
         - `chat:read:<channel>` → `{user, :r, "channels/<channel>.md"}`.
         - `chat:write:*` and `chat:write:<channel>` → produce NO ACL entry. Per D-08 channels are `---` (denied) for every agent — Elixir is sole writer; the chat:write permission is checked at Router level (Plan 02), not at kernel level.
         - `agents:message:*` and `agents:message:<target>` → no ACL entry needed (writes go through Router → recipient inbox; recipient inbox is not the sender's, so no ACL grant required for the SENDER; Router copies on the SENDER's behalf).
         - `tasks:update:<scope>` → `{user, :rwx, "projects/<scope>/tasks"}`.
         - `agents:create:*` → no ACL entry (no agent is granted this in v1; Plan 04 enforces at Router; including in whitelist so AGT-05 enforcement reads from same parser).
       - All return values are deterministic (sorted by path) so Plan 05's `permissions_hash` is stable.

    2. Create `lib/glorbo/runtime/uid_allocator.ex`:

       - `subuid_base/1`: reads the `:subuid_path` (default `/etc/subuid`), pulls the line starting with `"#{user}:"`, parses the second column (subordinate base) as integer. `{:error, :no_subuid_entry}` if absent.
       - `current_allocations/1`: reads the sidecar JSON; returns `%{}` if absent.
       - `allocate/3`: arguments `(company, agents_list, opts)`. Reads current allocations; if company entry exists: computes the difference (new agents to add + missing agents to tombstone), assigns next free UID within the existing `uid_base..uid_base+99` range to new agents, moves removed agents to `tombstoned`. If absent: assigns next ordinal (0-based on entry count), uid_base = `subuid_base + 100*ordinal`, sequential UID assignment from that base. Writes JSON atomically (`File.write!(tmp); File.rename!(tmp, sidecar_path)`) with mode 0600.
       - Pure-IO boundary: `subuid_path` and `sidecar_path` are explicit opts so tests use tmp files; no `Path.expand("~/.glorbo")` inside the module body — the caller (Plan 05) supplies the production path.

    3. Create `.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md` documenting the 16 Phase-3 audit event keys from RESEARCH §"Audit event naming". For each event include: `action:` key, actor (sender_agent | system | director), fired-by module, payload schema (key + type). This file is the canonical source-of-truth Plans 02/03/04/05 reference when calling `Glorbo.Company.AuditLog.append/2`.

    4. Write `test/glorbo/security/acl_mapper_test.exs` covering behaviors 1–10. Use `ExUnit.Case, async: true`. No fixtures needed (pure module).

    5. Write `test/glorbo/runtime/uid_allocator_test.exs` covering behaviors 11–16. Use ExUnit `setup` to create tmp `subuid` + `sidecar` paths under `System.tmp_dir!()`.

    6. Run `mix format`, `mix credo --strict`, `mix compile --warnings-as-errors`, `mix test test/glorbo/security/acl_mapper_test.exs test/glorbo/runtime/uid_allocator_test.exs`.

    Why pure modules: research recommends "plain module with pure functions" for ACLMapper (RESEARCH §Claude's Discretion); UidAllocator separates parsing/IO so tests don't need /etc/subuid privileges. The IO surface (sidecar write) is small and dep-injectable via `:sidecar_path` opt — same pattern as Phase 2's Doctor.
    Why deterministic ACL entry ordering: `permissions_hash` on the agents table (added by Task 1) is computed from `acl_entries` output by Plan 05; non-deterministic ordering would cause spurious reconciles.
    Why AUDIT_EVENTS.md NOT in lib/: it's a documentation contract, not runtime code; lives next to CONTEXT/RESEARCH so the phase folder is self-contained for posterity.
  </action>
  <verify>
    <automated>mix test test/glorbo/security/acl_mapper_test.exs test/glorbo/runtime/uid_allocator_test.exs --color && mix credo --strict lib/glorbo/security/acl_mapper.ex lib/glorbo/runtime/uid_allocator.ex && mix format --check-formatted && grep -c '^| `' .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def parse_permission' lib/glorbo/security/acl_mapper.ex` succeeds
    - `grep -q 'def check_action' lib/glorbo/security/acl_mapper.ex` succeeds
    - `grep -q 'def acl_entries' lib/glorbo/security/acl_mapper.ex` succeeds
    - `grep -qE '@whitelisted_resources.*projects.*chat.*agents.*tasks' lib/glorbo/security/acl_mapper.ex` succeeds (verb whitelist present)
    - `! grep -E 'String\.to_atom|String\.to_existing_atom' lib/glorbo/security/acl_mapper.ex` (no atom creation from user input — T-03-01 mitigation)
    - `grep -q 'def subuid_base' lib/glorbo/runtime/uid_allocator.ex` succeeds
    - `grep -q 'def allocate' lib/glorbo/runtime/uid_allocator.ex` succeeds
    - `grep -q 'tombstoned' lib/glorbo/runtime/uid_allocator.ex` succeeds (D-04)
    - `grep -q '0o600\|0o0600' lib/glorbo/runtime/uid_allocator.ex` succeeds (sidecar chmod 0600 — T-03-02 mitigation)
    - `mix test test/glorbo/security/acl_mapper_test.exs test/glorbo/runtime/uid_allocator_test.exs` reports ≥ 16 tests, 0 failures
    - `wc -l .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md` ≥ 30 (header + table for 16 events)
    - `for ev in message.route message.reject permission.denied skill.missing provider.unknown budget.usage budget.alert budget.hard_stop approval.requested approval.granted approval.denied agent.wake agent.dispatch agent.complete acl.reconcile network.policy_applied; do grep -q "$ev" .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md || exit 1; done` succeeds
    - `mix credo --strict` exits 0
  </acceptance_criteria>
  <done>ACLMapper exposes parse_permission/1, check_action/3, acl_entries/2 with all 10 behaviors green; UidAllocator exposes subuid_base/1, allocate/3, current_allocations/1 with subuid-base detection + tombstone semantics + 0600 sidecar; AUDIT_EVENTS.md documents all 16 Phase-3 event keys; format/credo/tests clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Worker /run extends with skills_resolved + outbox/usage report writer</name>
  <files>
    containers/glorbo-runtime/worker/routes.py,
    containers/glorbo-runtime/worker/dispatch.py,
    containers/glorbo-runtime/worker/context.py,
    containers/glorbo-runtime/worker/usage.py,
    containers/glorbo-runtime/tests/test_usage.py,
    containers/glorbo-runtime/tests/test_routes.py
  </files>
  <read_first>
    containers/glorbo-runtime/worker/routes.py (existing RunRequest, /run handler, _live_tasks lifecycle),
    containers/glorbo-runtime/worker/dispatch.py (existing run_task signature, model-string convention),
    containers/glorbo-runtime/worker/context.py (existing load_task_context — extend to inject skills_resolved into messages),
    .planning/phases/02-filesystem-foundation-container-runtime-local-llm/02-CONTEXT.md (D-36 /run body schema STABILITY invariant — additive only),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-24 usage written AFTER result; D-25 litellm.completion_cost canonical; D-34 skills as full markdown body),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Code Examples §"litellm: completion + cost + usage", Pitfall A2 — coerce None to 0.0)
  </read_first>
  <behavior>
    - Test 1: `RunRequest(task_path="t", provider="ollama", model="x", request_id="r1")` parses with `skills_resolved == []` (default value present, additive).
    - Test 2: `RunRequest(..., skills_resolved=["# skill1\nbody1", "# skill2\nbody2"])` parses; field round-trips.
    - Test 3: `load_task_context("path", skills=[], skills_resolved=["FOO BAR"])` returns `messages` whose first message has role `system` and content containing literal `"FOO BAR"` (skills injected verbatim into system prompt, in order).
    - Test 4: `load_task_context("path", skills=[], skills_resolved=[])` produces a system message that does NOT contain a "Skills:" header (no empty section).
    - Test 5: `write_usage_report(outbox_root, agent_slug, request_id, task_id, provider="anthropic", model="claude-3-5-sonnet-20241022", response=mock_response)` creates `<outbox_root>/agents/<agent_slug>/outbox/usage/<request_id>.json` with all required keys (task_id, request_id, timestamp, provider, model, prompt_tokens, completion_tokens, total_tokens, cost_usd, cost_usd_cents) — verified by parsing the file.
    - Test 6: When `litellm.completion_cost` returns `None`, `write_usage_report` writes `cost_usd: 0.0` and `cost_usd_cents: 0` (Pitfall A2 coercion).
    - Test 7: When `litellm.completion_cost` returns `0.000045`, `cost_usd_cents` is `0` (int(round(0.000045 * 100)) = 0; report still records `cost_usd: 0.000045`).
    - Test 8: When `litellm.completion_cost` returns `0.0125`, `cost_usd_cents` is `1` (round(0.0125 * 100) = 1).
    - Test 9: `/run` integration test (using FastAPI's TestClient + monkeypatched dispatch.run_task): on success, file `<outbox_root>/agents/<agent>/outbox/usage/<request_id>.json` exists AFTER the response is returned; on failure (exception in run_task), the file is NOT written (D-24: "after writing the result").
    - Test 10: Negative — `/run` body without `skills_resolved` still succeeds (default = `[]`); confirms additive-only Phase-2 D-36 contract.
  </behavior>
  <action>
    1. Create `containers/glorbo-runtime/worker/usage.py` exporting `write_usage_report(outbox_root: str, agent_slug: str, request_id: str, task_id: str, provider: str, model: str, response, cost_fn=None) -> None`. Default `cost_fn=litellm.completion_cost`. Body: extract `prompt_tokens`/`completion_tokens`/`total_tokens` from `response.get("usage", {})` (defaults to 0); call `cost_fn(completion_response=response)`; coerce `None` → `0.0`; compute `cost_usd_cents = int(round(cost_usd * 100))`; build the dict per the schema in `<interfaces>`; write to `f"{outbox_root}/agents/{agent_slug}/outbox/usage/{request_id}.json"` via `Path.mkdir(parents=True, exist_ok=True)` + `Path.write_text(json.dumps(usage))`. Filename convention is request_id (NOT task_id) so concurrent retries of the same task get unique files.

    2. Modify `containers/glorbo-runtime/worker/routes.py`:
       - Add `skills_resolved: List[str] = []` to `RunRequest` (additive — preserves Phase 2 D-36 stability invariant).
       - Add `outbox_root: str = "/company"` field (defaults to the bind-mount path; tests override).
       - Add `agent_slug: str` field — REQUIRED so usage report knows where to write. Plan 03's Dispatch will populate from `agent.md`.
       - In `/run` handler's success path (after `result = await asyncio.wait_for(...)`): wrap the existing `RunResponse(ok=True, result=result)` with a try/except that calls `write_usage_report(req.outbox_root, req.agent_slug, req.request_id, task_id_from_path(req.task_path), req.provider, req.model, result.get("usage_response_obj") or result)`. The dispatch layer (modified in step 3) returns the raw response object alongside the existing `completion`/`usage`/`model` keys so usage.py can access litellm's response object for `completion_cost`.
       - **Failure paths** (TimeoutError, CancelledError, generic Exception): do NOT write usage report (D-24: "after writing the result"). The `finally` block only pops `_live_tasks`.
       - The new field placement: `skills_resolved` and `outbox_root` and `agent_slug` are added with defaults in a position that doesn't break existing pydantic validation order; `agent_slug` is required → Plan 03 must always send it (acceptable since Phase 3's Dispatch is the sole caller — Phase 2's airplane_mode_test will be back-edited to include `agent_slug: "ceo"` and `outbox_root: <test-tmp>` to keep it green).

    3. Modify `containers/glorbo-runtime/worker/dispatch.py`:
       - `run_task` returns dict that also includes `_response_obj: response` (underscore prefix marks internal-use; routes.py pops this before serializing the public RunResponse). Reason: `litellm.completion_cost(completion_response=response)` requires the original response object, not just the extracted usage dict.

    4. Modify `containers/glorbo-runtime/worker/context.py`'s `load_task_context` to accept `skills_resolved: List[str]` (default `[]`). When non-empty, prepend a system message with content `"You have access to the following skills:\n\n" + "\n\n---\n\n".join(skills_resolved)` to the existing messages list. When empty, do NOT add a "Skills:" header (clean prompt).

    5. Modify `routes.py` `/run` handler: pass `req.skills_resolved` through to `load_task_context(req.task_path, req.skills, req.skills_resolved)`. (Existing `req.skills` field is kept for now — Plan 03 will populate `skills_resolved` and leave `skills` empty; v1.1 may remove `skills`.)

    6. Update `containers/glorbo-runtime/tests/test_routes.py` and create `test_usage.py` covering behaviors 1–10. Use FastAPI's `TestClient` for /run tests; `monkeypatch` to replace `worker.dispatch.run_task` with a fake; `tmp_path` fixture for `outbox_root`.

    7. Run the worker's existing pytest suite to confirm zero regressions: `cd containers/glorbo-runtime && python -m pytest tests/ -x`.

    Why request_id (not task_id) in usage filename: a task may be retried (e.g., timeout → user resubmits) producing multiple usage reports for the same task_id; request_id is unique per /run call. Plan 02's BudgetTracker aggregates by month, not by task, so duplicate task_id rows are sum-correct.
    Why dispatch.py returns `_response_obj`: litellm.completion_cost needs the full response (it walks model+usage+timestamps); we pass it through as an internal contract. RunResponse.result strips underscored keys before JSON serialization back to Elixir (no leakage).
    Why agent_slug required not optional: usage report MUST land in the agent's outbox (Watcher routes by path prefix `agents/<name>/outbox/usage/`); guessing the slug from task_path would couple worker to filesystem schema. Plan 03 always knows the slug at dispatch.
    Phase 2 stability check: airplane_mode_test.exs in Phase 2 uses `RunRequest(task_path=..., provider=..., model=..., request_id=...)` — adding `agent_slug` as required would break it. Mitigation: back-edit airplane_mode_test.exs in this task's commit to send `agent_slug: "ceo", outbox_root: <test_tmp>`. This is a Phase-2-spec back-edit declared here (analogous to Phase 2 Plan 04's `extra_volumes` back-edit to Plan 03).
  </action>
  <verify>
    <automated>cd containers/glorbo-runtime && python -m pytest tests/test_usage.py tests/test_routes.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'skills_resolved: List\[str\]' containers/glorbo-runtime/worker/routes.py` succeeds
    - `grep -q 'agent_slug: str' containers/glorbo-runtime/worker/routes.py` succeeds
    - `grep -q 'outbox_root: str' containers/glorbo-runtime/worker/routes.py` succeeds
    - `grep -q 'def write_usage_report' containers/glorbo-runtime/worker/usage.py` succeeds
    - `grep -q 'completion_cost' containers/glorbo-runtime/worker/usage.py` succeeds
    - `grep -q 'cost_usd_cents' containers/glorbo-runtime/worker/usage.py` succeeds
    - `grep -q 'or 0.0' containers/glorbo-runtime/worker/usage.py` (None coercion — Pitfall A2)
    - `grep -q 'skills_resolved' containers/glorbo-runtime/worker/context.py` succeeds
    - pytest reports all new tests passing; no regressions in existing tests
    - airplane_mode_test.exs (Phase 2 back-edit applied) still green when run with `--include airplane` on a host with networking off
    - Phase 2 D-36 stability check: `python -c "from worker.routes import RunRequest; RunRequest(task_path='t', provider='p', model='m', request_id='r', agent_slug='a')"` succeeds (skills_resolved + outbox_root default, agent_slug required)
  </acceptance_criteria>
  <done>Worker accepts skills_resolved (additive Phase-2 contract), writes usage report after success only (D-24), uses litellm.completion_cost with None→0.0 coercion (Pitfall A2), all worker tests + airplane back-edit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Director (host) → Repo migrations | Migrations run as Director-trusted process; SQLite owned by Director |
| agent.md frontmatter (filesystem) → ACLMapper.parse_permission/1 | User-authored permission strings parsed; must reject malformed without raising or atom-creating |
| /etc/subuid (system) → UidAllocator.subuid_base/1 | World-readable; reading is not a privilege escalation |
| FastAPI worker (in container) → outbox/usage/*.json (mounted volume) | LLM-derived response data serialized to host-visible disk; allow-list keyed schema (no free-form serialization) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | lib/glorbo/security/acl_mapper.ex | mitigate | parse_permission/1: String.split limit:3; resource verb against fixed whitelist `~w(projects chat agents tasks)`; NO `String.to_atom`/`to_existing_atom` on user input (acceptance-criteria-enforced grep) |
| T-03-02 | Information Disclosure | lib/glorbo/runtime/uid_allocator.ex | mitigate | /etc/subuid read-only; sidecar JSON written with explicit chmod 0600 (acceptance-criteria-enforced grep); no secrets in file (only username→uid mapping) |
| T-03-03 | Information Disclosure | containers/glorbo-runtime/worker/usage.py | mitigate | Allow-list dict keys: task_id, request_id, timestamp, provider, model, prompt_tokens, completion_tokens, total_tokens, cost_usd, cost_usd_cents. NO message content. NO api_key. Schema is hand-built per write, not response.dict() |
| T-03-04 | Integrity | priv/repo/migrations/* | mitigate | All Phase-3 schemas additive (new tables + 1 new column). Forward-only. Phase-2 schema parity test (existing test_helper) ensures no rename/drop |
| T-03-05 | Denial of Service | containers/glorbo-runtime/worker/usage.py | accept | Disk-fill bounded by per-agent monthly budget hard-stop (SEC-05, Plan 02). No additional rate limit in v1 — accepted as residual |
| T-03-06 | Spoofing | containers/glorbo-runtime/worker/routes.py | mitigate | agent_slug REQUIRED in /run body; usage report path is `agents/<agent_slug>/outbox/usage/...` — a worker can only write to the agent_slug it was passed. Plan 05's per-container ACLs enforce kernel-side that the worker process can only write to its own agent's outbox. Defence in depth |
</threat_model>

<verification>
- `mix deps.get` (crontab resolves)
- `mix ecto.migrate` (3 migrations apply against fresh test repo)
- `mix test test/glorbo/budget_schema_test.exs test/glorbo/tasks_approval_state_test.exs test/glorbo/security/acl_mapper_test.exs test/glorbo/runtime/uid_allocator_test.exs` — all unit tests green
- `mix credo --strict` — 0 issues vs Phase-2 baseline
- `mix format --check-formatted` — clean
- `mix compile --warnings-as-errors` — clean
- `cd containers/glorbo-runtime && python -m pytest tests/ -x` — all worker tests green, no regression on existing test_routes.py
- Phase 2 back-edit verified: airplane_mode_test.exs RunRequest call updated for new required `agent_slug` field (kept :airplane gated; not run in default suite)
- `.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md` exists and documents 16 event keys (grep verifies)
- `locked_decisions:` in this PLAN frontmatter resolves OQ1, OQ2, OQ3 explicitly so Plans 02-05 can reference them
</verification>

<success_criteria>
- All 7 frontmatter must_haves.truths verified by acceptance criteria
- 3 migrations + 2 schemas + 2 pure modules + 1 Python module + 1 docs file all created
- No new GenServers; no supervisor changes; no container invocation changes
- Worker /run schema additive (Phase 2 D-36 invariant preserved — confirmed by RunRequest backward-compat test)
- The 3 critical open questions (OQ1, OQ2, OQ3) have locked decisions in PLAN frontmatter that Plans 02-05 reference
- All Plan 02-05 dependencies (ACLMapper exports, Budget schema, UidAllocator API, AUDIT_EVENTS.md, worker usage.py) exist and are documented in `<interfaces>`
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-routing-kernel-permissions-budgets/03-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- requirements-completed: [SEC-01 (partial — pure ACL mapping primitive, full enforcement in Plan 02), SEC-05 (partial — budget schema + worker usage emission, aggregation in Plan 02), AGT-04 (partial — worker accepts skills_resolved; Elixir resolver in Plan 03)]
- key-decisions: confirm OQ1/OQ2/OQ3 dispositions; cents-not-floats; deterministic ACL entry ordering for permissions_hash
- patterns-established: pure-module + dep-injectable-IO pattern (ACLMapper / UidAllocator); additive Python schema extension; AUDIT_EVENTS.md as cross-plan canonical reference
- back-edits: containers/glorbo-runtime/worker/* extended; airplane_mode_test.exs updated for agent_slug requirement
</output>
