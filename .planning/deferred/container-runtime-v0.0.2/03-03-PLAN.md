---
phase: 03
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - lib/glorbo/agent/server.ex
  - lib/glorbo/agent/dispatch.ex
  - lib/glorbo/agent/definition.ex
  - lib/glorbo/agent/registry.ex
  - lib/glorbo/skills/resolver.ex
  - lib/glorbo/llm/provider.ex
  - test/glorbo/agent/server_test.exs
  - test/glorbo/agent/dispatch_test.exs
  - test/glorbo/agent/definition_test.exs
  - test/glorbo/skills/resolver_test.exs
  - test/glorbo/llm/provider_test.exs
autonomous: true
gap_closure: false
requirements: [AGT-01, AGT-02, AGT-04, LLM-03, LLM-04]
locked_decisions:
  - "Per-agent Task.Supervisor lives INSIDE Agent.Server (started in init/1) so an agent crash takes its in-flight task with it (D-19 crash isolation goal; researcher-recommended)."
  - "Agent.Server state machine: %{current_task: nil | task_id, pending_wakes: [trigger]}. wake/2 enqueues if current_task != nil; dispatches otherwise (Pitfall 5 race mitigation)."
  - "Pending wakes processed FIFO in v1.0 (Open Question #4 deferred per researcher recommendation; reordering is surprising; reconsider in Phase 4 with priority-queue UI)."
  - "Provider whitelist enforced at agent.md PARSE TIME: provider must be in `[:ollama, :huggingface, :anthropic, :openai, :google]` or `Glorbo.Agent.Definition.parse/1` returns `{:error, {:invalid_provider, name}}`. LLM-04 one-provider-one-model enforced at parse time too."
  - "API keys read from `~/.glorbo/config.md` once per dispatch (no in-Elixir cache); file expected to be chmod 0600 (Doctor will check; Plan 03 reads via `Glorbo.LLM.Provider.resolve_key/2` which logs a warning if mode > 0600 but does not refuse to read)."
  - "Per-agent container LIFECYCLE granularity (Open Question #5 resolution): one container per (company, agent) for `lifecycle: persistent`; ephemeral mode spawns one container per task. No mixed-lifecycle sharing within a company. This matches the company-isolation invariant at finer grain."
  - "Skills resolution at dispatch time (D-34): missing skills produce a warning + `skill.missing` audit event + are dropped from `skills_resolved`; the dispatch proceeds (D-35 — recoverable)."
key-links:
  - from: lib/glorbo/agent/server.ex
    to: lib/glorbo/agent/dispatch.ex
    via: "Agent.Server.handle_info dispatches via Task.Supervisor.async"
  - from: lib/glorbo/agent/dispatch.ex
    to: lib/glorbo/company/budget_tracker.ex
    via: "Plan 02 BudgetTracker.check_budget/2 — pre-dispatch hard-stop"
  - from: lib/glorbo/agent/dispatch.ex
    to: lib/glorbo/skills/resolver.ex
    via: "Resolver.resolve/2 produces skills_resolved list"
  - from: lib/glorbo/agent/dispatch.ex
    to: lib/glorbo/llm/provider.ex
    via: "Provider.resolve_key/2 reads ~/.glorbo/config.md"
  - from: lib/glorbo/agent/dispatch.ex
    to: lib/glorbo/container/worker_client.ex
    via: "Phase 2 WorkerClient.post_run/4 with skills_resolved + agent_slug + outbox_root in body"
  - from: lib/glorbo/agent/registry.ex
    to: lib/glorbo/agent/server.ex
    via: "Registry.via_tuple/2 — name registration for per-agent GenServers"
  - from: lib/glorbo/agent/definition.ex
    to: lib/glorbo/security/acl_mapper.ex
    via: "Plan 01 ACLMapper.parse_permission/1 — validates each entry in agent.md `permissions:` list"

must_haves:
  truths:
    - "Glorbo.Agent.Definition.parse/1 reads agent.md frontmatter and returns {:ok, %{slug, provider, model, permissions, heartbeat, network, skills, lifecycle, budget_usd_cents, timeout_seconds}} for a valid file"
    - "Definition.parse rejects multi-model agent.md (provider:openai, models:[a,b]) with {:error, {:multiple_models, _}} (LLM-04)"
    - "Definition.parse rejects unknown provider with {:error, {:invalid_provider, _}}"
    - "Definition.parse rejects malformed permissions (e.g. `not:a:permission:with:five:parts`) with {:error, {:invalid_permission, _}} via ACLMapper.parse_permission/1"
    - "Glorbo.Skills.Resolver.resolve/2 reads skills/<name>.md from base, returns {:ok, [markdown_body, ...]} in declared order; missing skill produces {:warning, [body, ...], [missing_name]} + audits skill.missing"
    - "Glorbo.LLM.Provider.resolve_key/2 reads ~/.glorbo/config.md frontmatter `api_keys:` map; returns {:ok, key_string} for known providers; returns {:ok, nil} for ollama (no key needed); returns {:error, :missing_key} when provider needs key but config.md lacks it"
    - "Glorbo.Agent.Server.wake/2 with current_task=nil dispatches via injected dispatch_fun; with current_task != nil enqueues the trigger and returns :queued (Pitfall 5)"
    - "On task complete, Agent.Server processes pending_wakes FIFO and dispatches the next; current_task tracking moves to that task's id"
    - "Glorbo.Agent.Dispatch.run/3 pipeline: BudgetTracker.check_budget → load definition → resolve skills → resolve api_key → write current-task.json (D-22) → WorkerClient.post_run with skills_resolved + agent_slug + outbox_root → on completion, audit agent.complete and clear current-task.json"
    - "Dispatch budget hard-stop path: when check_budget returns {:stop, ...}, dispatch aborts BEFORE WorkerClient.post_run is called, emits budget.hard_stop audit, writes a notice to agents/<slug>/inbox/, returns {:error, :budget_stop}"
    - "Glorbo.Agent.Registry is a Registry process; per-agent name lookups via via_tuple/2 succeed; multiple agents in same company coexist without name collision"
  artifacts:
    - path: "lib/glorbo/agent/definition.ex"
      provides: "Pure agent.md parser with provider whitelist, single-model enforcement, permission validation"
      exports: ["parse/1", "parse_file/1"]
    - path: "lib/glorbo/agent/registry.ex"
      provides: "Per-agent process registration"
      exports: ["start_link/1", "via_tuple/2"]
    - path: "lib/glorbo/agent/server.ex"
      provides: "Per-agent GenServer with wake-queue state machine"
      exports: ["start_link/1", "wake/2", "current_task/1"]
    - path: "lib/glorbo/agent/dispatch.ex"
      provides: "Pre-dispatch pipeline (budget → skills → api_key → state file → WorkerClient)"
      exports: ["run/3"]
    - path: "lib/glorbo/skills/resolver.ex"
      provides: "Skills file → markdown body resolver with missing-skill warning"
      exports: ["resolve/2"]
    - path: "lib/glorbo/llm/provider.ex"
      provides: "config.md api_keys reader + provider whitelist"
      exports: ["resolve_key/2", "validate_provider/1"]
  key_links:
    - from: "lib/glorbo/agent/server.ex"
      to: "lib/glorbo/agent/dispatch.ex"
      via: "Task.Supervisor.async_nolink(state.task_sup, fn -> Dispatch.run(...) end)"
      pattern: "Glorbo\\.Agent\\.Dispatch\\.run"
    - from: "lib/glorbo/agent/dispatch.ex"
      to: "lib/glorbo/company/budget_tracker.ex"
      via: "BudgetTracker.check_budget/2 (Plan 02)"
      pattern: "BudgetTracker\\.check_budget"
    - from: "lib/glorbo/agent/dispatch.ex"
      to: "lib/glorbo/container/worker_client.ex"
      via: "WorkerClient.post_run/4 with skills_resolved+agent_slug+outbox_root"
      pattern: "WorkerClient\\.post_run"
    - from: "lib/glorbo/agent/definition.ex"
      to: "lib/glorbo/security/acl_mapper.ex"
      via: "Enum.map(perms, &ACLMapper.parse_permission/1)"
      pattern: "ACLMapper\\.parse_permission"

threat_model:
  trust_boundaries:
    - boundary: "agent.md frontmatter (Director-authored) → Definition.parse"
      description: "Director-trusted, but Definition still validates whitelist + structure to prevent runtime errors deep in dispatch"
    - boundary: "config.md api_keys (Director secret) → LLM.Provider.resolve_key"
      description: "Plain-text keys read from disk; never written to company directory; passed to WorkerClient body only"
    - boundary: "skills/<name>.md (Director-authored) → Skills.Resolver"
      description: "Resolver reads markdown bodies and concatenates into LLM context; Director's responsibility to vet skill content; resolver enforces only path-traversal sanity (skill name must match `[a-zA-Z0-9_-]+`)"
    - boundary: "Per-agent Linux user → Container Task.Supervisor → WorkerClient"
      description: "Plan 05 enforces per-agent ACLs at kernel; this plan does NOT touch ACLs — it only invokes the worker"
  threats:
    - id: "T-03-20"
      category: "Information Disclosure"
      component: "lib/glorbo/llm/provider.ex"
      disposition: "mitigate"
      mitigation: "API key never persisted in module state; resolved fresh per dispatch from ~/.glorbo/config.md; passed through WorkerClient request body only (D-37 + D-38). NEVER written to company dir (test grep verifies no `companies/` write inside provider.ex). Logger.warning emitted (NOT Logger.info or .debug — info would echo to file logs) if config.md mode > 0600."
    - id: "T-03-21"
      category: "Tampering"
      component: "lib/glorbo/agent/definition.ex"
      disposition: "mitigate"
      mitigation: "Provider whitelist + permission whitelist + single-model enforcement at parse time. No `String.to_atom` on input. Negative tests cover every rejection branch."
    - id: "T-03-22"
      category: "Tampering"
      component: "lib/glorbo/skills/resolver.ex"
      disposition: "mitigate"
      mitigation: "Skill name must match `~r/^[a-zA-Z0-9_-]+$/` — prevents path traversal (e.g., `../../../../etc/passwd`). Joins via Path.join(base, \"skills\", \"#{name}.md\"). Test asserts a name with `..` is rejected before any File.read."
    - id: "T-03-23"
      category: "Elevation of Privilege"
      component: "lib/glorbo/agent/dispatch.ex"
      disposition: "mitigate"
      mitigation: "Pre-dispatch budget check is the FIRST step of run/3; on {:stop, _, _}, no WorkerClient call, no container start, no LLM spend. Dispatch test asserts WorkerClient.post_run is NOT invoked on budget_stop path (mock-call counter)."
    - id: "T-03-24"
      category: "Repudiation"
      component: "lib/glorbo/agent/server.ex"
      disposition: "mitigate"
      mitigation: "Every wake/2 call emits agent.wake audit event (with trigger). Every Dispatch.run completion emits agent.complete (or agent.error on exception). Director can reconstruct the agent's wake/dispatch history from audit log alone."
    - id: "T-03-25"
      category: "DoS"
      component: "lib/glorbo/agent/server.ex"
      disposition: "mitigate"
      mitigation: "DynamicSupervisor (Plan 05) runs Agent.Server with `max_restarts: 3, max_seconds: 60`; pathological wake-loop crashes are isolated per agent. current-task.json + pending_wakes give crash-recovery semantics: on restart, server reads current-task.json and either resumes or rolls back to idle."
---

<objective>
Implement the per-agent runtime: the agent.md parser, the per-agent registry, the per-agent GenServer (with wake-queue state machine to handle the Pitfall 5 approval-mid-dispatch race), the Dispatch pipeline (budget check → skills resolve → api_key resolve → current-task.json write → WorkerClient call → audit), the skills resolver, and the LLM-provider key resolver.

This plan owns the AGENT side of Phase 3. It depends on Plan 01 (ACLMapper.parse_permission, AUDIT_EVENTS.md, worker /run schema with skills_resolved + agent_slug + outbox_root) and Plan 02 (BudgetTracker.check_budget — but BudgetTracker is dep-injected so Plan 02 doesn't need to be merged before this plan compiles + tests). Files modified are DISJOINT from Plan 02 (router/scheduler/budget_tracker), Plan 04 (approvals/permission/AGT-05), and Plan 05 (security/network/container) — same wave, no file overlap.

Approval gates (D-30..D-33) are NOT in this plan — they belong to Plan 04. The Agent.Server here knows about a generic `:director-approval` trigger but doesn't read/write sentinels; Approvals.Gate (Plan 04) calls `Agent.Server.wake(server, :director-approval)` with the task already approved.

Purpose: Deliver a fully-tested per-agent runtime where every dispatch path goes through budget → skills → api_key → durable state → worker call. No GenServer wired into a supervisor here (Plan 05 wires `AgentSupervisor` as the 6th child of Company.Supervisor and starts agents from disk).

Output: 6 modules + 5 test files. The Agent.Server can be `start_supervised!`'d in tests with all upstream deps injected; it correctly handles Pitfall 5 (concurrent wakes) and Pitfall 4 indirectly (BudgetTracker.check_budget is the budget-side hot path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-VALIDATION.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-01-PLAN.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md
@CLAUDE.md
@DESIGN.md
@lib/glorbo/agent/server.ex
@lib/glorbo/security/acl_mapper.ex
@lib/glorbo/container/worker_client.ex
@lib/glorbo/container/invocation.ex
@lib/glorbo/container_manager.ex
@lib/glorbo/filesystem/frontmatter.ex
@lib/glorbo/filesystem/hierarchy.ex

<interfaces>
<!-- Plan 04 (Approvals) calls Agent.Server.wake/2 with :director-approval trigger -->
<!-- Plan 05 (supervisor wiring) starts AgentSupervisor and per-agent children -->

Glorbo.Agent.Definition:
```elixir
@type definition :: %{
        slug: String.t(),
        provider: :ollama | :huggingface | :anthropic | :openai | :google,
        model: String.t(),
        permissions: [{String.t(), String.t(), String.t()}],
        heartbeat: String.t() | nil,                # cron expr or nil
        network: :none | :api_only | :open,
        skills: [String.t()],
        lifecycle: :ephemeral | :persistent,
        budget_usd_cents: non_neg_integer(),        # 0 means no cap
        timeout_seconds: pos_integer()
      }
@spec parse(map() | binary()) :: {:ok, definition()} | {:error, term()}
@spec parse_file(Path.t()) :: {:ok, definition()} | {:error, term()}
```

Glorbo.Agent.Registry:
```elixir
@spec start_link(opts :: keyword()) :: GenServer.on_start()
@spec via_tuple(company :: String.t(), agent_slug :: String.t()) :: {:via, Registry, {Glorbo.Agent.Registry, {String.t(), String.t()}}}
```

Glorbo.Agent.Server:
```elixir
@type trigger :: :inbox | :heartbeat | :mention | :director_approval | :director_request

@spec start_link(opts :: keyword()) :: GenServer.on_start()
# opts: name (via_tuple), company, agent_slug, base, definition, dispatch_fun, audit_fun, budget_check_fun

@spec wake(server :: GenServer.server(), trigger()) :: :dispatched | :queued
@spec current_task(server :: GenServer.server()) :: nil | %{task_id: String.t(), trigger: trigger(), started_at: DateTime.t()}
```

Glorbo.Agent.Dispatch:
```elixir
@spec run(company :: String.t(), agent_slug :: String.t(), trigger :: atom(), opts :: keyword()) ::
        {:ok, map()} | {:error, :budget_stop | :missing_skill | :no_task | term()}
# opts: definition, base, budget_check_fun, resolve_skills_fun, resolve_key_fun, post_run_fun, audit_fun, pick_task_fun
```

Glorbo.Skills.Resolver:
```elixir
@spec resolve(skills :: [String.t()], opts :: keyword()) ::
        {:ok, [String.t()]} | {:warning, [String.t()], missing :: [String.t()]}
# opts: base (default ~/.glorbo)
```

Glorbo.LLM.Provider:
```elixir
@spec validate_provider(name :: String.t() | atom()) ::
        {:ok, :ollama | :huggingface | :anthropic | :openai | :google} | {:error, :invalid_provider}
@spec resolve_key(provider :: atom(), opts :: keyword()) ::
        {:ok, String.t() | nil} | {:error, :missing_key | :config_unreadable}
# opts: base (default ~/.glorbo)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure parsers — Definition + Skills.Resolver + LLM.Provider</name>
  <files>
    lib/glorbo/agent/definition.ex,
    lib/glorbo/skills/resolver.ex,
    lib/glorbo/llm/provider.ex,
    test/glorbo/agent/definition_test.exs,
    test/glorbo/skills/resolver_test.exs,
    test/glorbo/llm/provider_test.exs
  </files>
  <read_first>
    lib/glorbo/security/acl_mapper.ex (Plan 01 — parse_permission/1),
    lib/glorbo/filesystem/frontmatter.ex (Phase 2 — parse_file/1 + extract metadata),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-37/D-38/D-39/D-40 provider rules; D-34/D-35/D-36 skills),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Don't Hand-Roll table — YAML parsing; Pitfall 5 — race; ASVS V5 input validation),
    DESIGN.md §5.1 (agent.md schema example), DESIGN.md §4.3 (config.md api_keys schema)
  </read_first>
  <behavior>
    Glorbo.Agent.Definition:
    - Test 1: Valid full agent.md with provider:ollama + model + permissions + heartbeat + network:none + skills + lifecycle:ephemeral + budget_usd: 0 + timeout_seconds: 300 parses to a definition struct with all fields populated.
    - Test 2: Missing required field (no `provider:`) → `{:error, {:missing_field, :provider}}`.
    - Test 3: Multi-model — frontmatter has `models: [foo, bar]` instead of `model: foo` → `{:error, {:multiple_models, ...}}` (LLM-04).
    - Test 4: Unknown provider — `provider: gemini2` → `{:error, {:invalid_provider, "gemini2"}}`.
    - Test 5: Malformed permission — `permissions: [foo, "bad:perm:format:fivecolons"]` → `{:error, {:invalid_permission, "bad:perm:format:fivecolons"}}` (delegates to ACLMapper.parse_permission).
    - Test 6: Default values — frontmatter without `network:` → `network: :none` (D-10 default); without `lifecycle:` → `:ephemeral` (D-23); without `budget_usd:` → `budget_usd_cents: 0` (no cap); without `timeout_seconds:` → 300.
    - Test 7: Provider:ollama is the only one that does NOT require `model:` to be set explicitly (defaults to "llama3.2:1b" — example company convention); other providers REQUIRE explicit `model:`.
    - Test 8: Heartbeat cron is validated via Crontab parser at parse time (since Plan 02 runs the parser at schedule time too, this is belt-and-braces); `heartbeat: "garbage"` → `{:error, {:invalid_heartbeat, "garbage"}}`.
    - Test 9: Network whitelist — `network: open_to_world` → `{:error, {:invalid_network, "open_to_world"}}`. Only `none|api-only|open` accepted.

    Glorbo.Skills.Resolver:
    - Test 10: `resolve(["a", "b"], base: tmpdir)` where `<tmpdir>/skills/a.md` and `<tmpdir>/skills/b.md` exist → `{:ok, [body_a, body_b]}` in declared order.
    - Test 11: `resolve(["a", "missing", "b"], base: tmpdir)` → `{:warning, [body_a, body_b], ["missing"]}` (skipped, declared order preserved).
    - Test 12: `resolve(["../../etc/passwd"], base: tmpdir)` → `{:warning, [], ["../../etc/passwd"]}` (path traversal rejected; treated as missing).
    - Test 13: `resolve([], opts)` → `{:ok, []}` (empty case).

    Glorbo.LLM.Provider:
    - Test 14: `validate_provider("anthropic")` → `{:ok, :anthropic}`. `validate_provider("rogue")` → `{:error, :invalid_provider}`.
    - Test 15: `resolve_key(:ollama, base: tmpdir_with_no_config)` → `{:ok, nil}` (Ollama needs no key; no error).
    - Test 16: `resolve_key(:anthropic, base: tmpdir_with_config_with_anthropic_key)` → `{:ok, "sk-..."}`.
    - Test 17: `resolve_key(:anthropic, base: tmpdir_with_config_no_keys)` → `{:error, :missing_key}`.
    - Test 18: `resolve_key(:anthropic, base: tmpdir_with_no_config)` → `{:error, :config_unreadable}`.
    - Test 19: When config.md exists with mode 0644 (loose), `resolve_key` still returns the key BUT emits a `Logger.warning` (use `ExUnit.CaptureLog` — assert log message contains "0644" or "permissions").
  </behavior>
  <action>
    1. Create `lib/glorbo/agent/definition.ex`:
       - `@valid_providers ~w(ollama huggingface anthropic openai google)a`
       - `@valid_networks ~w(none api-only open)a`
       - `@valid_lifecycles ~w(ephemeral persistent)a`
       - `parse(frontmatter_map)`: pattern-match each required field; validate provider via `LLM.Provider.validate_provider/1`; reject `models:` key (multi-model); validate each permission via `ACLMapper.parse_permission/1`; validate cron via `Crontab.CronExpression.Parser.parse/1` if heartbeat present; default missing optional fields per Test 6.
       - `parse_file(path)`: `YamlFrontMatter.parse_file(path) |> case ... |> parse(_)`; agent slug derived from parent directory name (`agents/<slug>/agent.md`).

    2. Create `lib/glorbo/skills/resolver.ex`:
       - `@skill_name_re ~r/^[a-zA-Z0-9_-]+$/`
       - `resolve(skills, opts)`: for each name, validate against regex first (path traversal defense — T-03-22); if match: `File.read(Path.join([base, "skills", "#{name}.md"]))` → on error treat as missing. Returns `{:ok, bodies}` if no missing, else `{:warning, bodies, missing}`.

    3. Create `lib/glorbo/llm/provider.ex`:
       - `validate_provider/1`: accepts string OR atom; converts string via `String.to_existing_atom` ONLY if it's in the whitelist (use a `Map.has_key?` check first to avoid creating new atoms from input — T-03-21 mitigation).
       - `resolve_key/2`: `:ollama` → `{:ok, nil}` (short-circuit; never reads config).
         For others: `Path.join(base, "config.md")`; check `File.stat` to verify mode <= 0o600 (warn if not); `YamlFrontMatter.parse_file/1`; pull `api_keys[provider]` (provider as string key in YAML); `nil` → `{:error, :missing_key}`; missing/unreadable file → `{:error, :config_unreadable}`.
       - Logger.warning on loose mode; the resolve still proceeds (Director's prerogative — they may have intentionally world-read for sharing within own machine).

    4. Write the three test files. Use `ExUnit.Case, async: true` (pure modules; no DB needed). For LLM.Provider tests, use `tmp_path` fixtures and `File.chmod!/2`. Use `ExUnit.CaptureLog.capture_log/1` for Test 19.

    5. Run `mix test` for the three new test files; `mix credo --strict`; `mix format --check-formatted`.

    Why parse-time validation for cron+permissions+provider+network: fail FAST. Plan 02's Scheduler also parses cron, but agent.md errors should surface before the agent ever starts (Director sees them in `glorbo doctor` or `mix glorbo.agents.validate`).
    Why path-traversal regex on skill names: D-34 says skills are read from `skills/<n>.md`; without validation, a skill named `../../../../etc/passwd` would let an LLM trick the worker into reading sensitive files. Reject before File.read.
    Why warn-not-fail on loose config.md mode: Director owns ~/.glorbo/; they chose the mode. Doctor can refuse-to-start in a future hardened-mode flag.
    Why models: list explicitly rejected (not silently use models[0]): RESEARCH §LLM-04 — "one provider+model per agent"; ambiguous configs should fail loudly.
  </action>
  <verify>
    <automated>mix test test/glorbo/agent/definition_test.exs test/glorbo/skills/resolver_test.exs test/glorbo/llm/provider_test.exs --color --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@valid_providers' lib/glorbo/agent/definition.ex` succeeds (whitelist enforced)
    - `grep -q 'ACLMapper.parse_permission' lib/glorbo/agent/definition.ex` succeeds (permissions delegated)
    - `grep -q 'Crontab.CronExpression.Parser.parse' lib/glorbo/agent/definition.ex` succeeds (heartbeat validated)
    - `grep -q '@skill_name_re' lib/glorbo/skills/resolver.ex` succeeds (path-traversal regex defined)
    - `! grep -E 'String\.to_atom\\(' lib/glorbo/llm/provider.ex` (no new atom creation; T-03-21)
    - `! grep -E 'String\.to_atom\\(' lib/glorbo/agent/definition.ex` (same)
    - `grep -q 'Logger.warning\|Logger.warn' lib/glorbo/llm/provider.ex` succeeds (loose-mode warning)
    - `! grep -E 'companies/' lib/glorbo/llm/provider.ex` (api_key never written/read from companies dir; T-03-20)
    - `mix test test/glorbo/agent/definition_test.exs test/glorbo/skills/resolver_test.exs test/glorbo/llm/provider_test.exs` exits 0 with all 19 behaviors green
    - `mix credo --strict lib/glorbo/agent/definition.ex lib/glorbo/skills/resolver.ex lib/glorbo/llm/provider.ex` exits 0
  </acceptance_criteria>
  <done>3 pure parsers exist with whitelist enforcement (provider, network, lifecycle, permissions, cron), path-traversal defense on skill names, loose-mode warning on config.md. All 19 behaviors green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Glorbo.Agent.Registry + Glorbo.Agent.Server with wake-queue state machine</name>
  <files>
    lib/glorbo/agent/registry.ex,
    lib/glorbo/agent/server.ex,
    test/glorbo/agent/server_test.exs
  </files>
  <read_first>
    lib/glorbo/agent/server.ex (Phase 1 stub — being replaced),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-19, D-20, D-22, D-23),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pitfall 5 §"Approval-flip race"; Architecture §"per-Agent Task.Supervisor"),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md (agent.wake, agent.dispatch, agent.complete payloads)
  </read_first>
  <behavior>
    Registry:
    - Test 1: `Registry.via_tuple("acme", "ceo")` returns `{:via, Registry, {Glorbo.Agent.Registry, {"acme", "ceo"}}}` (deterministic).
    - Test 2: starting two GenServers using `via_tuple/2` for distinct agent slugs both succeed without name collision.

    Agent.Server:
    - Test 3 (idle wake → dispatch): start server in idle state; inject `dispatch_fun` capturing args + returning `{:ok, %{task_id: "t1"}}`. Call `wake(server, :inbox)` → returns `:dispatched`; `current_task/1` returns map containing `task_id: "t1"`; an `agent.wake` audit event is emitted with `trigger: :inbox`.
    - Test 4 (busy wake → queue): server's current_task is set (e.g., dispatch is awaiting); call `wake(server, :heartbeat)` → returns `:queued`; `current_task` unchanged; pending_wakes includes :heartbeat. NO additional dispatch_fun call.
    - Test 5 (task complete → drain queue FIFO): with pending_wakes = [:heartbeat, :mention], simulate task completion via `send(server, {:task_complete, "t1", :ok})`; assert dispatch_fun is called for :heartbeat (FIFO head); current_task moves to next task_id; :mention remains in queue.
    - Test 6 (Pitfall 5 — approval mid-dispatch): start server idle; wake(server, :inbox) starts t1; wake(server, :director_approval) returns :queued; complete t1 → :director_approval dispatched next.
    - Test 7 (dispatch failure handled): dispatch_fun returns `{:error, :budget_stop}` → server emits `agent.error` audit (or `agent.complete` with error key per AUDIT_EVENTS.md), clears current_task, drains pending_wakes.
    - Test 8 (crash recovery — current_task.json): pre-populate `<base>/companies/<co>/agents/<ag>/state/current-task.json`; restart Agent.Server; init reads the file and decides:
       - file present + recent (within 5 minutes) → emit `agent.recovered` audit with the stale task_id, then DELETE the file (no auto-resume in v1; Director investigates via audit log; resumed via inbox/manual trigger).
       - file absent or stale → no-op.
    - Test 9 (registry name): server starts via `via_tuple/2`; `GenServer.call(via_tuple("acme", "ceo"), :get_state)` reaches it.
  </behavior>
  <action>
    1. Create `lib/glorbo/agent/registry.ex`:
       ```elixir
       defmodule Glorbo.Agent.Registry do
         @doc "Child spec for an Application supervisor — Plan 05 wires this into Glorbo.Application."
         def child_spec(opts) do
           Registry.child_spec(keys: :unique, name: __MODULE__, partitions: 1)
         end

         def via_tuple(company, agent_slug) do
           {:via, Registry, {__MODULE__, {company, agent_slug}}}
         end
       end
       ```

    2. Replace `lib/glorbo/agent/server.ex` Phase-1 stub with full implementation:

       - State: `%{company, agent_slug, base, definition, current_task: nil | %{task_id, trigger, started_at}, pending_wakes: [], task_sup, dispatch_fun, audit_fun, recovery_window_seconds: 300}`.
       - `init/1`: starts `Task.Supervisor.start_link(strategy: :one_for_one)` and stores PID in state (D-19 — per-agent task supervisor lives inside the GenServer; agent crash kills the task with it). Calls `recover/1` which reads `state/current-task.json` if present + recent → emits `agent.recovered` audit + deletes the file. Returns `{:ok, state}`.
       - `wake/2` (call): if `state.current_task == nil` → `dispatch_internal/2` (emits `agent.wake` audit, sets current_task, spawns Task via `Task.Supervisor.async_nolink`); returns `:dispatched`. Else: prepend trigger to pending_wakes; emits `agent.wake` audit with `queued: true` flag (so audit log distinguishes queued vs dispatched); returns `:queued`.
       - `current_task/1` (call): returns state.current_task (nil or struct).
       - `handle_info({:task_complete, task_id, result}, state)`: if `state.current_task.task_id == task_id`: emit `agent.complete` audit (with `result`), delete `state/current-task.json`, clear current_task. Then: if pending_wakes non-empty → pop FIFO head (`Enum.reverse(state.pending_wakes) |> tl |> Enum.reverse` — pending_wakes is stored in reverse order for O(1) prepend; reverse on drain), dispatch_internal again. Else: stay idle.
       - `dispatch_internal/2`: writes `<base>/companies/<co>/agents/<ag>/state/current-task.json` with `{task_id: ..., trigger: ..., started_at: ...}` (D-22 — durable state for crash recovery). Calls `Task.Supervisor.async_nolink(state.task_sup, fn -> result = state.dispatch_fun.(state.company, state.agent_slug, trigger, opts); send(parent_pid, {:task_complete, task_id, result}) end)`. Audit `agent.dispatch`.
       - `handle_info({:DOWN, ref, :process, _, reason}, state)`: when async_nolink Task crashes, treat as `{:task_complete, current_task.task_id, {:error, {:task_crashed, reason}}}`.

    3. Write `test/glorbo/agent/server_test.exs` covering 9 behaviors. Use `start_supervised!/1`. Inject `dispatch_fun` (capturing to test pid via `send` for assertion); inject `audit_fun` (capturing to ETS or Agent for inspection); use `tmp_path` for `base`. For Test 8 (recovery), pre-create the JSON file before `start_supervised!` and assert post-start that the file is gone + an `agent.recovered` audit was emitted.

    Why per-agent Task.Supervisor inside Server (not as Company child): D-19's crash-isolation goal — if the agent crashes, the in-flight task should die with it (not continue processing under a different process tree). Linking via `async_nolink` means the Server still gets `:DOWN` to clean up.
    Why pending_wakes prepend + reverse on drain: O(1) wake-time enqueue (prepend to list head); O(n) drain only when actually firing pending wakes — which is bounded by however many wakes arrived during a single dispatch.
    Why FIFO not priority: RESEARCH Open Question #4 — researcher recommends FIFO for v1.0 (reordering surprises Director); Phase 4 with dashboard UI can introduce a priority enum.
    Why current-task.json read on init but NOT auto-resumed: D-22 says it's for crash recovery context, not for guaranteed at-most-once execution. v1 chooses safety (no double-execution) over completeness (Director re-triggers via inbox).
  </action>
  <verify>
    <automated>mix test test/glorbo/agent/server_test.exs --color --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def via_tuple' lib/glorbo/agent/registry.ex` succeeds
    - `grep -q 'Registry.child_spec' lib/glorbo/agent/registry.ex` succeeds
    - `grep -q 'def wake' lib/glorbo/agent/server.ex` succeeds
    - `grep -q 'def current_task' lib/glorbo/agent/server.ex` succeeds
    - `grep -q 'Task.Supervisor.start_link\|Task.Supervisor.async_nolink' lib/glorbo/agent/server.ex` succeeds (per-agent task supervisor — D-19)
    - `grep -q 'pending_wakes' lib/glorbo/agent/server.ex` succeeds (Pitfall 5 mitigation)
    - `grep -q 'current-task.json' lib/glorbo/agent/server.ex` succeeds (D-22 durable state)
    - `grep -q 'agent.wake\|agent.dispatch\|agent.complete' lib/glorbo/agent/server.ex` succeeds (audit events)
    - `mix test test/glorbo/agent/server_test.exs` exits 0; all 9 behaviors green
    - `mix credo --strict lib/glorbo/agent/registry.ex lib/glorbo/agent/server.ex` exits 0
  </acceptance_criteria>
  <done>Agent.Server has wake-queue state machine resolving Pitfall 5; per-agent Task.Supervisor inside Server (D-19); current-task.json read on init for crash-recovery context; Registry via_tuple working; all 9 behaviors green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Glorbo.Agent.Dispatch — pre-dispatch pipeline (budget → skills → key → state → worker)</name>
  <files>
    lib/glorbo/agent/dispatch.ex,
    test/glorbo/agent/dispatch_test.exs
  </files>
  <read_first>
    lib/glorbo/agent/server.ex (Task 2 — uses dispatch_fun = Dispatch.run/3),
    lib/glorbo/agent/definition.ex (Task 1 — definition struct),
    lib/glorbo/skills/resolver.ex (Task 1),
    lib/glorbo/llm/provider.ex (Task 1),
    lib/glorbo/container/worker_client.ex (Phase 2 — post_run/4 with body),
    lib/glorbo/company/budget_tracker.ex (Plan 02 — check_budget/2),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-23 lifecycle, D-27 hard-stop, D-37/D-38 api_key body injection),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pattern 5 §"Pre-dispatch budget hard-stop"; Code Examples)
  </read_first>
  <behavior>
    - Test 1 (happy path): all deps return success; pipeline executes in order budget → load definition → resolve skills → resolve api_key → write current-task.json → WorkerClient.post_run with body containing skills_resolved + agent_slug + outbox_root: "/company" (literal in-container bind-mount path, NOT host path) + api_key + provider + model; audit `agent.dispatch` (with provider+model+container_id) and `agent.complete` (with duration_ms+exit_status); current-task.json is deleted at end. Returns `{:ok, %{...}}`.
    - Test 2 (budget stop short-circuits): inject `budget_check_fun` returning `{:stop, 100, 100}` → Dispatch.run returns `{:error, :budget_stop}` BEFORE WorkerClient is called (mock counter assertion: post_run_fun never invoked); audit `budget.hard_stop` AND `agent.error` (or skipped agent.dispatch); a notice file is appended to `agents/<slug>/inbox/<timestamp>-budget-stop.md`.
    - Test 3 (no task to pick): `pick_task_fun` returns `{:error, :no_task}` → returns `{:error, :no_task}`; audit `agent.idle` (or no audit — depends on AUDIT_EVENTS.md; if no event documented, do not invent; return + log only). NO WorkerClient call.
    - Test 4 (missing skill): `resolve_skills_fun` returns `{:warning, [body], ["foo"]}` → emits `skill.missing` audit per missing skill (D-35); proceeds with the partial list; WorkerClient called with `skills_resolved: [body]`.
    - Test 5 (api_key resolution failure for provider that needs key): provider :anthropic, `resolve_key_fun` returns `{:error, :missing_key}` → emits `provider.unknown` (or specific `provider.no_key`) audit; returns `{:error, :missing_key}` WITHOUT WorkerClient call.
    - Test 6 (api_key never in audit): inspect every audit_fun call's payload — none contain the literal api_key value (defense against accidentally logging keys). Test fixture uses `"sk-VERYSECRET"`; assert no audit payload string contains "VERYSECRET".
    - Test 7 (current-task.json lifecycle): file written BEFORE post_run; file deleted AFTER post_run completes (whether success or worker error). On budget_stop short-circuit (Test 2), file is NEVER written.
    - Test 8 (lifecycle:persistent vs ephemeral): Definition with `lifecycle: :persistent` causes ContainerManager.start_container/2 (mock) to be called with `mode: :persistent`; default :ephemeral with `mode: :ephemeral`. (Verify via injected `start_container_fun` capturing the mode arg.)
    - Test 9 (outbox_root contract): the value passed to post_run_fun for outbox_root is the literal string "/company" (the in-container bind-mount path). A contract assertion in dispatch_test.exs verifies `String.starts_with?(captured_body.outbox_root, "/company")`. This guards against passing a host path like `/home/.../companies/acme` which would cause the worker to write usage reports to a nonexistent in-container directory — SEC-05 usage chain silently fails if this drifts.
  </behavior>
  <action>
    1. Create `lib/glorbo/agent/dispatch.ex` exposing `run/4` (positional company + agent_slug + trigger + opts). All side effects are dep-injected via opts — no implicit module references — so the test can fully isolate.

       Pseudocode:
       ```
       def run(company, agent_slug, trigger, opts) do
         %{
           definition: definition,
           base: base,
           budget_check_fun: budget_fn,    # default → BudgetTracker.check_budget/2 via via-name
           resolve_skills_fun: skills_fn,  # default → Skills.Resolver.resolve/2
           resolve_key_fun: key_fn,        # default → LLM.Provider.resolve_key/2
           pick_task_fun: pick_fn,         # default → pick_next_task_from_inbox/2
           start_container_fun: start_fn,  # default → ContainerManager.start_container/2
           post_run_fun: post_fn,          # default → WorkerClient.post_run/4
           audit_fun: audit_fn,            # default → AuditLog.append/2 via via-name
         } = normalize_opts(opts)

         with {:budget, :ok}        <- {:budget, budget_fn.(company, agent_slug)},
              {:task, {:ok, task}}  <- {:task, pick_fn.(company, agent_slug, base)},
              {:skills, skills_resolved} <- {:skills, resolve_skills(definition.skills, base, audit_fn, skills_fn)},
              {:key, {:ok, api_key}}    <- {:key, key_fn.(definition.provider, base: base)},
              :ok <- write_current_task(base, company, agent_slug, task.id, trigger),
              {:ok, container_id} <- start_fn.(company, agent_slug, mode: definition.lifecycle, base: base),
              :ok <- audit_fn.(company, %{action: "agent.dispatch", agent: agent_slug, task_path: task.path, provider: to_string(definition.provider), model: definition.model, container_id: container_id}),
              {:ok, response, duration_ms} <- timed(fn -> post_fn.(company, agent_slug, %{task_path: task.path, provider: to_string(definition.provider), model: definition.model, api_key: api_key, skills_resolved: skills_resolved, agent_slug: agent_slug, outbox_root: "/company", request_id: gen_request_id(), timeout_seconds: definition.timeout_seconds}, []) end),
              :ok <- audit_fn.(company, %{action: "agent.complete", agent: agent_slug, task_path: task.path, duration_ms: duration_ms, exit_status: response.ok}),
              :ok <- delete_current_task(base, company, agent_slug) do
           {:ok, response}
         else
           {:budget, {:stop, used, cap}} ->
             audit_fn.(company, %{action: "budget.hard_stop", agent: agent_slug, used_cents: used, cap_cents: cap, attempted_task: trigger})
             append_inbox_notice(base, company, agent_slug, "budget exceeded: $#{cents_to_usd(used)} of $#{cents_to_usd(cap)}")
             {:error, :budget_stop}
           {:task, {:error, :no_task}} -> {:error, :no_task}
           {:key, {:error, reason}} ->
             audit_fn.(company, %{action: "provider.unknown", agent: agent_slug, provider: to_string(definition.provider), reason: to_string(reason)})
             {:error, reason}
           # ... similar branches for write/start/post errors
         end
       end
       ```

    2. Internal helpers:
       - `resolve_skills(skills, base, audit_fn, skills_fn)`: calls skills_fn; on `{:warning, bodies, missing}` emit `skill.missing` per missing entry; return bodies. On `{:ok, bodies}` return bodies.
       - `pick_next_task_from_inbox/3`: lists `agents/<slug>/inbox/*.md`, parses each frontmatter, picks the FIRST one (FIFO by mtime); returns `{:ok, %{id, path}}` or `{:error, :no_task}`. v1 keeps it simple — Plan 04 will refine when approval gates land.
       - `write_current_task(base, co, ag, task_id, trigger)`: writes JSON `{task_id, trigger, started_at: now_iso}` to `state/current-task.json` with mkdir_p.
       - `delete_current_task/3`: idempotent rm.
       - `append_inbox_notice/4`: writes to `agents/<slug>/inbox/<timestamp>-system.md` so the agent reads the rejection on next wake.
       - `cents_to_usd/1`: integer math display only (no float math anywhere in real arithmetic per Plan 01 locked decision).

    3. Audit payload defense: the `audit_fn` calls in Dispatch MUST NEVER include `api_key` in the payload. Add a hard-coded grep guard in the dispatch module (a `# T-03-20: api_key MUST never reach audit_fn` comment near the call sites) and a Test 6 assertion that no audit payload string contains the test's api_key value.

    4. Write `test/glorbo/agent/dispatch_test.exs` covering 9 behaviors. Use `Mox`-free pure functions (Phase 1/2 convention — inject closures instead of mocks). Each test sets up `opts` with all dep-funs as test-pid-message-sending closures so assertions can inspect the call sequence.

    Why everything dep-injected: keeps Dispatch testable without DataCase, without Watcher, without Podman. Test 6's "no api_key in audit" is a pure assertion over captured calls.
    Why `with` chain not nested case: the "fail-fast at any step" semantics map to Elixir's `with` perfectly; the `else` branches name each failure.
    Why pick_next_task is its own injectable function: Plan 04 will replace this with an approval-aware version that skips `requires_approval: director` tasks not in approved state. v1 here just FIFOs the inbox.
    Why request_id generated per-run: matches Plan 01's worker usage filename convention; gen via `Base.url_encode64(:crypto.strong_rand_bytes(16))`.
  </action>
  <verify>
    <automated>mix test test/glorbo/agent/dispatch_test.exs --color --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def run' lib/glorbo/agent/dispatch.ex` succeeds
    - `grep -q 'budget_check_fun\|budget_fn' lib/glorbo/agent/dispatch.ex` succeeds (dep-injected)
    - `grep -q 'skills_resolved' lib/glorbo/agent/dispatch.ex` succeeds (passed to worker)
    - `grep -q 'agent_slug:\s*agent_slug\|agent_slug:' lib/glorbo/agent/dispatch.ex` succeeds (passed to worker)
    - `grep -q 'budget.hard_stop' lib/glorbo/agent/dispatch.ex` succeeds (audit event)
    - `grep -q 'agent.dispatch\|agent.complete' lib/glorbo/agent/dispatch.ex` succeeds (audit events)
    - `grep -q 'current-task.json' lib/glorbo/agent/dispatch.ex` succeeds (D-22)
    - `! grep -E 'api_key:.*audit\\|audit.*api_key' lib/glorbo/agent/dispatch.ex` (audit-payload defense — see comment marker for T-03-20)
    - `grep -q '# T-03-20' lib/glorbo/agent/dispatch.ex` succeeds (hard-coded marker comment near audit call sites)
    - `mix test test/glorbo/agent/dispatch_test.exs` exits 0; Test 2 verifies WorkerClient never called on budget_stop; Test 6 verifies no api_key in any audit payload
    - `grep -q '"/company"' lib/glorbo/agent/dispatch.ex` succeeds (outbox_root is literal in-container path, not host path — Blocker 2 fix)
    - Test 9 (dispatch_test.exs) asserts captured post_run_fun body has `outbox_root: "/company"` — guards SEC-05 usage chain
    - `mix credo --strict lib/glorbo/agent/dispatch.ex` exits 0
  </acceptance_criteria>
  <done>Dispatch pipeline runs budget → task → skills → key → state → worker with full dep injection; budget_stop short-circuits BEFORE worker call; api_key never in audit payload; current-task.json lifecycle correct; all 9 behaviors green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent.md frontmatter (Director-authored) → Definition.parse | Director-trusted but validated against whitelists |
| ~/.glorbo/config.md api_keys → LLM.Provider.resolve_key | Keys read fresh per dispatch; never persisted; warning on loose mode |
| skills/<name>.md → Skills.Resolver | Skill name regex prevents path traversal |
| Per-agent Task.Supervisor → Container/Worker | Task crash isolated per-agent (D-19) |
| audit_fun call payloads | Must NEVER contain api_key (T-03-20 guard via grep + Test 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Information Disclosure | Dispatch + LLM.Provider | mitigate | Provider resolves key fresh per dispatch; never persisted; passed only in WorkerClient body. Dispatch audit calls explicitly exclude api_key (Test 6 + grep guard). Loose-mode config warning |
| T-03-21 | Tampering | Definition + Provider | mitigate | Whitelist enforcement (provider/network/lifecycle/permission), no String.to_atom on input, single-model rejection at parse time |
| T-03-22 | Tampering | Skills.Resolver | mitigate | Skill name regex `~r/^[a-zA-Z0-9_-]+$/` rejects path traversal before File.read; Test 12 verifies |
| T-03-23 | Elevation of Privilege | Dispatch | mitigate | Pre-dispatch budget check is FIRST step; on stop, no WorkerClient call (Test 2 mock counter); pre-dispatch hard-stop pattern (D-27, Pattern 5) |
| T-03-24 | Repudiation | Agent.Server | mitigate | Every wake/dispatch/complete emits audit event with canonical key (per AUDIT_EVENTS.md); Director can replay agent timeline from audit log alone |
| T-03-25 | DoS | Agent.Server | mitigate | DynamicSupervisor (Plan 05) `max_restarts: 3, max_seconds: 60`; pathological wake loops isolated; current-task.json + pending_wakes give crash-recovery semantics |
| T-03-26 | Information Disclosure | Dispatch | mitigate | request_id is cryptographically random (`:crypto.strong_rand_bytes`) so a worker writing to `outbox/usage/<rid>.json` cannot collide with another agent's request_id (defense against worker→worker leakage in pathological multi-agent same-container futures) |
</threat_model>

<verification>
- `mix test test/glorbo/agent/definition_test.exs test/glorbo/agent/server_test.exs test/glorbo/agent/dispatch_test.exs test/glorbo/skills/resolver_test.exs test/glorbo/llm/provider_test.exs` — all green
- `mix credo --strict` — 0 issues vs Phase-2 baseline
- `mix format --check-formatted` clean
- `mix compile --warnings-as-errors` clean
- Negative-grep gates: no `String.to_atom` on user input; no `companies/` writes in provider.ex (api_key never lands in company dir); no api_key string in audit payloads
- Plan 04 dependencies satisfied: Agent.Server.wake/2 accepts `:director_approval` trigger; Definition.parse exposes `requires_approval` task-frontmatter handling left for Plan 04
- Plan 05 dependencies satisfied: Agent.Registry exposes `child_spec/1` for application supervisor; Agent.Server is `start_link/1`-able under DynamicSupervisor with via_tuple naming
</verification>

<success_criteria>
- All 11 must_haves.truths verified by acceptance criteria
- 6 modules (Definition, Registry, Server, Dispatch, Skills.Resolver, LLM.Provider) created with public APIs matching `<interfaces>` exactly
- Per-agent Task.Supervisor lives inside Agent.Server (D-19 crash-isolation)
- Pitfall 5 (concurrent wake race) mitigated via wake-queue state machine
- Pitfall 4 not directly hit (Ledger lives in Plan 02), but Dispatch's budget_check_fun → ETS hot path remains
- Audit-payload api_key defense: comment marker T-03-20 + Test 6 prove no api_key escapes Dispatch into audit
- LLM-04 (one-provider-one-model) enforced at agent.md PARSE time
- Skills resolution at dispatch time per D-34; missing skills are warnings not errors per D-35
- All audit events emitted by Agent.Server + Dispatch use canonical keys from AUDIT_EVENTS.md (Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-routing-kernel-permissions-budgets/03-03-SUMMARY.md`. Include:
- requirements-completed: [AGT-01 (partial — Agent.Server crash isolation via per-agent Task.Supervisor; full when Plan 05 wires DynamicSupervisor as Company child #6), AGT-02 (partial — Server accepts all 4 trigger types via wake/2; Plan 04 wires director-approval, Plan 05 wires inbox via Watcher), AGT-04 (partial — Skills.Resolver works in isolation; full when Dispatch is invoked end-to-end in Plan 05 integration test), LLM-03 (full — config.md api_keys read at dispatch, body-injected to worker, never env var), LLM-04 (full — single-model enforced at parse time)]
- key-decisions: per-agent Task.Supervisor inside Server; FIFO pending_wakes (OQ#4); per-agent container lifecycle (OQ#5); api_key audit defense comment marker
- patterns-established: full-dep-injection in Dispatch (Phase-1/2 convention extended); audit-payload allow-list pattern for sensitive data; whitelist-enforcement at parse time
- back-edits: NONE
</output>
