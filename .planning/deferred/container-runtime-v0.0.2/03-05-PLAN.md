---
phase: 03
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - lib/glorbo/security/network_policy.ex
  - lib/glorbo/security/acl_reconciler.ex
  - lib/glorbo/security/user_provisioner.ex
  - lib/glorbo/container/invocation.ex
  - lib/glorbo/container_manager.ex
  - lib/glorbo/company/supervisor.ex
  - lib/glorbo/application.ex
  - lib/glorbo/filesystem/watcher.ex
  - lib/glorbo/company/agents_loader.ex
  - containers/glorbo-runtime/Containerfile
  - containers/glorbo-runtime/entrypoint.sh
  - test/glorbo/security/network_policy_test.exs
  - test/glorbo/security/user_provisioner_test.exs
  - test/glorbo/container/invocation_test.exs
  - test/glorbo/company/supervisor_test.exs
  - test/glorbo/application_test.exs
  - test/integration/kernel_acl_denial_test.exs
  - test/integration/network_policy_test.exs
  - test/integration/agent_create_denial_test.exs
  - test/integration/seven_child_supervisor_test.exs
  - test/integration/approval_flow_test.exs
  - test/integration/budget_hard_stop_test.exs
  - test/test_helper.exs
autonomous: false
gap_closure: false
requirements: [AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, LLM-03]
locked_decisions:
  - "Network policy ladder: `none` → `--network none`; `open` → `--network slirp4netns`; `api-only` → `--network slirp4netns:allow_host_loopback=false --cap-add NET_ADMIN` + in-container nftables drop-everything-except-allowlist applied by entrypoint (Pitfall 1, Plan 01 OQ2 disposition)."
  - "NET_ADMIN cap-add is NARROWLY scoped to api-only ONLY. Negative tests in invocation_test.exs assert `--cap-add` is ABSENT for `none` and `open`. Phase-2 RT-04 invariant remains intact for the default network: none path."
  - "ACL reconciliation runs INSIDE the container via the entrypoint script (Pitfall 1 anti-pattern: never run setfacl from host on user-data dirs). Entrypoint reads `/run/glorbo-acl-recipe.json` (host writes it pre-podman-run via ACLReconciler.write_recipe/2) and applies setfacl commands."
  - "Per-company /etc/passwd overlay generated by UserProvisioner.write_passwd_fragment/2 to `~/.glorbo/runtime/passwd/<company>/passwd` + `group`; bind-mounted RO at container start via Invocation.build_argv/4's extra_volumes mechanism (Phase-2 Plan-04 back-edit reused)."
  - "Approval flow integration: Watcher diffs `projects/*/tasks/*.md` frontmatter on :modified events; on status: change, calls Glorbo.Approvals.Gate.handle_task_status_change/3 (Plan 04). The diff requires Watcher to maintain a per-file last-known-frontmatter cache in ETS; cache rebuilt on company init from current task files."
  - "Per-agent Agent.Server children started lazily — on first inbox event for the agent OR on first heartbeat schedule. AgentSupervisor (DynamicSupervisor) is created in Company.Supervisor; agents are not eagerly enumerated. This avoids spinning up a Server for every agent on company boot, but means on first wake there's a one-time `start_child` cost. Acceptable: the cost is microseconds and only on first ever wake."
  - "Seven-child supervisor: AuditLog, Filesystem.Watcher, BudgetTracker, Router, Scheduler, Approvals.Gate, AgentSupervisor (DynamicSupervisor) — acknowledged deviation from CONTEXT.md D-44's 6-child count. D-44 was authored before Plan 04 introduced Glorbo.Approvals.Gate as a sibling module; Gate is an independent child (not nested under Router) so Gate-crash and Router-crash are isolated per the AGT-01 crash-isolation goal. Start order matters (`:one_for_one` starts children in list order): AuditLog first (must start before any audit-emitter); BudgetTracker before Router + AgentSupervisor (agents + Router call BudgetTracker pre-dispatch); Gate before AgentSupervisor (Agent.Server's :director_approval wake path calls Gate)."
key-links:
  - from: lib/glorbo/security/network_policy.ex
    to: lib/glorbo/container/invocation.ex
    via: "Invocation.build_argv accepts :network_mode + :extra_caps keyword opts; NetworkPolicy returns the right combo per agent.network value"
  - from: lib/glorbo/security/acl_reconciler.ex
    to: lib/glorbo/security/acl_mapper.ex
    via: "Plan 01 ACLMapper.acl_entries/2 produces the entries Reconciler turns into setfacl commands"
  - from: lib/glorbo/security/user_provisioner.ex
    to: lib/glorbo/runtime/uid_allocator.ex
    via: "Plan 01 UidAllocator.allocate/3 produces UIDs UserProvisioner writes into passwd fragment"
  - from: lib/glorbo/company/supervisor.ex
    to: "all Phase 3 GenServers from Plans 02/03/04"
    via: "supervisor children list expanded from 2 to 7 (AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor)"
  - from: lib/glorbo/filesystem/watcher.ex
    to: lib/glorbo/company/router.ex
    via: "outbox/* file events → Router.route_outbox_file/2 (Plan 02)"
  - from: lib/glorbo/filesystem/watcher.ex
    to: lib/glorbo/approvals/gate.ex
    via: "projects/*/tasks/*.md modified events → diff frontmatter → Gate.handle_task_status_change/3 (Plan 04)"
  - from: lib/glorbo/filesystem/watcher.ex
    to: lib/glorbo/agent/server.ex
    via: "agents/*/inbox/* events → start (or lookup) Agent.Server + wake/2 with :inbox trigger (Plan 03)"
  - from: containers/glorbo-runtime/entrypoint.sh
    to: containers/glorbo-runtime/worker/main.py
    via: "entrypoint applies ACLs + nftables, then exec's uvicorn"

must_haves:
  truths:
    - "Glorbo.Security.NetworkPolicy.podman_flags/1 returns the exact argv contributions for none/api-only/open per locked_decisions (verified by unit test)"
    - "Glorbo.Container.Invocation.build_argv/4 accepts :network_mode + :extra_caps keyword opts; for `:none` produces argv WITHOUT `--cap-add`; for `:api_only` produces argv WITH `--cap-add NET_ADMIN`; for `:open` produces argv WITHOUT `--cap-add`"
    - "Glorbo.Security.UserProvisioner.write_passwd_fragment/2 writes a per-company passwd + group fragment containing root + every agent UID derived from UidAllocator (Plan 01); files are mode 0644 (read by container's PID 1 root)"
    - "Glorbo.Security.ACLReconciler.write_recipe/2 produces a JSON file at `<base>/runtime/sockets/<company>/glorbo-acl-recipe.json` consumed by entrypoint.sh; recipe contains the deterministic acl_entries from ACLMapper for each agent (Plan 01)"
    - "containers/glorbo-runtime/Containerfile installs `acl` and `nftables` packages; entrypoint.sh exists and (when given the recipe + nftables config) applies setfacl commands inside container then exec's `uvicorn worker.main:app --uds /run/agent.sock`"
    - "Glorbo.Company.Supervisor children list = 7 (was 2 in Phase 2): AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor; supervisor_test.exs asserts length(children) == 7 and enumerates all 7 child modules in order"
    - "Glorbo.Filesystem.Watcher dispatches outbox events to Router; inbox events to Agent.Server (start_child via DynamicSupervisor + wake); projects/*/tasks/*.md changes diffed via ETS cache → on status: change, Gate.handle_task_status_change called"
    - "Integration test `kernel_acl_denial_test.exs` (tagged :acl + :podman + :integration): spawn container for agent X with permission `projects:write:A`; podman exec into container as `glorbo-<co>-X`; attempt `touch /company/projects/B/smoke`; assert command exits non-zero AND stderr contains `Permission denied` AND the kernel-level audit (getfacl) shows the deny ACL — D-09 SC-4 contract"
    - "Integration test `network_policy_test.exs` (tagged :netavark + :podman + :integration): spawn api-only container; from inside, `curl -m 5 http://1.1.1.1` (or any non-allowlisted IP) FAILS; `curl -m 5 https://api.anthropic.com` (allowlisted) succeeds (or fails ONLY due to no API key, NOT due to network unreachable) — D-13 contract"
    - "Integration test `agent_create_denial_test.exs` (tagged :integration): write an outbox message from agent X targeting `agents/rogue/agent.md`; assert Router rejects (no agents/rogue/ dir created) AND `permission.denied` audit event present — D-43 contract"
    - "Integration test `seven_child_supervisor_test.exs` (tagged :inotify + :integration): start a Company.Supervisor; assert 7 children present + each child's PID (AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor); kill the Router PID; supervisor restarts ONLY Router; the OTHER SIX children's PIDs unchanged — AGT-01 crash isolation contract"
    - "Integration test `approval_flow_test.exs` (tagged :inotify + :integration): create task with `requires_approval: director`; agent picks task; Gate writes sentinel; Director (test) flips status: approved in task file; Watcher detects, calls Gate, agent wakes with :director_approval, dispatches; assert agent.complete audit event AND task removed from awaiting state — SEC-04 end-to-end"
    - "Integration test `budget_hard_stop_test.exs` (tagged :inotify + :integration): write usage JSON files to `agents/<slug>/outbox/usage/` that push the ledger over the monthly cap; Watcher routes them to BudgetTracker (Plan 02) which persists to the Budget ledger; then invoke Dispatch for that agent; assert BudgetTracker.check_budget returns {:stop, ...}, Dispatch returns {:error, :budget_stop}, WorkerClient.post_run was NEVER called, `budget.hard_stop` audit event emitted, inbox notice file written — SC-6 end-to-end contract"
  artifacts:
    - path: "lib/glorbo/security/network_policy.ex"
      provides: "Network policy → podman flags + nftables rule generator"
      exports: ["podman_flags/1", "nftables_rules/1", "allow_list/0"]
    - path: "lib/glorbo/security/acl_reconciler.ex"
      provides: "ACL recipe writer (host-side) + reconcile orchestrator"
      exports: ["write_recipe/3", "reconcile_company/2"]
    - path: "lib/glorbo/security/user_provisioner.ex"
      provides: "Per-company /etc/passwd + /etc/group fragment generator"
      exports: ["write_passwd_fragment/3"]
    - path: "lib/glorbo/company/agents_loader.ex"
      provides: "Loads all agent.md files for a company; produces map of slug → Agent.Definition"
      exports: ["load_company/2"]
    - path: "containers/glorbo-runtime/entrypoint.sh"
      provides: "Container PID-1 entrypoint: applies ACL recipe + nftables rules + runs `runuser` to launch uvicorn as agent UID"
      contains: "exec runuser"
    - path: "test/integration/kernel_acl_denial_test.exs"
      provides: "SC-4 kernel-observed denial proof"
      contains: "@moduletag :acl"
    - path: "test/integration/network_policy_test.exs"
      provides: "D-13 api-only egress rejection proof"
      contains: "@moduletag :netavark"
    - path: "test/integration/agent_create_denial_test.exs"
      provides: "AGT-05 D-43 Router rejection proof"
      contains: "permission.denied"
    - path: "test/integration/seven_child_supervisor_test.exs"
      provides: "AGT-01 crash isolation contract; supervisor children list = 7"
      contains: "length(children) == 7"
    - path: "test/integration/approval_flow_test.exs"
      provides: "SEC-04 end-to-end Watcher→Gate→Agent.Server flow"
      contains: "requires_approval: director"
    - path: "test/integration/budget_hard_stop_test.exs"
      provides: "SC-6 end-to-end budget hard-stop proof (usage file → Watcher → BudgetTracker → Ledger → Dispatch rejection)"
      contains: "budget.hard_stop"
  key_links:
    - from: "lib/glorbo/company/supervisor.ex"
      to: "All Plan 02/03/04 GenServers"
      via: "children list expanded from 2 to 7"
      pattern: "AuditLog.*Watcher.*BudgetTracker.*Router.*Scheduler.*Gate.*AgentSupervisor"
    - from: "lib/glorbo/filesystem/watcher.ex"
      to: "Plan 02 Router + Plan 03 Agent.Server + Plan 04 Gate"
      via: "path-prefix dispatch — extends Phase 2's Watcher routing table"
      pattern: "Glorbo\\.(Company\\.Router|Agent\\.Server|Approvals\\.Gate)"
    - from: "containers/glorbo-runtime/entrypoint.sh"
      to: "containers/glorbo-runtime/worker/main.py"
      via: "exec runuser -u glorbo-... -- uvicorn worker.main:app --uds /run/agent.sock"
      pattern: "exec runuser"

threat_model:
  trust_boundaries:
    - boundary: "Host (Elixir) → in-container entrypoint (recipe JSON)"
      description: "ACL + nftables recipe written to a bind-mounted volume by Elixir; entrypoint reads + applies. Recipe content is not Director-influenced — derived from agent.md by ACLMapper which Plan 01 hardened."
    - boundary: "Container PID-1 (root, runs setfacl + nftables) → uvicorn worker (per-agent Linux user)"
      description: "PID-1 drops privileges via runuser before exec'ing uvicorn. Per-agent ACLs apply to the worker process; PID-1 root never serves requests."
    - boundary: "Per-agent Linux user (uvicorn worker) → /company filesystem"
      description: "Kernel-enforced ACLs: write attempts outside grants → EACCES. SC-4 proof test"
    - boundary: "Per-agent Linux user (uvicorn worker) → outbound network"
      description: "api-only: nftables drops everything except allow-list CIDRs. Plan-01 OQ3 disposition: nftables + pre-resolved CIDR for v1.0; tinyproxy on UDS deferred to v1.1"
    - boundary: "Director's edits to projects/*/tasks/*.md → Watcher → Gate"
      description: "Director-trusted; Gate's terminal-state invariant + reconciliation handles edge cases"
  threats:
    - id: "T-03-40"
      category: "Elevation of Privilege"
      component: "containers/glorbo-runtime/entrypoint.sh"
      disposition: "mitigate"
      mitigation: "Entrypoint runs as PID-1 root (within rootless userns — host UID = Director). Drops to per-agent UID via `exec runuser -u <user> -- uvicorn ...`. Worker NEVER runs as root. Test asserts `podman exec <container> id -u` for the worker process returns the agent's UID, not 0"
    - id: "T-03-41"
      category: "Elevation of Privilege"
      component: "lib/glorbo/security/network_policy.ex"
      disposition: "mitigate"
      mitigation: "NET_ADMIN cap is added ONLY for api-only containers (locked_decisions OQ2). Negative test: `none` and `open` flags lists do NOT contain `--cap-add`. Plus Plan 02 Router still rejects egress-attempt messages from agents lacking permissions — defense in depth"
    - id: "T-03-42"
      category: "Information Disclosure"
      component: "lib/glorbo/security/acl_reconciler.ex"
      disposition: "mitigate"
      mitigation: "Recipe JSON is written to `runtime/sockets/<co>/` which is Director-owned (Phase 2 D-35); contains no secrets (just usernames + paths + modes). Mode 0640 sufficient. Test verifies file mode."
    - id: "T-03-43"
      category: "Tampering"
      component: "lib/glorbo/filesystem/watcher.ex"
      disposition: "mitigate"
      mitigation: "Watcher's per-file frontmatter cache is in-process ETS; Director's manual edit to a task file produces ONE diffed status change per modify event (debounced). Gate is idempotent against duplicate diffs (Plan 04 Test 5)"
    - id: "T-03-44"
      category: "DoS"
      component: "lib/glorbo/company/supervisor.ex"
      disposition: "mitigate"
      mitigation: "DynamicSupervisor for AgentSupervisor uses `max_restarts: 3, max_seconds: 60`. Per-agent crash storms isolated; pathological agent dies after 3 restarts in 60s; sibling agents unaffected. Per-child kill test in seven_child_supervisor_test.exs asserts only the killed child restarts"
    - id: "T-03-45"
      category: "Tampering"
      component: "lib/glorbo/security/user_provisioner.ex"
      disposition: "mitigate"
      mitigation: "passwd fragment generation is deterministic from UidAllocator output; written to `runtime/passwd/<co>/` (Director-owned, 0700 dir). No agent input. Bind-mounted RO into container."
    - id: "T-03-46"
      category: "Information Disclosure"
      component: "containers/glorbo-runtime/entrypoint.sh"
      disposition: "mitigate"
      mitigation: "nftables ruleset for api-only is `accept` for allow-list CIDRs, `drop` everything else. ICMP NOT allowed (no ping leaks). Logging disabled to prevent kernel log floods that could leak request metadata via host journal"
---

<objective>
Wave 2: wire everything together. This plan is the integration capstone — it adds the missing security modules (NetworkPolicy + ACLReconciler + UserProvisioner), extends the container Invocation with `:network_mode` + `:extra_caps`, expands `Glorbo.Company.Supervisor` from its Phase-2 2-child shape to the Phase-3 7-child shape (AuditLog + Watcher + BudgetTracker + Router + Scheduler + Gate + AgentSupervisor), upgrades the Watcher to dispatch by path-prefix into Router/Gate/Agent.Server, extends the runtime container image with `acl` + `nftables` packages + an entrypoint that applies the ACL recipe and nftables rules then exec's uvicorn as the agent's Linux user, and ships the SIX kernel-observed integration tests that Phase 2 deferred (plus SC-6 end-to-end budget hard-stop):

1. **kernel_acl_denial_test.exs** (D-09 SC-4): real container, real setfacl, real EACCES — kernel-observed denial.
2. **network_policy_test.exs** (D-13): real api-only container, real nftables, real curl — egress rejection.
3. **agent_create_denial_test.exs** (D-43): outbox-originated `agents/<X>/agent.md` write rejected by Router.
4. **seven_child_supervisor_test.exs** (AGT-01): 7-child supervisor + crash-isolation per child.
5. **approval_flow_test.exs** (SEC-04): end-to-end Watcher → Gate → Agent.Server.wake on status flip.
6. **budget_hard_stop_test.exs** (SC-6): end-to-end usage file → Watcher → BudgetTracker → Ledger → Dispatch rejection. No podman needed; tagged `:inotify`.

This plan has 4 tasks (close to the 2-3 budget — but the integration-test work is necessarily concentrated here, and splitting would put per-test fixtures across multiple plans).

`autonomous: false` because Task 4 contains `checkpoint:human-verify` for the integration tests requiring host setup (podman + nftables-in-container + /etc/subuid + setfacl). Auto-advance per workflow.auto_advance still applies; Director runs the rituals on a Fedora-like host and records results in SUMMARY.

Output: 4 new lib modules + 1 new agents loader + 4 modified lib files + Containerfile changes + entrypoint script + 6 integration tests + supervisor_test.exs + invocation_test.exs updates + test_helper.exs additions for new module tags (:acl + :netavark).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-VALIDATION.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-01-PLAN.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-02-PLAN.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-03-PLAN.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/03-04-PLAN.md
@.planning/phases/03-agents-routing-kernel-permissions-budgets/AUDIT_EVENTS.md
@CLAUDE.md
@DESIGN.md
@lib/glorbo/company/supervisor.ex
@lib/glorbo/container/invocation.ex
@lib/glorbo/container_manager.ex
@lib/glorbo/filesystem/watcher.ex
@lib/glorbo/application.ex
@lib/glorbo/runtime/uid_allocator.ex
@lib/glorbo/security/acl_mapper.ex
@containers/glorbo-runtime/Containerfile

<interfaces>
<!-- This is the integration plan — its outputs are the final wire-up. -->

NetworkPolicy:
```elixir
@type policy :: :none | :api_only | :open
@spec podman_flags(policy()) :: [String.t()]
# :none -> ["--network", "none"]
# :open -> ["--network", "slirp4netns"]
# :api_only -> ["--network", "slirp4netns:allow_host_loopback=false", "--cap-add", "NET_ADMIN"]
@spec nftables_rules(policy()) :: String.t()
# Returns nftables ruleset string entrypoint applies for api-only; "" for none/open
@spec allow_list() :: [String.t()]
# Static base list: Anthropic + OpenAI + Google + HuggingFace endpoint CIDRs (pre-resolved at compile time; Plan 01 OQ3)
```

UserProvisioner:
```elixir
@spec write_passwd_fragment(company :: String.t(), allocation :: UidAllocator.allocation(), opts :: keyword()) :: :ok
# opts: base, runtime_root (default ~/.glorbo/runtime/passwd)
# Writes <runtime_root>/<company>/{passwd, group}; mode 0644 each, dir 0700
```

ACLReconciler:
```elixir
@type recipe :: %{
        company: String.t(),
        agents: [%{slug: String.t(), uid: pos_integer(), entries: [ACLMapper.acl_entry()]}]
      }
@spec write_recipe(company :: String.t(), agents :: %{String.t() => Agent.Definition.t()}, opts :: keyword()) :: :ok
# Writes <base>/runtime/sockets/<company>/glorbo-acl-recipe.json (mode 0640)
@spec reconcile_company(company :: String.t(), opts :: keyword()) :: :ok | {:error, term()}
# Orchestrates: load agents → write_passwd_fragment → write_recipe → audit `acl.reconcile`. Called by Company.Supervisor on init AND on agent.md change events.
```

AgentsLoader:
```elixir
@spec load_company(company :: String.t(), opts :: keyword()) ::
        {:ok, %{String.t() => Agent.Definition.t()}} | {:error, term()}
# Walks <base>/companies/<co>/agents/*/agent.md, parses each via Agent.Definition.parse_file/1
```

Invocation extension:
```elixir
# Existing build_argv/4 gains opts:
#   :network_mode (default :none) — passes through NetworkPolicy.podman_flags
#   :extra_caps (default []) — auto-derived from network_mode; explicit override for tests
# When network_mode == :none, extra_caps MUST be [] (negative-test enforced)
```

Watcher extension:
```elixir
# Phase-2 Watcher's path-prefix routing extended:
#   agents/<n>/inbox/<file> → ensure Agent.Server started (DynamicSupervisor.start_child) then wake(via_tuple, :inbox)
#   agents/<n>/outbox/<file> → Router.route_outbox_file(via, abs_path)
#   agents/<n>/outbox/usage/<file> → BudgetTracker.record_usage_file(via, abs_path)
#   channels/<file> → no agent wake (Router does the @mention scan when it processes the outbox file that triggered the channel append)
#   projects/<p>/tasks/<file> on :modified → diff frontmatter via ETS cache → if status changed AND prior status was :awaiting_approval → Gate.handle_task_status_change
#   audit/<file> → no-op (handled by Phase 2)
#   else → reindex (Phase 2 default)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security modules — NetworkPolicy + UserProvisioner + ACLReconciler + AgentsLoader + Invocation extension</name>
  <files>
    lib/glorbo/security/network_policy.ex,
    lib/glorbo/security/user_provisioner.ex,
    lib/glorbo/security/acl_reconciler.ex,
    lib/glorbo/company/agents_loader.ex,
    lib/glorbo/container/invocation.ex,
    lib/glorbo/container_manager.ex,
    test/glorbo/security/network_policy_test.exs,
    test/glorbo/security/user_provisioner_test.exs,
    test/glorbo/container/invocation_test.exs
  </files>
  <read_first>
    lib/glorbo/security/acl_mapper.ex (Plan 01 — acl_entries/2),
    lib/glorbo/runtime/uid_allocator.ex (Plan 01 — allocate/3, subuid_base/1),
    lib/glorbo/agent/definition.ex (Plan 03 — Agent.Definition.parse_file/1),
    lib/glorbo/container/invocation.ex (Phase 2 Plan 04 back-edit — already accepts :extra_volumes; extend with :network_mode + :extra_caps),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-01..D-13 ACL + network),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pattern 4 + Pitfall 1; Code Examples §"Network policy argv construction" + §"setfacl inside container"),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-01-PLAN.md (locked_decisions OQ2 + OQ3)
  </read_first>
  <behavior>
    NetworkPolicy:
    - Test 1: `podman_flags(:none)` returns `["--network", "none"]`.
    - Test 2: `podman_flags(:open)` returns `["--network", "slirp4netns"]`.
    - Test 3: `podman_flags(:api_only)` returns `["--network", "slirp4netns:allow_host_loopback=false", "--cap-add", "NET_ADMIN"]` (OQ2 disposition).
    - Test 4: `allow_list/0` is non-empty list of CIDR/IP strings; contains at minimum addresses for `api.anthropic.com`, `api.openai.com`, `generativelanguage.googleapis.com`, `huggingface.co` (pre-resolved at compile time — recorded as a comment with the date the resolution was done so Director can refresh in v1.1).
    - Test 5: `nftables_rules(:api_only)` returns a non-empty string starting with `flush ruleset` and containing `accept ip daddr {` followed by allow_list contents and ending with a default `drop` rule. Returns `""` for `:none` and `:open`.

    UserProvisioner:
    - Test 6: `write_passwd_fragment("acme", %{ordinal: 0, uid_base: 524288, agents: %{"ceo" => 524288, "engineer" => 524289}, tombstoned: []}, opts)` writes `<runtime_root>/acme/passwd` containing root + glorbo-acme-ceo + glorbo-acme-engineer entries, and `<runtime_root>/acme/group` with parallel group entries.
    - Test 7: passwd fragment is mode 0644; parent dir is mode 0700; group fragment is mode 0644 (verified via File.stat).
    - Test 8: tombstoned agents are NOT included in fresh passwd (D-04 — tombstone is for UID-stability, not container-side resurrection).

    ACLReconciler:
    - Test 9: `write_recipe("acme", agents_map, opts)` writes a JSON file at `<sockets_root>/acme/glorbo-acl-recipe.json` (mode 0640) with shape `%{company: "acme", agents: [%{slug: "ceo", uid: 524288, entries: [...]}, ...]}`. Each agent's entries match `ACLMapper.acl_entries(username, perms)`.
    - Test 10: `reconcile_company("acme", opts)` — given injected `load_company_fun` returning two agents, `uid_allocate_fun` returning the allocation, `passwd_fun` capturing the call, `recipe_fun` capturing the call, `audit_fun` capturing — calls all four in sequence and emits `acl.reconcile` audit with `agent_count: 2`.

    AgentsLoader:
    - Test 11: `load_company("acme", base: tmpdir_with_2_agent_md_files)` returns `{:ok, %{"ceo" => def1, "engineer" => def2}}`.
    - Test 12: agent.md with parse error (e.g., invalid permission) → returns `{:error, {:agent_load_failed, slug, reason}}` (fail-fast — Director must fix before company starts).

    Invocation:
    - Test 13: `build_argv("acme", "ceo", :ephemeral, network_mode: :api_only)` argv contains `["--network", "slirp4netns:allow_host_loopback=false", "--cap-add", "NET_ADMIN"]`; argv canonical security flags (`--read-only, --userns keep-id, --tmpfs /tmp`) still present.
    - Test 14: `build_argv(..., network_mode: :none)` argv DOES NOT contain `--cap-add` (T-03-41 negative assertion); contains `--network none`.
    - Test 15: `build_argv(..., network_mode: :open)` argv DOES NOT contain `--cap-add`; contains `--network slirp4netns` (no allow_host_loopback option).
    - Test 16: Default `network_mode` (omitted) is `:none` — preserves Phase-2 RT-04 default.
    - Test 17 (regression): existing `:extra_volumes` opt continues to work (Phase 2 Plan 04 back-edit preserved).
    - Test 18 (passwd overlay): `build_argv(..., extra_volumes: [passwd_overlay])` produces argv with the passwd bind-mount pair.

    ContainerManager:
    - Test 19: `start_container/2` accepts `:network_mode` opt (default `:none`) and threads through to `Invocation.build_argv/4`.
  </behavior>
  <action>
    1. Create `lib/glorbo/security/network_policy.ex`:
       - `@allow_list` module attribute = pre-resolved CIDR list (use `dig +short api.anthropic.com api.openai.com generativelanguage.googleapis.com huggingface.co` outputs at write time; document the date in a `@allow_list_resolved_at "2026-04-16"` attribute so Director knows when to re-resolve).
       - `podman_flags/1`: pattern match on `:none | :open | :api_only` per Test 1-3.
       - `nftables_rules/1`: for `:api_only`, return a multiline string:
         ```
         flush ruleset
         table inet filter {
           chain output {
             type filter hook output priority 0; policy drop;
             meta l4proto { tcp, udp } ip daddr { <allow_list> } accept
             ip daddr 127.0.0.0/8 accept
             ct state established,related accept
           }
         }
         ```
         For `:none | :open` return `""`.

    2. Create `lib/glorbo/security/user_provisioner.ex`:
       - `write_passwd_fragment/3`: takes company + allocation + opts; mkdir_p `<runtime_root>/<company>` with mode 0700; build passwd contents with one root line + one line per non-tombstoned agent: `glorbo-<co>-<slug>:x:<uid>:<uid>::/company/agents/<slug>:/bin/sh`; build group similarly; `File.write!/3` with mode 0644; return :ok.

    3. Create `lib/glorbo/security/acl_reconciler.ex`:
       - `write_recipe/3`: build the recipe map; `Jason.encode!/2`; write to `<base>/runtime/sockets/<company>/glorbo-acl-recipe.json`; chmod 0640.
       - `reconcile_company/2`: orchestrate:
         a. `agents_map = load_company_fun.(company)`.
         b. `allocation = uid_allocate_fun.(company, Map.keys(agents_map))`.
         c. `passwd_fun.(company, allocation)`.
         d. `recipe_fun.(company, agents_map_with_uids)`.
         e. `audit_fun.(company, %{action: "acl.reconcile", agent_count: map_size(agents_map), rules_applied: <count from acl_entries>})`.
         f. Return `:ok`.

    4. Create `lib/glorbo/company/agents_loader.ex`:
       - `load_company/2`: `Path.wildcard("<base>/companies/<co>/agents/*/agent.md")`; for each, derive slug from path + `Agent.Definition.parse_file/1`; collect into map; return `{:ok, map}` or `{:error, {:agent_load_failed, slug, reason}}` on first failure.

    5. Modify `lib/glorbo/container/invocation.ex`:
       - Add `:network_mode` opt (default `:none`).
       - Compute `network_args = NetworkPolicy.podman_flags(network_mode)`.
       - Replace the hard-coded `["--network", "none"]` segment in `base_argv` with the dynamic `network_args`.
       - The existing `--cap-add` ABSENCE assertion in invocation_test.exs already exists (Phase 2); reinforce by adding Tests 13-16 above.

    6. Modify `lib/glorbo/container_manager.ex`: thread `:network_mode` through to `Invocation.build_argv/4` (analogous to Phase-2 Plan-04's `:extra_volumes` threading).

    7. Update `test/glorbo/container/invocation_test.exs` with Tests 13-18; create `test/glorbo/security/network_policy_test.exs` with Tests 1-5; create `test/glorbo/security/user_provisioner_test.exs` with Tests 6-8; ACLReconciler tests are pure-Elixir-injectable (no integration env needed) — write them as part of the same network_policy_test.exs OR a new acl_reconciler_test.exs (your choice).

    Why static CIDR not DNS-at-runtime: simplicity for v1.0; DNS-at-startup adds a moving part; researcher recommends "ships nftables + pre-resolved CIDR allow-list" (OQ3 disposition). Document the resolution date so Director can manually refresh in v1.1 if drift causes failures.
    Why nftables ruleset returned as a string (not applied here): Elixir doesn't call nftables — the entrypoint inside the container does (Pitfall 1 anti-pattern: never run setfacl/nftables from host). Elixir generates the recipe + the rules; entrypoint applies them.
    Why `:none` is default for build_argv: preserves Phase-2 RT-04 invariant. Plan 03's Dispatch passes `definition.network` (an atom from agent.md). For agents without explicit `network:`, Definition.parse defaults to `:none`.
  </action>
  <verify>
    <automated>mix test test/glorbo/security/network_policy_test.exs test/glorbo/security/user_provisioner_test.exs test/glorbo/container/invocation_test.exs --color --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def podman_flags' lib/glorbo/security/network_policy.ex` succeeds
    - `grep -q '@allow_list_resolved_at' lib/glorbo/security/network_policy.ex` succeeds (refresh date documented)
    - `grep -q 'NET_ADMIN' lib/glorbo/security/network_policy.ex` succeeds (api_only only)
    - `grep -q 'def write_passwd_fragment' lib/glorbo/security/user_provisioner.ex` succeeds
    - `grep -q 'def write_recipe\|def reconcile_company' lib/glorbo/security/acl_reconciler.ex` succeeds
    - `grep -q 'def load_company' lib/glorbo/company/agents_loader.ex` succeeds
    - `grep -q ':network_mode' lib/glorbo/container/invocation.ex` succeeds (extension applied)
    - `grep -q 'NetworkPolicy.podman_flags' lib/glorbo/container/invocation.ex` succeeds (delegates)
    - `! grep -E '\\["--network", "none"\\]' lib/glorbo/container/invocation.ex` (no hard-coded none — replaced by dynamic flags)
    - All test files exit 0; the negative `--cap-add` ABSENT assertion for `:none`/`:open` (Tests 14/15) is critical
    - `mix credo --strict` exits 0
  </acceptance_criteria>
  <done>NetworkPolicy + UserProvisioner + ACLReconciler + AgentsLoader created; Invocation extended with :network_mode (default :none preserves RT-04); cap-add NET_ADMIN narrowly scoped (negative tests for none/open); 19 behaviors green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Container image extension — Containerfile + entrypoint.sh applies ACLs + nftables, drops to agent UID</name>
  <files>
    containers/glorbo-runtime/Containerfile,
    containers/glorbo-runtime/entrypoint.sh,
    containers/glorbo-runtime/tests/test_entrypoint.py
  </files>
  <read_first>
    containers/glorbo-runtime/Containerfile (existing — Phase 2 — extend with acl + nftables packages),
    containers/glorbo-runtime/worker/main.py (uvicorn entrypoint command — entrypoint must exec into uvicorn),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pattern 4 + Pitfall 1; Code Examples §"setfacl inside container"),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-05 reconciliation timing; D-09 SC-4 contract)
  </read_first>
  <behavior>
    - Test 1: Containerfile `RUN` step installs `acl` + `nftables` + `util-linux` (for runuser) packages; `acl-2.x` and `nftables-1.x` versions confirmed via `apt list --installed` in image.
    - Test 2: entrypoint.sh exists; is executable (mode 0755); has shebang `#!/bin/sh` (POSIX, not bash-only — works across base images).
    - Test 3: Containerfile sets `ENTRYPOINT ["/usr/local/bin/glorbo-entrypoint.sh"]`; existing `CMD` for uvicorn is preserved AS the default args entrypoint exec's into.
    - Test 4 (entrypoint script flow): on a test rig (Python pytest spawning the script via subprocess in a docker/podman container OR a mocked filesystem):
       a. Reads `/run/glorbo-acl-recipe.json`; for each agent, applies `setfacl -b /company` + the entries from the recipe.
       b. Reads optional `/run/glorbo-nftables.rules`; if present, `nft -f /run/glorbo-nftables.rules` (silent if no rules — none/open path).
       c. Determines target user from env var `GLORBO_AGENT` (set by Invocation per agent) → `glorbo-${GLORBO_COMPANY}-${GLORBO_AGENT}`.
       d. `exec runuser -u <user> -- "$@"` (uvicorn args from CMD).
    - Test 5: Negative — entrypoint NEVER calls `setfacl --remove-all` on `/host` or anywhere outside `/company/`. Verified by grep on entrypoint.sh.
  </behavior>
  <action>
    1. Modify `containers/glorbo-runtime/Containerfile`:
       - Add `RUN apt-get update && apt-get install -y --no-install-recommends acl nftables util-linux && rm -rf /var/lib/apt/lists/*` (Ubuntu 24.04 base from Phase 2 D-11).
       - Add `COPY entrypoint.sh /usr/local/bin/glorbo-entrypoint.sh` and `RUN chmod 0755 /usr/local/bin/glorbo-entrypoint.sh`.
       - Add `ENTRYPOINT ["/usr/local/bin/glorbo-entrypoint.sh"]`.
       - Existing `CMD ["uvicorn", "worker.main:app", "--uds", "/run/agent.sock"]` is preserved (or the equivalent — read existing Containerfile first).

    2. Create `containers/glorbo-runtime/entrypoint.sh`:
       ```sh
       #!/bin/sh
       set -eu

       RECIPE=/run/glorbo-acl-recipe.json
       NFT_RULES=/run/glorbo-nftables.rules

       # Apply ACLs if recipe present
       if [ -f "$RECIPE" ]; then
         # Use jq if available; else POSIX shell parse minimally
         python3 -c "
       import json, subprocess, sys
       recipe = json.load(open('$RECIPE'))
       subprocess.run(['setfacl', '-b', '-R', '/company'], check=True)
       for ag in recipe['agents']:
         user = 'glorbo-' + recipe['company'] + '-' + ag['slug']
         for entry in ag['entries']:
           [_, mode, path] = entry  # entry is [username, mode, path]
           target = '/company/' + path
           if path != '' and (path == '.' or '/' in path or path == 'channels' or path == 'projects'):
             # Apply if path exists; ACLs over non-existent paths fail noisy
             import os
             if os.path.exists(target):
               subprocess.run(['setfacl', '-R', '-m', f'u:{user}:{mode}', target], check=True)
       "
       fi

       # Apply nftables rules if present (api-only)
       if [ -f "$NFT_RULES" ]; then
         nft -f "$NFT_RULES"
       fi

       # Drop to agent user (env vars set by Invocation)
       AGENT_USER="glorbo-${GLORBO_COMPANY}-${GLORBO_AGENT}"
       exec runuser -u "$AGENT_USER" -- "$@"
       ```
       Note: `mode` must be lowercase letters (rwx, rx, r) or "---" — entrypoint translates `:rwx` atom-style strings to setfacl chars.

    3. Write `containers/glorbo-runtime/tests/test_entrypoint.py` covering Tests 1-5. Some tests are pure pytest (Test 2 — file mode + shebang; Test 5 — grep negative), others are Containerfile lint via `podman build --pull=false -f Containerfile -t test-image .` and `podman run test-image apt list --installed | grep -E 'acl|nftables'` for Test 1. Tag `:podman` so they only run on hosts with podman available.

    4. Build and tag the image locally as `ghcr.io/foobarto/glorbo-runtime:v0.2.0-rc1` (semver bump from Phase-2 v0.1.0); document in PLAN frontmatter that Plan 05 ships an image bump that must be pushed to ghcr.io as part of the integration test (Director ritual in checkpoint task 4).

    Why JSON parsing in Python (not pure shell): the recipe is a structured JSON; shell + jq would also work but Python is already present in the runtime image (Phase 2 D-12). Avoids adding jq dependency.
    Why `runuser` (not `su` or `sudo`): runuser doesn't require a TTY, doesn't authenticate (faster), and is in util-linux — already installed on Ubuntu 24.04 base.
    Why `set -eu`: fail fast on any error (set -e) + treat unset vars as errors (set -u — catches typos in env-var names).
    Why entrypoint applies ACLs INSIDE container (not Elixir from outside): Pitfall 1 anti-pattern — `setfacl` from host on `~/.glorbo/companies/...` could break Director's own access (mask interactions). Inside container, the userns view is isolated.
  </action>
  <verify>
    <automated>cd containers/glorbo-runtime && python -m pytest tests/test_entrypoint.py -x --tb=short 2>&1 | head -50; echo "---" ; podman build --pull=false -f Containerfile -t glorbo-runtime-test . 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'apt-get install.*acl.*nftables' containers/glorbo-runtime/Containerfile` succeeds
    - `grep -q 'ENTRYPOINT.*glorbo-entrypoint' containers/glorbo-runtime/Containerfile` succeeds
    - `test -x containers/glorbo-runtime/entrypoint.sh` succeeds (executable bit set)
    - `head -1 containers/glorbo-runtime/entrypoint.sh | grep -q '^#!/bin/sh$'` succeeds
    - `grep -q 'setfacl -b -R /company' containers/glorbo-runtime/entrypoint.sh` succeeds (D-05 idempotent baseline)
    - `grep -q 'exec runuser' containers/glorbo-runtime/entrypoint.sh` succeeds (T-03-40 — drops to agent UID)
    - `! grep -E 'setfacl.*[^/]*/host' containers/glorbo-runtime/entrypoint.sh` (T-03-45 — no host-side setfacl from inside script)
    - pytest tests pass (where podman is available); image build succeeds locally
  </acceptance_criteria>
  <done>Containerfile installs acl + nftables + util-linux; entrypoint.sh applies ACL recipe + nftables rules + exec's uvicorn as agent UID via runuser; image built locally. Push to ghcr.io is Director ritual in Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Watcher path-prefix dispatch + Company.Supervisor 7-child shape + Application.Registry wiring</name>
  <files>
    lib/glorbo/filesystem/watcher.ex,
    lib/glorbo/company/supervisor.ex,
    lib/glorbo/application.ex,
    test/glorbo/filesystem/watcher_test.exs,
    test/glorbo/company/supervisor_test.exs,
    test/glorbo/application_test.exs,
    test/test_helper.exs
  </files>
  <read_first>
    lib/glorbo/filesystem/watcher.ex (Phase 2 Plan 04 — current path-prefix routing dispatches reindex; extend with Router/Gate/Agent.Server),
    lib/glorbo/company/supervisor.ex (Phase 2 Plan 04 — current 2-child shape; extend to 7),
    lib/glorbo/application.ex (Phase 1/2 Application — add Glorbo.Agent.Registry as sibling child),
    test/glorbo/application_test.exs (Phase 2 Plan 04 — asserts 2 children; update assertion),
    test/glorbo/filesystem/watcher_test.exs (Phase 2 Plan 04 — current event-routing tests; extend with Router/Gate/Agent.Server dispatch tests),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-44, D-45),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (Pattern 1 §"Six-child per-company supervisor" — note locked_decisions deviation: Plan 05 commits to SEVEN children per Gate-as-sibling rationale)
  </read_first>
  <behavior>
    Watcher (extending Phase-2 module):
    - Test 1 (regression): Existing inbox/outbox/audit/channels/else routing from Phase 2 still dispatches to Reindex.mark_dirty/2 for non-special paths (no regression).
    - Test 2 (outbox dispatch): a new file at `agents/<X>/outbox/<f>.md` triggers `Router.route_outbox_file(via, abs_path)` (verified via injected `router_fun` capturing).
    - Test 3 (outbox/usage dispatch): a new file at `agents/<X>/outbox/usage/<r>.json` triggers `BudgetTracker.record_usage_file(via, abs_path)` (NOT Router — the path-prefix is a sub-prefix of outbox).
    - Test 4 (inbox dispatch): a new file at `agents/<X>/inbox/<f>.md` triggers a `start_or_lookup_agent_fun.(company, "X")` (which DynamicSupervisor.start_child or Registry.lookup) followed by `Agent.Server.wake(via, :inbox)`.
    - Test 5 (task status diff): a `:modified` event at `projects/<p>/tasks/<t>.md` triggers a frontmatter diff via the Watcher's per-file ETS cache; if status changed AND prior was `:awaiting_approval` AND new is `:approved` or `:denied`, calls `Gate.handle_task_status_change(via, abs_path, new_status)`. If unchanged or first observation (no prior cache entry), updates cache only.
    - Test 6 (channel append): a `:modified` event on `channels/<c>.md` does NOT trigger any agent wake directly — Router handles @mention scan when it processes the originating outbox file.

    Company.Supervisor:
    - Test 7: children list = exactly 7 specs in this order: AuditLog, Filesystem.Watcher, BudgetTracker, Router, Scheduler, Approvals.Gate, AgentSupervisor (DynamicSupervisor).
    - Test 8: each child starts successfully on `start_supervised!/1` (using `Glorbo.DataCase` for Repo + tmpdir for base).
    - Test 9 (crash isolation): kill Router PID via `Process.exit(pid, :kill)`; supervisor restarts ONLY Router; other 6 PIDs unchanged. (For each of the 7 children, Test 9 has a sub-case.)
    - Test 10 (DynamicSupervisor max_restarts): AgentSupervisor uses `max_restarts: 3, max_seconds: 60` (verified via `Supervisor.which_children/1` → child_spec).

    Application:
    - Test 11: `Glorbo.Application.start/2` includes `Glorbo.Agent.Registry` as a child of the application supervisor (singleton — one Registry process for the whole BEAM node, used by every Company.Supervisor's per-agent server names).
    - Test 12 (regression): existing Phase 1/2 Application children unchanged in count + order; Registry inserted at a specific position (between Repo and CompanySupervisor — adjust based on existing Application code).
  </behavior>
  <action>
    1. Modify `lib/glorbo/application.ex` to insert `Glorbo.Agent.Registry` into the children list. Position: AFTER `Glorbo.Repo` (Registry doesn't need DB) and BEFORE `Glorbo.CompanySupervisor` (CompanySupervisor's children may use the Registry on init). Update `test/glorbo/application_test.exs` if it asserts a specific children count.

    2. Modify `lib/glorbo/company/supervisor.ex` from 2-child Phase-2 shape to 7-child Phase-3 shape. Final children list (locked per frontmatter locked_decisions — deviation from CONTEXT D-44's 6-child count is acknowledged there; Gate is a sibling child, not nested under Router, to preserve AGT-01 crash-isolation at finer grain):

       ```elixir
       children = [
         {Glorbo.Company.AuditLog,      [name: child_name(company, :audit_log), company: company, base: base]},
         {Glorbo.Filesystem.Watcher,    [name: child_name(company, :file_watcher), company: company, base: base,
                                          router: child_name(company, :router),
                                          gate: child_name(company, :gate),
                                          budget_tracker: child_name(company, :budget),
                                          dynamic_sup: child_name(company, :agent_sup)]},
         {Glorbo.Company.BudgetTracker, [name: child_name(company, :budget), company: company, base: base]},
         {Glorbo.Company.Router,        [name: child_name(company, :router), company: company, base: base,
                                          audit_fun: &Glorbo.Company.AuditLog.append/2,
                                          agents_fun: &Glorbo.Company.AgentsLoader.load_company/2]},
         {Glorbo.Company.Scheduler,     [name: child_name(company, :scheduler), company: company, base: base]},
         {Glorbo.Approvals.Gate,        [name: child_name(company, :gate), company: company, base: base]},
         {DynamicSupervisor, strategy: :one_for_one, max_restarts: 3, max_seconds: 60,
                              name: child_name(company, :agent_sup)}
       ]
       ```

       Order rationale (`:one_for_one` starts children in list order):
       - AuditLog FIRST — every subsequent child may emit audit events on init.
       - Watcher SECOND — must be up to observe BudgetTracker/Router/Gate initialisation events (and so Router/Gate names are already registered when Watcher routes events; Watcher tolerates `:noproc` responses until siblings start).
       - BudgetTracker THIRD — Router (next) and AgentSupervisor (last) both call BudgetTracker.check_budget at routing / dispatch time.
       - Router FOURTH — Watcher routes outbox events here.
       - Scheduler FIFTH — independent of Router/Gate; order not load-bearing but listed here so Gate is the last pre-AgentSupervisor sibling.
       - Gate SIXTH — Watcher routes task-status-change events here; Agent.Server's :director_approval path calls Gate.
       - AgentSupervisor LAST — dynamic children (Agent.Server instances) lazy-started on first wake; by then all six siblings are up.

    3. Modify `lib/glorbo/filesystem/watcher.ex` to extend the path-prefix dispatch from the Phase-2 logging-only behavior to actual function calls:
       - Add new state fields: `router, gate, budget_tracker, dynamic_sup` (atoms/names from Supervisor opts).
       - Maintain ETS table for task-frontmatter cache: `{task_path → status}`.
       - Path-prefix routing extended:
         - `agents/<X>/outbox/usage/...` → `BudgetTracker.record_usage_file(state.budget_tracker, abs_path)`.
         - `agents/<X>/outbox/...` (NOT usage subdir) → `Router.route_outbox_file(state.router, abs_path)`.
         - `agents/<X>/inbox/...` → start-or-lookup Agent.Server via Registry; if not started, `DynamicSupervisor.start_child(state.dynamic_sup, agent_child_spec(state.company, X))`; then `Agent.Server.wake(via, :inbox)`.
         - `projects/<p>/tasks/<t>.md` (modified): parse frontmatter; lookup ETS cache; if status changed AND prior was :awaiting_approval AND new is :approved or :denied → `Gate.handle_task_status_change(state.gate, abs_path, new_status)`. Update cache.
         - Else → existing `Reindex.mark_dirty/2` (Phase 2).

    4. Update `test/glorbo/filesystem/watcher_test.exs` adding Tests 1-6 (use injected `router_fun, gate_fun, budget_fun, agent_wake_fun` per established Phase-1/2 dep-injection pattern). Update `test/glorbo/company/supervisor_test.exs` with Tests 7-10 (start_supervised! with all deps; assert children list shape = 7 entries in the documented order; test crash isolation per child with a separate sub-case per child). Update `test/glorbo/application_test.exs` for Tests 11-12.

    5. Update `test/test_helper.exs` to register new module tags: `:acl`, `:netavark` — auto-exclude when `setfacl` or `nft` are absent from PATH (extend Phase-2's `:inotify` auto-exclusion pattern).

    Why Gate as separate sibling not nested under Router: preserves "every state-bearing concern owns its supervision child" pattern — Gate crash does NOT restart Router, and vice versa. This is the finer grain of AGT-01 crash isolation. The count drifts from CONTEXT.md D-44's literal 6 to 7; locked_decisions documents the deviation.
    Why Watcher takes router/gate/budget_tracker/dynamic_sup names as opts: dep-injection at the supervisor level — supervisor knows the names; Watcher receives them; tests can inject mock names backed by capturing GenServers.
    Why Registry as Application child not Company child: a Registry is a singleton lookup service for the whole node; Company.Supervisor children register INTO it via via_tuple. If Company restarts, registrations clean up via process death.
    Why ETS for task-frontmatter cache (not Repo): hot path (every modified event); task-status diff is process-local; rebuilding from disk on Watcher restart is cheap (walk projects dir on init).
  </action>
  <verify>
    <automated>mix test test/glorbo/filesystem/watcher_test.exs test/glorbo/company/supervisor_test.exs test/glorbo/application_test.exs --include inotify --color --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'AuditLog.*Watcher.*BudgetTracker.*Router.*Scheduler.*Gate.*AgentSupervisor\|BudgetTracker.*Router.*Scheduler.*Gate' lib/glorbo/company/supervisor.ex` succeeds (7-child list present)
    - Children list contains exactly 7 specs (counted via test): AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor (DynamicSupervisor)
    - `grep -q 'max_restarts: 3' lib/glorbo/company/supervisor.ex` succeeds (DynamicSupervisor restart bound)
    - `grep -q 'Glorbo.Agent.Registry' lib/glorbo/application.ex` succeeds (Registry in App children)
    - `grep -q 'Router.route_outbox_file\|Gate.handle_task_status_change\|BudgetTracker.record_usage_file\|Agent.Server.wake' lib/glorbo/filesystem/watcher.ex` succeeds (Watcher dispatch extended)
    - `grep -q 'outbox/usage' lib/glorbo/filesystem/watcher.ex` succeeds (usage path-prefix discrimination)
    - `mix test test/glorbo/filesystem/watcher_test.exs test/glorbo/company/supervisor_test.exs test/glorbo/application_test.exs` passes (with :inotify if installed)
    - `mix credo --strict` exits 0
  </acceptance_criteria>
  <done>Watcher dispatches outbox/inbox/usage/task-status events to the right Plan-02/03/04 module; Company.Supervisor expanded to 7-child shape with crash isolation; Glorbo.Agent.Registry added to Application children; assertion in application_test.exs updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Six kernel-observed integration tests (incl. SC-6 budget hard-stop) + Director ritual on host</name>
  <files>
    test/integration/kernel_acl_denial_test.exs,
    test/integration/network_policy_test.exs,
    test/integration/agent_create_denial_test.exs,
    test/integration/seven_child_supervisor_test.exs,
    test/integration/approval_flow_test.exs,
    test/integration/budget_hard_stop_test.exs
  </files>
  <read_first>
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-CONTEXT.md (D-09 SC-4, D-13 api-only egress, D-43 AGT-05, D-44 supervisor shape, SC-6 budget hard-stop),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-RESEARCH.md (§Validation Architecture — Wave 0 Gaps; §Phase Requirements → Test Map),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-02-PLAN.md (BudgetTracker.record_usage_file + check_budget contract),
    .planning/phases/03-agents-routing-kernel-permissions-budgets/03-03-PLAN.md (Dispatch.run budget_stop path),
    test/integration/airplane_mode_test.exs (Phase 2 Plan 04 — :airplane gating pattern; mirror for :acl, :netavark),
    test/test_helper.exs (auto-exclusion pattern for missing host deps)
  </read_first>
  <what-built>
    Six integration tests covering the KERNEL-OBSERVED and END-TO-END behaviors that Phase 3 promises:

    1. **kernel_acl_denial_test.exs** (D-09 SC-4): Spawn a real `glorbo-runtime` container for an agent with `permissions: [projects:write:A]` only; from inside via `podman exec` as the agent's Linux user, attempt `touch /company/projects/B/smoke`; assert the command fails with `EACCES` (exit code != 0, stderr contains "Permission denied"); also `getfacl /company/projects/B` shows the deny ACL for the agent. THIS IS THE PHASE-3 KERNEL CONTRACT — not just an Elixir-layer rejection.

    2. **network_policy_test.exs** (D-13): Spawn an `api_only` container; from inside, `curl -m 5 -s http://1.1.1.1` (non-allowlisted) MUST fail (exit code != 0); `curl -m 5 -s -o /dev/null -w '%{http_code}' https://api.anthropic.com` (allowlisted) MUST connect (returns SOME http code, not 0/connection refused). For airplane-mode safety, run with `--include netavark` and skip if no internet.

    3. **agent_create_denial_test.exs** (AGT-05 D-43): Start a Company.Supervisor; create an outbox file from agent X targeting `agents/rogue/agent.md`; trigger Router.route_outbox_file; assert (a) `agents/rogue/agent.md` does NOT exist, (b) source moved to `agents/X/history/<f>.rejected.md`, (c) `permission.denied` audit event present in `audit/YYYY-MM.jsonl` with `missing: "agents:create:rogue"`.

    4. **seven_child_supervisor_test.exs** (AGT-01): Start Company.Supervisor; assert 7 children present (post-Plan-04 reality; locked_decisions documents deviation from CONTEXT D-44's "6"); for each of the 7 children (AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor), kill its PID and assert ONLY that child restarts; sibling PIDs unchanged.

    5. **approval_flow_test.exs** (SEC-04 end-to-end): Create a task file with `requires_approval: director`; start Company.Supervisor + Agent.Server; trigger an inbox event for the agent (write a message); agent picks task, Dispatch sees requires_approval, calls Gate.request_approval; assert sentinel exists; Director (test) edits task file frontmatter to `status: approved` + `approved_by: director`; Watcher fires modified event (via inotify on real FS — `:inotify` tagged); Watcher calls Gate.handle_task_status_change; Gate calls Agent.Server.wake(:director_approval); agent dispatches; assert `agent.complete` audit event AND `tasks_approval_state` row updated to "approved".

    6. **budget_hard_stop_test.exs** (SC-6 end-to-end): No container needed (`:inotify` tagged only). Start Company.Supervisor. For agent X with `budget_usd: 0.10` (10 cents monthly cap): directly write a usage JSON file to `<base>/companies/<co>/agents/X/outbox/usage/<rid>.json` with `cost_usd_cents: 15` (pushes over cap on first report). Watcher routes to `BudgetTracker.record_usage_file/2`; BudgetTracker upserts the Budget ledger row. Wait for the DB write (`eventually` helper — poll for 2s). Then invoke `Dispatch.run(company, "X", :inbox, opts)` (all real deps from the supervisor, NO mocked post_run_fun — but inject a `post_run_fun` spy that MUST NOT be called, as `check_budget` should short-circuit before it). Assert:
       - `Dispatch.run` returns `{:error, :budget_stop}`;
       - `post_run_fun` spy was called 0 times;
       - audit log contains `budget.hard_stop` event with `agent: "X"`, `used_cents: 15`, `cap_cents: 10`;
       - inbox notice file `agents/X/inbox/<timestamp>-budget-stop.md` exists and mentions the cap amount.
       This covers the FULL end-to-end chain `worker-writes-usage-file → Watcher → BudgetTracker → Ledger → check_budget → Dispatch-rejection → audit + notice` in one test, satisfying SC-6 without requiring podman (the "worker" part is simulated by a direct file write, which is exactly what the real worker does from inside the container — the chain downstream of the file write is identical).
  </what-built>
  <how-to-verify>
    **Director rituals (run on Fedora-like host with all deps installed):**

    Pre-flight:
    ```bash
    # Verify host deps
    which setfacl getfacl nft podman ollama inotifywait 2>&1 | sort
    cat /etc/subuid | grep -E "^$USER:"   # MUST exist; if not: sudo usermod --add-subuids 524288-589823 $USER
    podman --version  # ≥ 5.x
    nft --version     # ≥ 1.x

    # Pull/build the runtime image with Phase-3 entrypoint
    cd containers/glorbo-runtime
    podman build -t ghcr.io/foobarto/glorbo-runtime:v0.2.0-rc1 .
    podman tag ghcr.io/foobarto/glorbo-runtime:v0.2.0-rc1 ghcr.io/foobarto/glorbo-runtime:latest
    # Optional: podman push ghcr.io/foobarto/glorbo-runtime:v0.2.0-rc1   # requires ghcr login
    ```

    Run the 6 tests:
    ```bash
    cd /var/home/user/Documents/glorbo
    mix test test/integration/kernel_acl_denial_test.exs    --include acl --include podman
    mix test test/integration/network_policy_test.exs       --include netavark --include podman
    mix test test/integration/agent_create_denial_test.exs  --include integration
    mix test test/integration/seven_child_supervisor_test.exs --include inotify
    mix test test/integration/approval_flow_test.exs        --include inotify
    mix test test/integration/budget_hard_stop_test.exs     --include inotify
    ```

    Expected:
    - Test 1: passes; assert message includes `EACCES` and the deny-ACL output from getfacl.
    - Test 2: passes (or skips if no internet); api.anthropic.com connectivity test should NOT be reachable from a `:none` container (negative control).
    - Test 3: passes; audit log contains `permission.denied`.
    - Test 4: passes; for each child kill, only that child's PID changes (7 sub-cases).
    - Test 5: passes; full Watcher → Gate → Agent.Server flow exercised on real inotify.
    - Test 6: passes; budget hard-stop chain exercised on real inotify (no podman needed).

    Record results below in this checkpoint:

    | Test | Pass/Fail | Notes |
    |------|-----------|-------|
    | kernel_acl_denial_test | _<pending>_ | _<paste first 10 lines of failure or "all green">_ |
    | network_policy_test | _<pending>_ | _<note: api_only egress to non-allowlisted IP rejected>_ |
    | agent_create_denial_test | _<pending>_ | _<note: agents/rogue/ dir does NOT exist after route attempt>_ |
    | seven_child_supervisor_test | _<pending>_ | _<note: 7 children total; per-child kill restarts only that child>_ |
    | approval_flow_test | _<pending>_ | _<note: end-to-end Watcher→Gate→Agent.Server.wake on status flip>_ |
    | budget_hard_stop_test | _<pending>_ | _<note: usage file → Watcher → BudgetTracker → Dispatch rejection>_ |

    Document any deviations or unexpected behaviors. If a test fails, capture full output to `<.planning/phases/03-.../03-05-FAILURE-NOTES.md>` and orchestrator's `/gsd-verify-work` will pick it up.
  </how-to-verify>
  <action>
    Write the 6 integration test files. Each test:
    - Starts a real Glorbo.Company.Supervisor (NOT mocked) with a tmp `~/.glorbo-test-<rand>` base.
    - Tags appropriately: `@moduletag :integration` plus one of `:acl`, `:netavark`, `:inotify`, `:podman`.
    - Uses `setup_all` to build/verify the runtime image is available (tests 1-2 only).
    - For Tests 1-2 (real container): use `Glorbo.ContainerManager.start_container/2` with a known agent.md fixture; `Glorbo.Container.WorkerClient.post_run` is NOT used here — the test is checking kernel/network behavior, not LLM behavior. Use `System.cmd("podman", ["exec", container_id, "/bin/sh", "-c", cmd])` for the assertion commands.
    - For Test 3 (Router rejection): no container needed; use Router directly with a real outbox file and capture-audit_fun.
    - For Test 4 (supervisor crash isolation): pure Elixir; no container; use start_supervised + Process.exit + Process.alive? — 7 sub-cases, one per child.
    - For Test 5 (approval flow): real inotify (`:inotify` tag); real Watcher; real Gate; real Agent.Server with mocked Dispatch.post_run_fun (because actual dispatch needs container; we want to verify the wake-on-flip BEHAVIOR, not the LLM call).
    - For Test 6 (budget hard-stop): real inotify (`:inotify` tag); real Watcher → BudgetTracker → Ledger chain via DataCase SQLite; direct file write simulates the worker's usage emission; Dispatch.run invoked with `post_run_fun: spy_that_fails_if_called`. No podman.

    Write `test/integration/seven_child_supervisor_test.exs` first (no host deps beyond inotify) — this is the AGT-01 contract test; pin children list to 7 explicitly and enumerate each expected module name so future drift is caught.
    Write `test/integration/budget_hard_stop_test.exs` second — also no host deps beyond inotify; exercises the SC-6 chain that no other test covers end-to-end.

    Add new module tags to `test/test_helper.exs`:
    ```elixir
    # Auto-exclude :acl when setfacl absent
    if System.find_executable("setfacl") == nil, do: ExUnit.configure(exclude: [:acl])
    # Auto-exclude :netavark when nft absent or no internet
    if System.find_executable("nft") == nil, do: ExUnit.configure(exclude: [:netavark])
    # :podman tag already handled in Phase 2
    ```

    The checkpoint covers the Director's host execution AND verification of test pass results. Auto-advance: yes (workflow.auto_advance=true), so the planner-emitted artifacts include the test files; the Director verifies on host.
  </action>
  <verify>
    Two-phase verification:
    Phase A (auto, runs in CI / local without host deps): `mix test test/integration/seven_child_supervisor_test.exs test/integration/budget_hard_stop_test.exs --include inotify --include integration` AND `mix test test/integration/agent_create_denial_test.exs --include integration` — these don't need podman/nft/setfacl. Should pass.
    Phase B (manual, Director on Fedora-like host): runs the 6 tests with all `--include` flags as listed in `<how-to-verify>`. Records results in this checkpoint's table.

    <automated>mix test test/integration/seven_child_supervisor_test.exs test/integration/budget_hard_stop_test.exs test/integration/agent_create_denial_test.exs --include integration --include inotify</automated>
  </verify>
  <acceptance_criteria>
    - All 6 integration test files exist
    - `grep -q '@moduletag :acl' test/integration/kernel_acl_denial_test.exs` succeeds
    - `grep -q '@moduletag :netavark' test/integration/network_policy_test.exs` succeeds
    - `grep -q 'EACCES\|Permission denied' test/integration/kernel_acl_denial_test.exs` succeeds (D-09 SC-4 contract)
    - `grep -q 'agents/rogue' test/integration/agent_create_denial_test.exs` succeeds
    - `grep -qE 'length\(children\) == 7|Enum\.count\(children\) == 7' test/integration/seven_child_supervisor_test.exs` succeeds (locked-decision deviation from CONTEXT D-44 = 6 → reality = 7 with Gate)
    - `grep -q 'requires_approval: director\|approved_by: director' test/integration/approval_flow_test.exs` succeeds
    - `grep -q 'budget.hard_stop' test/integration/budget_hard_stop_test.exs` succeeds (SC-6 contract)
    - `grep -q ':budget_stop' test/integration/budget_hard_stop_test.exs` succeeds (Dispatch return value)
    - Phase A automated tests pass; Phase B Director ritual table filled in below
  </acceptance_criteria>
  <resume-signal>Type "approved" once 6 tests have been run on a Fedora-like host AND results table above is filled in. If any test fails, capture output to `03-05-FAILURE-NOTES.md` and let `/gsd-verify-work` triage. If `workflow.auto_advance=true`, the checkpoint is auto-approved at write-time; the Director still must run the rituals + record results post-merge.</resume-signal>
  <done>6 integration tests exist; Phase A subset (no host deps beyond inotify: tests 3, 4, 6) auto-passes; Director ritual recorded for the kernel/podman/nftables-dependent tests. Phase 3 success criteria met when all 6 are green on host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host (Elixir) → in-container entrypoint (recipe JSON) | ACL recipe = derived from agent.md by hardened ACLMapper (Plan 01); not Director-input |
| Container PID-1 (root) → uvicorn worker (per-agent UID) | runuser drops privileges before exec'ing uvicorn |
| Per-agent Linux user → /company filesystem | Kernel-enforced ACLs; SC-4 proof test |
| Per-agent Linux user → outbound network | api-only: nftables drop-everything-except-allowlist; verified by network_policy_test |
| Director's edits to projects/*/tasks/*.md → Watcher → Gate | Director-trusted; Gate idempotent (Plan 04) |
| Watcher's per-file frontmatter ETS cache | Process-local; rebuilt on Watcher restart from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-40 | Elevation of Privilege | entrypoint.sh | mitigate | runuser drops to per-agent UID before exec'ing uvicorn; verified by `podman exec id -u` test |
| T-03-41 | Elevation of Privilege | NetworkPolicy | mitigate | NET_ADMIN ONLY for api-only; negative tests for none/open (RT-04 invariant intact for default path) |
| T-03-42 | Information Disclosure | ACLReconciler | mitigate | Recipe in Director-owned dir (mode 0640); no secrets; allow-list contents only |
| T-03-43 | Tampering | Watcher | mitigate | Per-file ETS cache; Director status flip → ONE diffed event (debounced); Gate idempotent (Plan 04 Test 5) |
| T-03-44 | DoS | Company.Supervisor | mitigate | DynamicSupervisor `max_restarts: 3, max_seconds: 60`; per-agent crash storms isolated; seven_child_supervisor_test asserts per-child isolation |
| T-03-45 | Tampering | UserProvisioner | mitigate | Deterministic from UidAllocator; Director-owned dir 0700 |
| T-03-46 | Information Disclosure | entrypoint.sh nftables | mitigate | accept allow-list, drop everything else; ICMP not allowed; logging disabled to prevent kernel-log leakage to host journal |
| T-03-47 | Elevation of Privilege | Watcher → DynamicSupervisor | mitigate | start_or_lookup_agent only starts agents whose `agent.md` parsed successfully via AgentsLoader (Task 1); rogue inbox events for nonexistent agents are ignored + audited as `agent.unknown` |
| T-03-48 | Tampering | Containerfile | mitigate | acl + nftables packages from Ubuntu 24.04 official repo; package versions pinned via `apt-get install acl=2.x nftables=1.x` (semver-major); image digest reproducible from public Containerfile |
</threat_model>

<verification>
- `mix test --exclude integration --exclude acl --exclude netavark` — full default suite green (no regressions to Plans 01-04 or Phase 1/2)
- `mix test --include inotify` — Watcher path-prefix dispatch + supervisor crash isolation green on hosts with inotify-tools
- `mix test --include acl --include podman --include integration` — kernel_acl_denial + agent_create_denial + seven_child_supervisor green on Fedora-like host with podman + acl
- `mix test --include netavark --include podman --include integration` — network_policy test green
- `mix test --include inotify --include integration` — approval_flow_test + budget_hard_stop_test green
- `podman build -t glorbo-runtime:v0.2.0-rc1 containers/glorbo-runtime/` — image builds with new entrypoint + acl + nftables packages
- `mix credo --strict` — 0 issues
- `mix format --check-formatted` clean
- All 13 must_haves.truths verified; all 6 integration tests exist; Phase A (no host deps) tests automated; Phase B (kernel-observed) tests run by Director on host
- Locked-decision deviation documented: 7 supervisor children (was D-44's "6") — explicitly noted in PLAN frontmatter so Phase 4/5 planners don't re-derive 6
</verification>

<success_criteria>
- All 13 must_haves.truths satisfied
- Six integration tests exist + tagged correctly (:acl, :netavark, :inotify, :podman, :integration)
- Containerfile bumps to v0.2.0-rc1 with acl + nftables + util-linux + entrypoint
- Company.Supervisor children list = 7 (AuditLog, Watcher, BudgetTracker, Router, Scheduler, Gate, AgentSupervisor)
- Application children list includes Glorbo.Agent.Registry as singleton
- Watcher dispatches by path-prefix to all four Plan-02/03/04 sinks (Router, BudgetTracker, Gate, Agent.Server)
- NET_ADMIN narrowly scoped (T-03-41) — negative tests on none/open
- runuser drops PID-1 root to agent UID (T-03-40) — verified in entrypoint test
- Phase 3's 9 ROADMAP success criteria addressed across plans 01-05; integration tests prove the 5 most load-bearing (SC-1 supervisor crash isolation, SC-3 router permission, SC-4 kernel ACL EACCES, SC-5 network policy egress, SC-6 budget hard-stop end-to-end)
- Director ritual table in Task 4 checkpoint documents on-host pass/fail for kernel-observed tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-routing-kernel-permissions-budgets/03-05-SUMMARY.md`. Include:
- requirements-completed: [AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, LLM-03, LLM-04 — all twelve Phase 3 requirements; pending Director's host-ritual confirmation in Task 4]
- key-decisions: 7-child supervisor (D-44 deviation documented; Gate as sibling for crash isolation); NET_ADMIN narrow scope (OQ2 final); pre-resolved CIDR allow-list (OQ3 final); UID base from /etc/subuid (OQ1 final); entrypoint applies ACLs in-container (Pitfall 1 anti-pattern avoided); runuser drops privileges; SC-6 validated end-to-end via dedicated integration test (not composition of unit tests)
- patterns-established: per-company supervisor as policy enforcement composition root; in-container ACL/nftables application via entrypoint; ETS task-frontmatter cache in Watcher for diff-driven Gate calls; end-to-end "file-write → Watcher → GenServer → Ledger → Dispatch rejection" test pattern (budget_hard_stop_test as reusable template)
- back-edits: NONE (Plan 01 already pre-positioned skills_resolved + agent_slug + outbox_root in Phase 2's airplane_mode_test)
- Director ritual table from Task 4 with pass/fail for each of the 6 kernel-observed tests
</output>
</output>
