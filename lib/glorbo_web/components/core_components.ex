defmodule GlorboWeb.CoreComponents do
  @moduledoc """
  Core UI components.

  *Phase 1 stub.* Phase 4 fills this in with the actual dashboard components
  (navigation, flash, tables, forms). We keep the module and `import` hook
  present in `GlorboWeb` so downstream phases do not need to plumb new imports
  through the `:html` macro.
  """
  use Phoenix.Component
end
