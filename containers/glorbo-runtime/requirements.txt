# Glorbo agent runtime — pinned Python AI SDK set.
#
# Authoritative list: DESIGN.md §4.2 + Phase 2 CONTEXT D-12.
# Version ranges are 2026-04 plausible; executor relaxes patch-range and
# documents in SUMMARY if pip install fails.
#
# CLAUDE.md invariant: Python NEVER runs on the host. These packages are
# resolved ONLY inside this container's build context. In-image pytest
# (/app/tests) is the sole Python validation surface.
fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx==0.27.*
litellm==1.52.*
ollama==0.3.*
huggingface-hub==0.25.*
anthropic==0.40.*
openai==1.55.*
google-genai==0.3.*
pyyaml==6.*
pytest==8.*
pytest-asyncio==0.24.*
