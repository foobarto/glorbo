# 2026-06-03 — v0.25.0 release cut

## Task picked

"Test locally first, push and cut version." Cut **v0.25.0**, rolling up
everything accumulated since v0.24.0 (2026-05-23): 6 codex+gemini security
hardening rounds + the Elixir 1.19.5/OTP 28.5 bump (pushed, #33–#39),
GEP-0053 director passphrase auth, the minimax native provider, and GEP-0054
live paperclip-instance import.

## What shipped

**Local CI-equivalent gate (before any push):** format ✓ · `mix test`
3015/0 ✓ · credo 0 ✓ · **sobelow** caught one High (Config.CSRF) ✗→fixed ·
hex.audit ✓ · deps.audit ✓ · dialyzer 168 ≤ 169 ✓.

**Adversarial pre-release review** (7-dimension workflow, 33 agents,
adversarial verify per finding): 26 findings, 25 confirmed, 1 refuted,
4 blockers — all four were the release-cut mechanics themselves (version
bump, GEP status flips + README table, CHANGELOG roll + footer, README
version line). No code defects; all security findings LOW/nit, non-blocking.

**Commit 1 — pre-release hardening** (`11d6c11`):
- `dashboard_api` pipeline gets `:protect_from_forgery` (clears the sobelow
  Config.CSRF blocker; no-op for the GET-only `/api/search`, enforces CSRF
  if a non-GET route is ever added to that cookie-authed pipeline).
- `@session_options` sets `http_only: true` explicitly (GEP-0053 D20) + a
  regression test on the `/login` Set-Cookie.

**Commit 2 — release cut + doc honesty:**
- mix.exs 0.24.0 → 0.25.0; CHANGELOG `[Unreleased]` → `## [0.25.0] —
  2026-06-03` + footer refs; README "Latest release" line.
- GEP-0053 + GEP-0054 `status: Accepted → Implemented` (+ dated history
  notes, + docs/geps/README.md table rows).
- GEP-0053 honesty: an "As-shipped deltas" note + two Behaviour-table cells
  corrected (throttle gates /login only; no `@reference_hash` timing
  flattener; logout drops the issuing cookie only, no disconnect broadcast;
  no LAN warning — loopback-only bind moots it).
- GEP-0054: H1 `GEP-54` → `GEP-0054`; D9 documents the home-leaf-trust
  trade-off.
- DESIGN.md (config.md now holds `director_password_hash`; §12
  two-credential model; `import paperclip [--as]` + GEP-0054), architecture.md
  §4 auth-layer pointer, CLAUDE.md toolchain pin 1.18.4 → 1.19.5.
- GRAPH_REPORT.md refreshed; deferred review nits logged in todo.md P2.

## Design calls I made without you

- **0.25.0 (minor), one release** rolling up all post-0.24.0 work — pre-1.0
  minor for new features + the toolchain bump.
- **Folded the doc-honesty fixes into the cut** rather than deferring: the
  two GEPs flip to Implemented, so the GEPs become the security contract and
  must be honest. Fixed the false claims; recorded refinements as deltas
  instead of rewriting the decision-log prose (which legitimately records
  design-time reasoning).
- **Added `http_only` explicit + CSRF plug** (code) rather than silencing
  sobelow — durable-secure over fast.
- **Shipping GEP-0053 with D6/D11 deferred** (your earlier open questions —
  session TTL, return_to). Both are "no vuln today" refinements; the review
  confirmed the deferrals are honest and safe. Cutting the version resolves
  them as ship-as-is; they stay visible in the GEP's Deferred block.

## Gates

Local gate green (above). Sequencing for the cut: push `main` → wait for CI
green → tag `v0.25.0` + push tag (triggers signed release + Homebrew tap).

## Skipped / not done

- The LOW/nit review follow-ups (GEP-0054 leaf lstat, companion TOCTOU,
  CHANGELOG reorder, GEP-29 backlink, assets version sed) → todo.md P2.
- No runtime test against the live MiniMax API (no key on host).

## Commit(s)

- `11d6c11` fix(security): GEP-0053 pre-release hardening
- (this commit) chore(release): v0.25.0
